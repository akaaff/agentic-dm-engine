# CLAUDE.md

Guidance for working in this repository, in the sibling repos' own convention: commands first, then an engineering log of real gotchas hit while building it - the "why" behind non-obvious decisions, not a restatement of what the code already says.

## Commands

Python 3.13, managed via [uv](https://docs.astral.sh/uv/). Non-packaged app (`tool.uv.package = false`).

- Install deps: `uv sync`
- Lint: `uv run ruff check .` / format: `uv run ruff format .` / type-check: `uv run mypy .`
- Unit tests (fully offline): `uv run pytest`
- Tests needing a live Ollama: `uv run pytest -m llm`
- Tests needing this project's own API running: `uv run pytest -m integration`
- Tests needing a CUDA GPU + the local SD-Turbo pipeline: `uv run pytest -m imagegen`
- Vendor SRD data: `uv run python scripts/download_srd.py`
- Full autoplay (2 AI companions, zero human input, judge-scored transcript, real scene images, whole campaign scene chain): `uv run python -m src.cli.play --autoplay --campaign goblin_ambush_oneshot` (default; also `wolf_den_short_arc`, `kobold_warren_full`)
- Generate a new campaign via the teacher model: `uv run python -m src.cli.generate_campaign --size {one_shot,short_arc,full} --id <campaign_id>` - writes `data/campaigns/<id>.yaml` and its encounter(s), playable via `--autoplay --campaign <id>` immediately after.
- Generate a distillation training dataset, in chunks (each writes its own `data/training/raw/<task>_seed<N>.jsonl`, safe against a mid-run crash): `uv run python -m src.cli.generate_training_dataset --task intent_parser --n 2000 --seed 1 --workers 16` (repeat with `--seed 2`, `3`, ... for more chunks), then merge+curate once into `data/training/curated/<task>_{train,val,test}.jsonl`: `uv run python -m src.cli.generate_training_dataset --task intent_parser --merge`. Both directories are gitignored/regenerable.
- LoRA fine-tune the intent-parser student (needs a CUDA GPU; `--wandb` needs `uv run wandb login`, `--push-to-hub` needs `uv run hf auth login`): `uv run python -m src.cli.finetune_intent_parser --wandb --push-to-hub` (add `--max-train 200 --max-val 50 --epochs 1` for a quick smoke test). Adapter saved to `models/intent_parser_lora/` (gitignored).
- 3-way eval (base vs fine-tuned vs teacher, held-out test set; needs a CUDA GPU + live Ollama): `uv run python -m src.cli.evaluate_intent_parser --n 300` - writes `data/training/eval_results/intent_parser_eval.md` + `_predictions.jsonl` (committed).
- Run the game/autoplay with the fine-tuned parser instead of the teacher: set `INTENT_PARSER_BACKEND=finetuned` (transformers/peft) or `finetuned_ollama` (GGUF via Ollama - the fast one, see engineering log; default `teacher`). `SCENE_IMAGES_ENABLED=0` skips SD-Turbo (frees ~3GB VRAM so a student backend can share the card with the Ollama teacher). Day 27 end-to-end re-eval (judge scores + parse latency, all backends): `uv run python -m src.cli.eval_end_to_end --runs 2` -> `data/training/eval_results/intent_parser_end_to_end.md`.
- Day 27 detour - export the fine-tuned adapter as a fast, Ollama-served GGUF model instead of transformers/peft: `uv run python -m src.cli.export_intent_parser_model` (merges the LoRA adapter, saves to `models/intent_parser_merged/`, gitignored), then convert with a local `git clone https://github.com/ggml-org/llama.cpp` (needs the full repo, not a single curled file) - `uv run --with sentencepiece --with protobuf --with pyyaml python <llama.cpp>/convert_hf_to_gguf.py models/intent_parser_merged --outfile models/intent_parser.f16.gguf --outtype f16` (no quantization needed at 0.5B), then `ollama create dm-intent-parser -f models/Modelfile.intent_parser` (a one-line `FROM <absolute path>.gguf` - the daemon resolves relative paths against its own cwd, not the shell's). `evaluate_intent_parser`'s `--ollama-model dm-intent-parser` adds it as a 4th eval column.
- Apply DB migrations (required before the API can persist anything): `uv run alembic upgrade head`
- Run the backend: `uv run uvicorn src.api.main:app --port 8000`
- Frontend (Day 17+, `web/` - Vite + React + TypeScript): `npm install` then `npm run dev` (serves on :5173); `npm run lint` (oxlint), `npx tsc -b` to type-check.

Ollama must be running on the host with `qwen2.5:7b-instruct` pulled for anything touching `tests/llm` or the live intent-parser/narrator nodes (Phase 3+). `torch`/`diffusers` need a CUDA-enabled torch build - installed via `uv add torch --index https://download.pytorch.org/whl/cu124` (plain `uv add torch` pulls a CPU-only wheel; `--index-url` instead of `--index` breaks resolution of the rest of the project's dependencies, since it replaces the default index globally rather than scoping to just this package).

## Architecture

See `README.md` for a summary and `DECISIONS.md` for the reasoning behind each major choice. Short version: `src/engine/` is a deterministic, LLM-free rules engine (dice/state/turn-order/movement/rules/conditions/character-creation/campaign); `src/graph/` wires LangGraph nodes (intent_parser, rules_engine, narrator, player_agent, judge, scene_image) around a shared `GameState`; `src/llm/` holds the Ollama client and prompt templates; `src/imagegen/` wraps the local SD-Turbo pipeline; `src/api/` is the FastAPI service (REST for character/campaign/companion data, WebSocket for live play); `web/` is the React+Vite frontend.

## Conventions to keep consistent

- **Live-verify before trusting.** Every day's plan step ends with a concrete live check (CLI/API/browser output diffed against a hand-computed fixture, or a saved eval table), not just "tests pass" - matching the pattern from `order-fulfillment-platform` and `agentic-rag-assistant`, which caught real bugs on nearly every build day.
- **Deterministic engine code gets exhaustive unit tests with hand-computed expected values; LLM/image-gen code gets live verification instead of mocking** - a mock only proves the mock is self-consistent, not that the real model call behaves as expected.
- **Commit in small, clearly-described batches**, one coherent chunk of work at a time, not one commit per day.
- Constructor-injected dependencies, matching the sibling repos' pattern.

## Engineering log (gotchas, in the order they were found)

**`uv run pre-commit run --all-files` crashed mypy with "INTERNAL ERROR", but `uv run mypy <same files>` directly never did.** Root cause: pre-commit batches/parallelizes a hook's file arguments across concurrent subprocess invocations once there are enough files, and multiple mypy processes racing on the same `.mypy_cache/` directory corrupts it - a known mypy footgun with its own documented fix. Confirmed by reproducing the exact file set pre-commit passes in one direct `uv run mypy` call (succeeded every time) versus letting pre-commit invoke it (failed). Fixed by adding `require_serial: true` to the mypy hook in `.pre-commit-config.yaml`, forcing pre-commit to run it as one non-parallel invocation.

**`from src.config import ...` didn't resolve under plain `uv run pytest`, unlike the sibling repos' `from app.config import ...`.** Neither sibling repo sets `pythonpath` in `[tool.pytest.ini_options]`, yet their tests import their top-level package fine - whatever makes that work there wasn't reproduced here (not investigated further; not worth the time). Fixed directly and explicitly by adding `pythonpath = ["."]` to `[tool.pytest.ini_options]` in `pyproject.toml`, which reliably puts the repo root on `sys.path` for test collection regardless of package-layout quirks.

**`Mapping[str, int]` did not accept a `dict[Literal["STR", ...], int]` argument under strict mypy**, even though every possible key is a plain string. `Mapping`'s key type parameter is invariant in typeshed (unlike its covariant value type), so a `Literal`-keyed dict is not considered a subtype of a `str`-keyed one. Fixed by typing `validate_standard_array`'s parameter as `Mapping[AbilityScore, int]` (the actual key type in play) rather than trying to generalize to `str` - direct dict-literal call sites in tests still type-check fine via mypy's bidirectional inference against that stricter parameter type.

**The CLI's trace printed the wrong round number for the last action of every round** - a display bug, not an engine bug, but worth remembering how it was caught. `run_scripted`'s verbose print used `state.round` *after* `resolve_action` returned, but `resolve_action` already advances `state.round` as part of the same call when a round wraps - so the action that ends round 1 printed as "[Round 2]". Confirmed the actual `Event.round` values stored in `state.events` were correct all along (captured before the turn-advance step runs) by dumping the raw event log directly and comparing against the printed trace - only the CLI's post-hoc `print()` needed fixing (capture `round_before = state.round` ahead of the call). A reminder that a convenience print statement can be wrong even when the underlying data model is right, and it's worth checking which one actually is before "fixing" anything.

**`fastapi.testclient.TestClient` emits a `StarletteDeprecationWarning` pointing at a package called `httpx2`.** Confirmed by reading the installed `starlette` (0.46.2) source directly: `starlette.testclient` now tries `import httpx2 as httpx` first, falling back to plain `httpx` with the deprecation warning. Not chased down further on Day 8 - it's a warning, not a failure, and the tests pass either way - but worth revisiting if `httpx2` turns out to be the real forward path before this project accumulates many more `TestClient`-based tests.

**The SRD's Bard class has two separate proficiency-choice pools (3 skills + 3 musical instruments), and `character_creation._validate_skill_choices` sums/pools every `proficiency_choices` entry together** - not obvious from the schema, and easy to get wrong when authoring a Bard companion. Confirmed live: calling `create_character` for a Bard with only 3 skill choices raises `"Bard requires exactly 6 skill choice(s), got 3"` - the fix is passing 6 items total (3 real skills + 3 instrument indices like `lute`/`flute`/`lyre`) in `chosen_skills`, which the validation accepts because it only checks pool membership, not semantic category. `data/companions/pip_larkspur.yaml` documents this inline since it's the one companion that hits it.

**A newly-connecting WebSocket client could silently never receive its own initial `awaiting_input` message.** `_send_awaiting_input(session)` searches the whole session for the *first* connection controlling the current actor and sends to just that one - correct for "an action resolved, tell whoever's turn is next," but it was also being called on every new connection's own setup, which means a second connection joining a session where an earlier connection already controls the same actor (true for every connection under Day 11's "one connection controls everyone" simplification) would find the *earlier* connection as the match and send it a duplicate, while the newly-connecting client got nothing. Caught live: a real two-connection test against an actual running server hung waiting for the second connection's `awaiting_input` that was never coming. Fixed by sending a personal, connection-scoped awaiting_input directly to a client on its own connect (checking only whether *that* connection controls the current actor), separate from the shared session-wide search used after an action resolves.

**Starlette's `TestClient` WebSocket transport deadlocks with two connections open at once, even from separate `TestClient` instances** - draining all of one connection's queued messages before touching the other's blocks the server mid-broadcast (it looks like a rendezvous-style channel, not an arbitrarily-buffered queue, though this wasn't fully root-caused). The fix wasn't message-ordering tricks - genuine multi-connection tests now spin up a real `uvicorn.Server` in a background thread and use the real `websockets` client library for actual concurrent connections; `TestClient` is kept for single-connection tests, where it works fine.

**The intent-parser prompt only explicitly explained "attack"/"move"/"dash"/"invalid" in its Rules section - even though every verb appeared in the JSON schema's enum - and `qwen2.5:7b-instruct` defaulted to "invalid" for "I dodge incoming attacks", "I disengage and back away from combat", and "I use a healing potion" (dodge/disengage/use_item), all reasonable unambiguous player intents.** Confirmed live via the golden-set test (`tests/llm/test_intent_parser.py`) failing on exactly those three cases, passing on attack/invalid (the two verbs the prompt actually described). The schema enum alone wasn't enough context for the model to map free text onto the less-common verbs - fixed by adding an explicit one-line description of every verb to the prompt (not few-shot examples, which weren't needed here); all 7 golden cases passed after that. Same underlying lesson the sibling `agentic-rag-assistant` repo hit with its router prompt ("zero-shot unreliable, needs explicit guidance for the less obvious branches") via a different mechanism (there it needed few-shot examples, here a verb legend sufficed).

**An unhandled `TurnEngineError`/`NotImplementedError` from the graph crashed the whole WebSocket connection instead of failing gracefully.** Caught live during Day 12's free-text verification: the LLM extracted `item_or_spell: "dagger"` for a *monster's* attack (a goblin has no action named "dagger" in its SRD stat block - only "Scimitar"/"Shortbow"), and separately, the intent-parser prompt describes 10 verbs (including dodge/cast_spell/skill_check/use_item/help/death_save) but `turn_engine.resolve_action` only actually implements attack/move/dash/end_turn (Day 7's deliberate scope) - both raised uncaught exceptions that killed the connection. Fixed by catching both exception types around the graph invocation in `api/ws/session.py` and sending an `{"type": "error", "detail": ...}` message back to the sender instead - safe because `resolve_action` validates before mutating in both failure paths, so the turn simply doesn't advance and the same actor can retry. The underlying gap (most verbs not yet implemented in the rules engine) is real and not fixed here - only the failure mode (crash vs. a reportable error) is.

**An intent-parser utterance that doesn't name a target lets the LLM pick badly** - "The goblin attacks again" (no target named) resulted in `goblin_2` attacking `goblin_1`, a friendly-fire mistake, because the prompt's "visible characters" list has no notion of sides/allegiance for the model to reason about. Not fixed - noted as a real limitation of routing monster turns through the same free-text intent-parser built for interpreting a *human's own* stated intent; monsters likely need their own simple targeting logic (or a dedicated prompt that already assumes "attack a PC") rather than guessing from an under-specified command, whenever monster-turn AI becomes its own concern (Day 13's player_agent work is the closest existing hook, but it's scoped to companions, not monsters).

**A character's `skill_proficiencies` could contain a literal duplicate entry** when a class-chosen skill overlapped with the background's automatically-granted one - caught immediately by adding the obvious test: Pip Larkspur (Bard, Acolyte) chooses `skill-insight` *and* Acolyte grants `skill-insight` automatically, so the naive `chosen + background` concatenation produced `["skill-insight", "skill-insight", ...]`. Harmless for the `in` membership check `_resolve_skill_check` actually does, but still wrong data - fixed with `dict.fromkeys(...)` (preserves order, dedupes) rather than a plain list concatenation.

**In-character phrasing for a skill check ("I try to intimidate the goblin") was misclassified as "invalid", while the mechanically-worded version ("I attempt an intimidation check") worked correctly** - confirmed by calling `intent_parser_node` directly with both phrasings side by side. The skill_check verb's prompt description asked for "something needing an ability/skill check" without examples mapping ordinary action verbs (intimidate, sneak, search, persuade, climb) to skills, so the model needed the word "check" to make the connection reliably. Fixed by adding explicit verb-to-skill examples to the prompt (same fix shape as Day 12's verb-legend gap, different specific cause - there the model needed a verb *description* it lacked, here it needed the *mapping* from natural phrasing to the abstract category). Locked in as a golden-set regression case.

**`use_item` rejected a perfectly valid request because of word order**: the real LLM parsed "I drink my healing potion" into `item_or_spell: "healing potion"` (natural adjective-noun order), but `_resolve_use_item` was exact-matching against the canonical SRD name `potion-of-healing` (noun-of-noun order) - normalizing case/hyphens wasn't enough since the *words themselves* are in a different order. Confirmed live by calling `intent_parser_node` directly with several natural phrasings before touching the engine. Fixed with a word-set match (`{"healing","potion"} <= set(words)`) instead of an exact string comparison - the same class of "don't assume the canonical name" lesson as Day 12's verb-legend gap, applied to item names instead of verbs.

**The SRD 5.1 only includes one background: Acolyte.** Confirmed live on Day 1 by actually inspecting `data/srd/5e-SRD-Backgrounds.json` after downloading it (1 entry) - not assumed from general D&D knowledge, which would suggest Soldier/Criminal/Folk Hero etc. are available too (they're not SRD content, they're in the core Player's Handbook, which this project deliberately doesn't use). Day 5's character creation wizard needs to either offer Acolyte only, or the project authors a small number of original (non-WotC) backgrounds itself - mechanically simple (skill proficiencies + equipment + one feature) and safe to invent, unlike races/classes/monsters/spells which stay strictly SRD-sourced. Decide this explicitly on Day 5 rather than assuming background variety exists.

**Day 15's first real autoplay run (2 AI companions, zero human input) hung forever on the very first non-trivial persona.** Root cause #1: `player_agent`'s initial prompt asked a companion to "declare what it does" without ever enumerating the actual verb set, so a "quiet, watchful ranger, slow to warm up to strangers" persona (Silvana) happily generated pure atmosphere - "stays alert, watches the shadows" - forever. Since Day 14 deliberately made "invalid" a no-op that *doesn't* cost a turn (the right call for a human who can just retry), a companion with no human to unstick it just looped on the same non-action every turn. Fixed by rewriting `player_agent.md` to enumerate the same verb set intent_parser's own prompt uses and require exactly one concrete action, plus a `consecutive_invalid` circuit breaker in `cli.play.run_autoplay` that forces `end_turn` after 3 straight non-actions (defense in depth - the prompt fix alone isn't provably 100% reliable against a 7B model). Root cause #2, found immediately after fixing #1: the forced `end_turn` fallback itself was rejected once a companion went unconscious, because `resolve_action`'s top-level guard only accepts `"death_save"` while `hp<=0` - so the circuit breaker's own fallback action failed too, forever, the same infinite loop one layer down. The real fix belongs in `player_agent_node` itself (not just the autoplay script, since live WebSocket play would hit the identical hang the moment a companion goes down): an unconscious-but-not-dead companion is now forced straight to a `death_save` action, skipping the LLM call entirely - there's no narrative content to role-play in "try to stay conscious" anyway. Also gave `run_autoplay` the same `except (TurnEngineError, NotImplementedError)` resilience `api/ws/session.py` already had for human free text (Day 12) - an AI-declared move can still name a non-adjacent destination and get rejected by `movement.py`, and there's no human to phrase it better on retry, so the driver just logs the failure and tries again rather than crashing the whole run.

**Companion-vs-companion friendly fire, confirmed live on the same Day 15 autoplay run, then fixed.** Silvana (an AI companion) attacked Grom (a different AI companion, on her own side) twice in one encounter. This is the same underlying gap Day 12 already flagged for monster free text - `intent_parser`'s "visible characters" list has no notion of allegiance, so an under-specified utterance can resolve to any character id, ally or enemy - now confirmed to hit companion-vs-companion too, not just monster-vs-monster. Rather than patch it at the prompt layer again (no zero-shot prompt tweak can be *guaranteed* to stop a small model from occasionally picking a bad target), fixed it once at the one place both `_resolve_attack` and `_resolve_cast_spell` funnel through: a new `_validate_attack_target` rejects any attack where `target.is_pc == actor.is_pc` (same side, whichever side) before dice are ever rolled - a real game-rule the deterministic engine should own regardless of which upstream LLM call produced the bad target. Two hand-computed regression tests added (PC-attacks-PC, monster-attacks-monster) plus a cast_spell equivalent; the pre-existing "not fixed" monster-vs-monster note above is now closed by this same guard.

**`tests/llm/test_autoplay.py` failed on its first automated run** by hitting `run_autoplay`'s `max_turns` safety valve, even though a manual `--autoplay` run moments earlier finished cleanly (victory in 16 turns). Not a hang or a logic bug - confirmed the turn-advancement invariant holds (a failed/invalid action never advances `state.current_turn`, so the *same* stuck actor is correctly retried and the `consecutive_invalid` circuit breaker does eventually fire for it) - just LLM sampling variance: even with the Day 15 prompt fix, a companion's declaration occasionally still fails to parse, and each such failure burns a turn without landing an attack, which can stretch an already-unlucky combat (bad dice on top of wasted turns) well past a tight turn ceiling. Raised `max_turns` from 200 to 400 as a generous-on-purpose margin rather than chasing full reliability out of a small local model's free-text generation - consistent with `tests/llm/test_intent_parser.py`'s own framing ("LLM output isn't byte-deterministic... tolerant of everything else"). This test can still occasionally need a rerun; that's expected for anything exercising live sampling, not a regression to chase to zero.

**Day 16 - `uv add torch` alone installs a CPU-only wheel; getting the CUDA build needs an *additional* index, not a wholesale replacement of the default one.** `uv add torch --index-url https://download.pytorch.org/whl/cu124` failed dependency resolution entirely - `--index-url` replaces the *default* index for the whole resolve, so `alembic` and everything else in the project suddenly couldn't be found (PyTorch's wheel index only hosts PyTorch-ecosystem packages). Fixed with `uv add torch --index https://download.pytorch.org/whl/cu124` instead, which resolved and installed `torch==2.6.0+cu124` correctly. Note this is *not* as narrowly scoped as it sounds, though: `uv.lock` shows several common transitive deps (certifi, idna, packaging, requests, urllib3, numpy, pillow...) also got pinned to the pytorch index's mirror rather than PyPI, since `--index` adds it as an extra general-purpose index for the whole project, not a source pin restricted to torch alone - confirmed by grepping `uv.lock` for `download.pytorch.org` and finding ~35 packages attributed to it, not just torch. Not a problem in practice (the lockfile pins exact versions either way and everything resolved/tested clean), but worth knowing before assuming `--index` scopes as tightly as `[tool.uv.sources]` normally would. Confirmed live: `torch.cuda.is_available()` -> `True`, `torch.cuda.get_device_name(0)` -> the RTX 3080.

**The critical VRAM-coexistence check (this day's actual gate) passed, but the first measurement attempt gave a false "barely fits" reading purely from tool-timing mistakes, not anything about the GPU.** Querying `nvidia-smi` from separate sequential tool calls around a backgrounded generation script kept catching the process either before it had started or after it had already exited and freed its VRAM - the fix was mundane (run the generation script and a polling `nvidia-smi` loop inside *one* shell invocation, backgrounding the python process with `&` and polling every 3s), not anything about the measurement itself. Once measured correctly: baseline with only `qwen2.5:7b-instruct` loaded was ~5.85GB; with SD-Turbo's pipeline also loaded and a generation in flight, combined usage held steady at ~9.55GB out of 10GB - confirmed live, not estimated (see DECISIONS.md #5). Separately, a `uv run python <script>` invocation using an absolute script path outside the project directory needs either `cd`-ing into the project first or `PYTHONPATH` set explicitly - `pythonpath = ["."]` in `pyproject.toml` only affects `pytest` collection, not arbitrary script runs, so `from src...` failed with a plain `ModuleNotFoundError` until one of those was added.

**A real, load-bearing bug in the VRAM guard: after the very first successful generation, every subsequent scene image for the rest of a session was silently skipped with "Not enough free VRAM."** Caught live running `--autoplay` with real image generation for the first time - only round 1 got an image, rounds 2 onward all logged the skip warning despite the GPU being nowhere near actually out of memory. Root cause: `_has_enough_vram()` was being called before *every* `generate_scene_image()` call, but it checks `torch.cuda.mem_get_info()`'s free-bytes figure - which reports memory free to the *driver*, not to this process specifically. Once SD-Turbo's pipeline is loaded, PyTorch's caching allocator retains the VRAM blocks it already grabbed so it can reuse them for the *next* inference without asking the driver again - which is the whole point of the allocator, but it means "free" stays low from then on even though the already-loaded pipeline needs zero *new* memory to run again. Fixed by moving the VRAM check into `_load_pipeline()` (an `@cache`'d function that only ever runs once per process) so it gates the one-time decision "should we even attempt to load the model" rather than being re-asked on every call; a genuine later OOM is still a real exception caught by `generate_scene_image`'s existing broad except. Re-ran the full autoplay afterward and confirmed all 10 rounds got a scene image, not just the first.

**Verified live: `scene_image_node`'s prompts, built from `game_state.encounter_id` + a same/enemy-labeled character-and-HP summary (no access to the actual `Scene.narrative_intro` text, which isn't threaded into `GraphState` yet), still produced 10/10 images that were visibly on-theme** - armed fighters clashing with green goblin-like creatures in rocky/forested terrain, consistent with "goblin ambush" - spot-checked by eye (5 of 10) rather than assumed. SD-Turbo's 1-step inference can't render specific narrative details (a character being unconscious, an exact HP count), which is an accepted limitation of the "turbo" distillation at this step count, not a prompt bug.

**Day 17 - the Browser pane's `preview_start` reads `.claude/launch.json` relative to this session's own primary working directory (the user's home directory), not wherever a shell has `cd`'d to.** Adding a project-local `.claude/launch.json` in `agentic-dm-engine/` had no effect - `preview_start({name: "backend"})` silently ran an unrelated, pre-existing "web-client" config for a *different* project (`order-fulfillment-platform`) found in the home-directory `launch.json`, since that was the only entry and the name didn't match. Fixed by adding entries to the home-directory `.claude/launch.json` instead, using `uv run --directory <absolute path> ...` and `npm --prefix <absolute path> ...` so each command doesn't depend on any particular starting cwd. Kept the project-local `.claude/launch.json` too (with plain relative commands) since a plain terminal session started with cwd already inside the project would resolve it correctly.

**A brand-new character-creator submission failed with a browser-reported CORS error that had nothing to do with CORS.** The actual cause (visible only in the backend's own logs, not the browser) was `sqlite3.OperationalError: no such table: characters` - the dev SQLite file existed but had never had `alembic upgrade head` run against it in this checkout. Confirmed by reading `preview_logs` for the backend server, not by chasing the CORS message: FastAPI/Starlette's `CORSMiddleware` only attaches `Access-Control-Allow-Origin` to responses that complete normally through it; an unhandled exception (this SQLAlchemy error isn't an `HTTPException`, so `ExceptionMiddleware` doesn't catch it either) propagates past `CORSMiddleware` to the outer `ServerErrorMiddleware`, which converts it to a 500 *without* CORS headers - so the browser reports the symptom (missing CORS header) rather than the cause (a 500). Worth remembering for any future "CORS blocked" report on a route that otherwise has working CORS: check the server's own logs for an unhandled exception before touching the CORS config at all.

**A real, silent data-loss bug in the character-persistence layer, caught by the wizard's own re-fetch-after-create check** (exactly the kind of check Day 17's plan called for, and exactly why it's worth doing for real instead of assuming persistence works). `CharacterRecord` (the SQLAlchemy model, Day 8) predates `Character.class_index` (Day 14, needed for spellcasting-ability lookup) and `Character.skill_proficiencies` (Day 13) - neither field was ever added to the DB model or the two mapper functions (`_character_to_record`/`_record_to_character`), so every character ever created through the API silently lost both fields the moment they round-tripped through the database, for every class, not just spellcasters. First created a Fighter (no spells to lose) and didn't notice; only caught it by deliberately re-fetching and comparing against the create response, which is the actual point of that verify step. Fixed by adding both columns (`skill_proficiencies` needed `server_default='[]'` in the migration specifically - SQLite refuses to add a `NOT NULL` column with no default to a table that already has rows, caught immediately by just running the migration against the existing dev DB) and updating both mapper functions. Re-created a Wizard afterward and confirmed `class_index` and all four `skill_proficiencies` (2 chosen + Acolyte's 2 automatic) survived a real re-fetch.

**Day 18 - a character's `inventory` can contain the same equipment index repeated N times for a stacked item** (a Ranger's starting kit includes a quiver of 20 arrows, which `character_creation.py`'s `inventory.extend([index] * quantity)` expands into 20 separate `"arrow"` entries rather than one `"arrow"` with a count). Noticed live creating a Ranger through the wizard - the result sheet's inventory line read as 20 repeated words. Not a bug (the underlying data is correct and this expansion is pre-existing, documented Day 5 behavior - a flat list of item instances, not a quantity map) and out of scope to fix today - flagged here so a future inventory-display day (frontend `CharacterSheet.tsx`, or the engine itself) knows to group-and-count rather than assuming one entry means one item.

**Day 19 - `companion_ids` from the API (`Character.id`, e.g. `"companion_grom"`) and `load_companion_spec`'s argument (a YAML filename stem, e.g. `"grom_ironfist"`) are two different namespaces for the same companion, and conflating them was a real, silent bug.** Wiring a real session's party from a `CampaignProgress` row called `load_companion_spec(companion_id)` directly using the API-facing id - which raised `FileNotFoundError` (no `companion_grom.yaml` exists) inside `_build_real_session_game_state`'s defensive `try/except`, so the whole session silently fell back to the Day-7 demo skirmish instead of erroring loudly. Caught by an offline test asserting the exact character-id set in the resulting encounter (thorin/companion_grom/goblin_1-3), which failed with the demo's elrond/goblin_1/goblin_2 instead - a good example of why that kind of exact-set assertion is worth writing even when "it looks like it should work." Fixed with a new `load_companion_spec_by_character_id` lookup in `engine/companions.py`, used everywhere outside that module that only ever sees the roster via `Character.id`.

**Day 19 - a rejected WebSocket action (`TurnEngineError`) left the human unable to ever retry, even though the server-side comment already said "let them try again."** The turn correctly stays with the same actor (nothing mutated before the error), but `_handle_client_message`'s error path only sent `{"type": "error"}` and returned - it never re-sent `awaiting_input`. The frontend's `sendPlayerAction` optimistically clears its own "it's my turn" flag the instant it sends (so a second click can't double-submit while waiting), and had nothing to restore it once the error came back - so the input box stayed disabled forever after exactly one rejected action. Confirmed live: attacking with `"my fireproof toaster"` (not a real weapon) correctly showed the error text but left the box dead until this was fixed by also calling `_send_awaiting_input(session)` on the error path; verified the exact same session could then retry with a real weapon and the attack resolved normally.

**Day 19 - two false alarms while testing live, both worth recording so they're not re-chased next time.** (1) A long-lived browser tab's WebSocket appeared to stop receiving messages after many `preview_logs`/wait/screenshot cycles during debugging - independently verified via a second diagnostic WebSocket connection to the *same* session that the server-side state was completely correct the whole time, so this was environment/tooling flakiness in the extended debugging session, not an application bug; a fresh tab with a tighter test loop reproduced nothing wrong. (2) Several `computer{action:"screenshot"}` calls rendered a visually "stale-looking" frame (a disabled-vs-enabled input looking identical under the dark theme) that read as a bug at a glance - reading the actual DOM (`input.disabled`, `.textContent`) directly via `javascript_tool` settled it immediately and should be the first move when a screenshot's *meaning* is ambiguous, not just when it might be out of date.

**Day 20 - the Browser pane's `computer{action:"left_click", coordinate:[x,y]}` did not register real clicks on the SVG combat grid at coordinates independently confirmed correct via `document.elementFromPoint`.** Verified the target `<rect class="grid-cell clickable">` really was at that point in the page (`elementFromPoint` returned it), yet the simulated click produced no server round-trip (no narration, no error, no position change) - while a JS-dispatched `new MouseEvent('click', {bubbles:true})` on the same element worked immediately and correctly. Root cause not pinned down further (likely something about how this tool's pointer-event simulation interacts with SVG hit-testing or a coordinate-frame mismatch after `resize_window`, which separately also broke ref-based `computer` clicks entirely until the tab was returned to its default size) - treated as a testing-tool limitation, not application code, since a real DOM click event (what an actual mouse click produces) worked every time. When a `computer` click on a small/precise target silently does nothing and the element is confirmed present at that point, dispatching a synthetic click via `javascript_tool` is a reliable fallback for verification purposes.

**Day 21 - `resize_window` to a custom size intermittently breaks `computer` clicks/keypresses on regular DOM elements too, not just the SVG grid.** After resizing to 1100x750 for a wider portfolio screenshot, a `computer` click on the free-text action input followed by `type` and a `key: "Return"` submit produced no server round-trip at all - confirmed via `document.activeElement`/`input.value` that the click never actually focused the input the first time, and separately that pressing Return in a focused, correctly-filled input did not trigger the form's native submit-on-Enter behavior in this environment. Resetting to the `desktop` preset made plain `computer` click+type work normally again immediately. Settled the actual verification by calling `form.requestSubmit()` directly via `javascript_tool` once the input was confirmed genuinely focused and filled - same lesson as Day 20's SVG finding: a JS-dispatched interaction is the reliable fallback once a `computer` action's effect can't be confirmed through the DOM, and it's worth checking the DOM state instead of trusting that a click "must have landed."

**Day 21 - a live playthrough action can genuinely take 15-20+ seconds of real wall-clock time to fully resolve, well past what looks like "probably done by now."** After Cassian's attack, checking narration/character-sheet state after an 8s wait showed the *narration* for his kill already appended but the *character sheets* still showing the target goblin alive and Cassian still marked as the current actor - looked exactly like Day 19's WS-message-ordering false alarm at first glance. Waiting another ~8s and re-reading the same DOM showed everything fully caught up and consistent (goblin marked dead, round incremented, turn correctly back to the human) - the delay was real: `_autoplay_non_human_turns` chains several sequential live LLM calls (narrator + intent_parser per companion/monster turn) after the human's own action, and narration for the human's action can visibly arrive well before the whole chain finishes. Worth remembering before re-diagnosing an "inconsistent state" finding as a bug: check whether enough real time has passed for however many sequential LLM calls are queued up, not just whether a fixed short wait "should" have been enough.

**Day 22 - both `cli.play.run_autoplay` and `api.ws.session._build_real_session_game_state` had, since Day 15/19, silently walked a campaign's scene chain straight to the first combat scene and skipped everything else - their own comments already flagged this as "Day 22's job."** Filled the gap with a new `campaign_runner.py` module deliberately kept outside `turn_engine`/`GameState`: narrative-beat and skill-challenge scenes have no turn order or battle map, so forcing them through combat-shaped machinery would mean fabricating fake turn/round bookkeeping for nothing. Skill-challenge narration is authored per-scene (`SkillChallengeDef.success_text`/`failure_text`), not LLM-generated, matching how `narrative_intro` already works - only in-combat turn narration goes through the narrator LLM. `run_autoplay` now loops over the whole chain (walk to next combat via `campaign_runner`, play it via the existing per-encounter turn loop, continue from wherever that scene points next) rather than playing exactly one encounter, and takes a `campaign_id` so any campaign can be autoplayed.

**Day 22 - live-verifying `kobold_warren_full` surfaced a real, pre-existing (Day 14) simplification rather than a new bug: an attack against an already-unconscious party member has no special effect** (the SRD rule that a hit on a helpless creature auto-crits and counts as two automatic death-save failures was never implemented - Day 14's scope was explicitly "single-target attack-roll spells... a single healing potion," not the full unconsciousness rule set). A monster's attack against a downed PC just rolls to hit and deals damage that's clamped at 0 HP, doing nothing further - the downed character's fate is decided purely by their own independent death-save rolls each turn, uncoupled from whether they keep getting hit. This made one live combat (both companions going down near-simultaneously against a still-healthy third kobold) run 353 turns/119 rounds before ending in a real, correctly-detected defeat - confirmed not a bug by checking `_resolve_attack`/`_resolve_death_save` directly (no unconscious-specific branch exists) and by the run terminating cleanly well under the `max_turns=400` safety valve with no invariant violations. Not fixed - flagged here since it's a legitimate 5e-rules gap that would make combat pacing more realistic if a future day touches death saves again, same spirit as the Day 18 inventory-stacking note.

**Day 23 - constraining `monster_index`/`skill` to a dynamically-built `Literal` (turned into a JSON-schema `enum`) instead of a plain `str` eliminated an entire class of retries, confirmed by all 3 live generation runs succeeding on the first attempt with zero retries needed.** The only retries the validate-and-retry loop actually has to handle now are cross-field business rules a JSON schema can't express (scene count, combat-scene count) - genuinely rare in practice, since a 7B model given clear numeric guidance in the prompt mostly gets a simple count right. Worth remembering generally: whenever a structured-output field's valid values are a known closed set at generation time (even a set computed at runtime, not hardcoded), building the schema with that exact set as a `Literal`/enum is strictly better than a plain `str` field plus post-hoc validation - it makes the invalid case structurally unreachable instead of just detectable.

**Day 23 - a background shell process I'd started with plain `&` (not the harness's own background-task tracking) got orphaned when its parent tool call returned, and kept holding ~5GB of VRAM from a half-loaded SD-Turbo pipeline indefinitely** - `nvidia-smi` showed only 371MB free (down from the expected ~4GB+), and a subsequent real autoplay run crashed with a CUDA OOM *inside `torch.cuda.mem_get_info()` itself*, before ever attempting to load anything - apparently even querying free memory can fail if there isn't enough headroom left for CUDA to do the query. Found by checking `nvidia-smi`'s process list directly (a stray `python3.13.exe` computing on the GPU, distinct from Ollama's `llama-server.exe`) rather than assuming the GPU itself was the problem; `taskkill`ing that one orphaned PID immediately freed the memory and the next run succeeded normally. Not an application bug - a reminder that this project's own background-verification runs need the harness's tracked backgrounding (which reliably reports completion/failure), not ad-hoc shell `&`, specifically because an orphaned GPU-resident process doesn't announce itself the way a crashed foreground command does.

**Day 24 - the generic toolkit's actual "is this genericity real" test needed a task that couldn't accidentally share assumptions with `ParsedAction`.** Reused the plan's own suggested toy task (extract a person's name+age from a sentence, a 2-field schema with nothing resembling a game action) specifically so the pipeline couldn't pass by coincidence - a toy schema that happened to share `ParsedAction`'s shape (a `verb`-like field, a `target`-like field) wouldn't have proven the boundary was real. Ran clean on the first live attempt: all 3 pipeline stages (generate/curate/evaluate) worked against a schema and prompt the code had never seen, no changes needed.

**Day 25 - a synthetic scenario's placeholder character id, the literal word "actor", confused the teacher into mislabeling `ParsedAction.actor` in 146/300 (49%) of a real generated dataset.** Caught by the plan's own "spot-check 10 examples" verify step - the mismatch pattern (the model inventing "actor_0"/"actor_1" or substituting a different visible character's id) only ever happened when the expected id was literally "actor", confirmed by grepping all 146 mismatches. Not a production bug (the real game only ever uses proper name-based or slug ids - "thorin", "companion_grom", "goblin_1" - never the bare word "actor"), but bad training signal, since `resolve_action`'s real actor-mismatch check (`TurnEngineError` "It is X's turn, not Y's") would reject exactly this pattern if it ever occurred live, so training on it would teach a lesson about a case that can't happen at inference time. Fixed by giving the synthetic actor a name-based id instead of the generic placeholder - regenerating dropped the mismatch rate to 7/300 (2.3%, mostly harmless casing slips), in line with ordinary 7B noise already accepted elsewhere in this project. General lesson, worth remembering beyond this one field: a synthetic-data placeholder that reads as an English word/role description rather than a plausible real value can itself become the confound, not the thing actually being tested.

**Day 25 follow-up - scaled the intent-parser dataset from 300 to 10,000 examples, verified Ollama's concurrency and its real ceiling empirically rather than assuming a documented default.** `OLLAMA_NUM_PARALLEL` has no hard max - it's an unbounded env var, limited in practice by GPU memory (KV cache scales with parallel slots x context length). A trivial "say banana" prompt scaled cleanly to 64 concurrent requests with zero errors and flat VRAM, but that doesn't reflect the real workload - measured directly against this project's actual intent-parser prompt/schema (longer context, grammar-constrained JSON output) instead: 8 sequential real calls took 23.2s, 16 concurrent took 11.1s for double the work (~4x effective throughput, with real per-call variance 4-11s, unlike the flat trivial-prompt result) - the number that actually mattered for planning the generation run, not the toy benchmark. Also found that scaling example *count* alone wouldn't have bought much: the original template banks were only 3-7 phrasings per category, so more volume on the same templates just repeated ~40-50 sentences with different game-state context glued on. Expanded every category's phrasing bank substantially before scaling up - the final 10k-example run showed 870 unique surface utterance texts (vs. a linear extrapolation of ~150 unique texts if the old narrow banks had just been resampled 33x more).

**Day 25 follow-up - `generate_dataset`'s concurrency needed prompts built sequentially before any threading starts, not interleaved with it.** `task.generate_prompt()` closures typically wrap a shared `random.Random` instance (see `intent_parser_task.py`), and `random.Random` isn't safe to call from multiple threads at once - a naive "dispatch generate_prompt+chat_structured together per worker thread" design would have produced a subtly corrupted/non-reproducible random sequence. Fixed by building the full list of `n` prompts up front on the calling thread (fast, pure computation) before handing only the slow I/O-bound teacher calls to a `ThreadPoolExecutor` - no shared mutable state touched from more than one thread at any point. Ran 5 chunks of 2,000 (seeds 1-5, 16-way concurrency each) with zero validation failures and zero crashes across all 10,000 examples; merged and curated into a leak-free 7,000/1,500/1,500 split with zero duplicates found even across all 5 independently-seeded chunks (expected - see the actor-id-fix entry above: full prompts embed enough randomized game-state detail that exact-string collisions are vanishingly unlikely, so cross-chunk dedup is a cheap safety net, not something doing real work here). Final actor-mismatch rate across the whole 10k: 49 (0.49%), consistent with the 300-example post-fix measurement.

**Day 26 - `uv add trl` silently resolved to trl 0.11.4 + `datasets==1.1.1` (2020-era), which then crashed on import against the modern `pyarrow` in the env (`module 'pyarrow' has no attribute 'PyExtensionType'`).** Root cause was Day 16's cu124 PyTorch `--index`: it mirrors its own (stale) copies of common packages like `requests`, and uv's default "first index wins" dependency-confusion-safe resolution then refuses to consider PyPI's newer `requests` once it finds a match there - forcing every dependency that needs `requests>=2.32.2` (modern trl/datasets) down to an ancient version. Fixed with `index-strategy = "unsafe-best-match"` in `[tool.uv]` - uv's own suggested fix, and safe here because both indexes are ones this project explicitly configured and trusts (official PyPI + official PyTorch), not the untrusted-third-party scenario that flag guards against. Resolved cleanly to trl 1.12.0 + datasets 5.0.1 afterward. General lesson: a loose extra `--index` doesn't scope as tightly as `[tool.uv.sources]` and can quietly hold back unrelated dependency upgrades months later.

**Day 26 - the fine-tune worked on the first real run; the informative comparison was making the base-model column a fair fight.** Qwen2.5-0.5B-Instruct fine-tuned on the 10k dataset (3 epochs, LoRA r=16, bf16, no quantization, ~56 min on the RTX 3080) went from **0% valid `ParsedAction` / 13% verb accuracy** (base, zero-shot on the production prompt) to **100% valid / 96% verb / 98.4% overall field accuracy** - within 0.6pp of the 7B teacher's 99.0%. The base 0.5B model isn't broken - it emits valid JSON 100% of the time - it just doesn't know the schema contract (drops the required `actor` field, so *nothing* validates as a real `ParsedAction`). That 0% is the honest number for "drop this model into production as-is on the one prompt the game actually sends," which is exactly the question the distillation answers. Eval deliberately runs base and fine-tuned through plain `transformers.generate()` (no grammar constraint) while the teacher gets Ollama's constrained decoding - so the table reports valid-JSON rate, strict-ParsedAction rate, and field accuracy separately rather than one blended score that would let "learned to format JSON" masquerade as "learned the right verb". Weakest fields for the fine-tune (and the teacher): `params.path` (move coordinates, ~86% vs ~89%) and `params.skill` (skill-name inference, ~86% vs ~93%) - the two that need real value synthesis rather than copying a token from the prompt.


**Day 27 - swapping the distilled student into the live loop worked; the latency win didn't materialise, and the reason is the interesting part.** `config.INTENT_PARSER_BACKEND=finetuned` routes the live `intent_parser` node to `llm/local_parser` (base + LoRA via transformers/peft) instead of the Ollama teacher. A full in-browser playthrough confirmed it: free-text "I attack goblin_1 with my longsword" and "I try to intimidate the goblins into fleeing" both parsed correctly and resolved, and every companion turn auto-played through it across three rounds. Judge scores over 2 autoplay runs each: fine-tuned [6,7] vs teacher [7,7] - equivalent play, the swap doesn't degrade the game. But parse latency was a wash: student 2.7s vs teacher 2.9s median, despite 14x fewer parameters. The naive `transformers.generate()` path (bf16 weights, per-token Python overhead, no exact stopping) gives up almost everything the smaller model should win - first pass was 5.5s until `merge_and_unload()` + `max_new_tokens` 256->96 + an explicit `<|im_end|>` stop token got it to 2.7s. The real win needs the student on the same engine as the teacher (llama.cpp: q4, fused kernels, grammar-constrained stopping). Ollama 0.33's experimental safetensors importer can't do it on Windows (needs Apple MLX), and the standalone llama.cpp GGUF converter is no longer a single self-contained script - so GGUF->Ollama stays a Phase 8 item. Worth remembering: at the 0.5-7B scale on one machine, the inference stack dominates the parameter count for latency - "distill to something smaller" only pays off in speed if you also serve it as efficiently.

**Day 27 - one shared limitation surfaced live, not a fine-tune regression: a relative target reference ("the closest goblin") parsed as `verb=attack` with `target=null`, which `_resolve_attack` rejects ("attack action requires a target").** The teacher hits this too - resolving "closest"/"nearest" to a specific character id needs spatial reasoning the prompt doesn't spell out. Naming the id explicitly ("goblin_1") worked first try. Not chased further; it's a prompt-design gap that predates the distillation.

**Day 27 detour - the GGUF conversion Day 27 abandoned as Windows-blocked wasn't actually Windows-blocked; it was one incomplete download.** Re-attempted by cloning the *full* `llama.cpp` repo (not `curl`-ing the single `convert_hf_to_gguf.py` file, which is what actually failed on Day 27 - it imports a local `gguf-py` package and a `conversion` package that only exist alongside it in the repo). With the full clone, conversion to F16 GGUF (no quantization - unnecessary at 0.5B, and skipping it also means skipping the C++ build toolchain entirely, no `cmake`/`llama-quantize` needed) ran locally on Windows in under 5 seconds, using the project's *existing* venv (torch/transformers already installed) plus three lightweight ephemeral deps via `uv run --with`. Importing the resulting `.gguf` into Ollama hit a second, unrelated snag: `ollama create` reported "invalid model name" regardless of the name tried, for a `FROM ./relative/path.gguf` Modelfile - the real cause was the path being resolved against the `ollama serve` daemon's own working directory, not the calling shell's; an absolute path fixed it immediately. Confirms the actual, narrower blocker from Day 27 was specifically Ollama's *experimental* direct-safetensors importer needing Apple MLX - a real GGUF, produced this way, imports via the ordinary `Modelfile` path with no MLX involvement on any OS.

**Day 27 detour - Ollama's grammar-constrained decoding, which helps the 7B teacher, actively corrupted the GGUF-served fine-tuned student's output on one field.** The first live eval column for the new backend showed field accuracy *drop* (98.4% -> 91.2%) with two fields cratering to exactly 0%: `params.path` and `params.skill` - not degraded, always wrong the *same* way (`params: {}`), which doesn't look like quantization noise (there wasn't any) or genuine model damage from the conversion. Confirmed by direct comparison at the same prompt/temperature: unconstrained decoding (`chat`, no `format`) produced the correct `params.path`; `chat_structured`'s grammar-constrained decoding on the identical request produced `{}`. Root cause: `ParsedAction.params: dict[str, Any]` (`src/engine/actions.py`) is untyped, so its JSON-schema-to-grammar compilation has no per-key structure to guide generation, and the fine-tuned 0.5B - never trained under this specific constraint, unlike the teacher's general instruction-following which pushes through it anyway - takes the grammar's easiest valid completion, an empty object, instead of synthesizing the right nested keys. Fixed with a new generic `chat_structured_best_effort` (`llm/providers.py`): unconstrained `chat()` + best-effort JSON extraction + validate + retry (greedy then sampled), the same no-grammar shape `local_parser.parse_intent_local` already uses for the transformers-served student - re-running the eval afterward gave 98.3%, matching the transformers-served number almost exactly and confirming the GGUF conversion itself was lossless all along.

**Day 27 detour - a live, unrelated bug found while chasing a confusing latency measurement: `httpx` resolving `"localhost"` added a flat ~2.2s to *every* Ollama call on this Windows machine, independent of model, prompt, or GPU load.** First noticed as wildly inconsistent latency readings for the exact same code path (sub-second in one micro-benchmark, ~2.7-3.4s moments later for what should have been the identical request) - ruled out GPU contention directly (confirmed via `ollama ps` showing 100% GPU residency for both models, and the gap persisting unchanged with the GPU completely idle, 13% util). Isolated by comparing Ollama's own self-reported `total_duration` against this project's wall-clock measurement for the identical request: a consistent ~2.2-2.3s gap neither the model nor the GPU could account for. Confirmed root cause by pointing the same request at `127.0.0.1` instead of `localhost`: the gap vanished (2.8s -> 0.65-0.96s) - the classic Windows IPv6-loopback-resolves-first-with-slow-fallback issue. This had been silently taxing *every* LLM call this whole project makes (narrator, player_agent, intent_parser, judge, the training/eval pipeline) since Day 12, not something specific to this detour's new backend - just the first time a clean enough isolated benchmark surfaced it. Fixed by changing `OLLAMA_BASE_URL`'s default in `config.py` to the literal `http://127.0.0.1:11434`.

**Day 27 detour - final honest end-to-end numbers, after both fixes above, and which fix actually mattered.** `teacher` ~1000ms median parse latency (down from the original Day 27 table's 2938ms; measured 726-1018ms across reruns - real run-to-run noise, still a ~3-4x drop either way), `finetuned` (transformers/peft) ~2700-2730ms (unaffected by the `localhost` fix - it never goes through Ollama), `finetuned_ollama` (GGUF, same llama.cpp engine as the teacher) ~460-510ms, field accuracy 98.3% (within a point of both the transformers-served fine-tune's 98.4% and the teacher's 99.2%). Judge scores noisy across both runs ([7,7]/[7,6]/[6,7] scattered across backends) - within the already-documented LLM-sampling variance, not treated as a quality signal on a 2-sample size. Exact figures in `data/training/eval_results/intent_parser_end_to_end.md` are whichever run was most recently committed.

Reflecting on these numbers honestly (prompted by the user pushing back that a ~200-300ms-per-parse-call win "was not that big"): the `finetuned_ollama` backend swap itself - roughly half the now-fast teacher's latency - is a real ~2x win, but not one a player would feel; it's one call out of several chained per turn, and the absolute savings is a few hundred milliseconds. **The `localhost` fix was the actual headline result, not a supporting detail** - it speeds up *every* LLM call the app makes (narrator, player_agent, judge, intent_parser regardless of backend), not just this one new backend. Verified directly rather than just inferring it from the isolated parse-latency numbers: a full `run_autoplay` of the goblin one-shot (scene images off, teacher backend - i.e. the app's actual default configuration) now averages **~7.1s/round**, down from Day 21's documented "15-20+ seconds/round" for the same kind of multi-companion round (that estimate also included scene-image generation, so the LLM-call-only improvement is likely larger still). The lesson worth keeping: a benchmark chasing one specific optimization can surface a much bigger, unrelated one, and the unrelated one is what's worth leading with when writing up the result - not the thing the benchmark was originally built to measure.

**Live playtesting (post-Day 28) - the user played the actual app themselves and found a real, pre-existing crash: selecting Monk in the character creator.** Manifested in the browser as a generic "Failed to fetch / is the backend running?" - actually a masked 500 (the exact CORS-hides-server-errors gap already documented for Day 17: an unhandled exception's response skips `CORSMiddleware`, so the browser reports a network failure instead of the real error). Root cause: Monk's SRD `proficiency_choices` has a 2nd entry ("choose one type of artisan's tools or one musical instrument") shaped as a nested choice-within-a-choice rather than the flat `option_type: "reference"` list every other class's entries use - confirmed by scanning every vendored class, Monk is the only one shaped this way. Both places parsing `proficiency_choices` assumed the flat shape unconditionally and raised a raw `KeyError: 'item'` - the read-only `GET /characters/classes/{id}` endpoint *and*, more seriously, `character_creation._validate_skill_choices` itself, meaning actually creating a Monk character was broken too. Fixed with one shared helper (`class_skill_choice_pool`) instead of two copies of the logic that had already drifted, skipping any entry whose options aren't flat references - tool/instrument proficiencies aren't modeled by this project at all, so a Monk now correctly only needs their 2 real skill choices.

**Live playtesting - "shouldn't there be restrictions for weapons and armor depending on class?" There weren't, and there should have been.** The character creator's optional-extra-gear step let any class choose any weapon or armor in the SRD with zero connection to proficiency - a Wizard could equip Plate Armor and a Greataxe with no error. New `class_equipment_options(cls, srd)` (in `rules.py`, not `character_creation.py` - both creation-time validation *and* turn_engine's live proficiency checks below need it, same reasoning as `skill_ability`'s Day 22 move) derives the real SRD-proficient weapon/armor set from a class's `proficiencies` list, which mixes three shapes: broad categories ("simple-weapons", "all-armor", "shields"), and for six classes (Wizard, Sorcerer, Bard, Rogue, Monk, Druid) specific named weapons instead of a category - Wizards get exactly 5 named weapons, not "simple weapons" broadly. One naming is irregular: the proficiency is `"hand-crossbows"` but the equipment list's own index is `"crossbow-hand"` (word order swapped). Enumerated directly from all 12 vendored classes' real proficiency lists rather than guessing a general pluralization rule. Wired in with the same discipline `class_skill_choice_pool` already has: `create_character` rejects an out-of-proficiency `chosen_equipment` item server-side, and `ClassDetail.equipment_options` lets the wizard only ever offer legal choices instead of erroring after submission.

This immediately caught a real, already-shipped data bug: Sister Mira (a pregen companion, Cleric) was authored with chain-mail (Heavy armor), but a base 5e Cleric only has light/medium armor + shield proficiency, not heavy - fixed to scale-mail (same AC formula as breastplate, keeps the "armored cleric" read, actually legal). A creation-time restriction alone doesn't stop a *hand-authored* mistake like that from reaching play, though, which is what motivated the next step.

**Live playtesting - "let's enhance the engine to restrict such cases" - i.e. don't just gate equipment *choices*, make the engine actually enforce proficiency during play too.** Two SRD mechanics, wired into `turn_engine.py`'s existing `resolve_attack`/`resolve_skill_check` call sites (both already accepted `advantage`/`disadvantage` flags, so this was additive, not a new code path): a PC attacking with a weapon their class isn't proficient with drops the proficiency bonus from `attack_bonus` (`_pc_attack_params`); a PC with non-proficient armor or a shield anywhere in their inventory gets disadvantage on attack rolls and on STR/DEX-governed skill checks specifically (Athletics/Acrobatics/Stealth/Sleight of Hand - *not* Perception/Persuasion/etc., per SRD). New `rules.is_class_proficient_with`/`has_non_proficient_armor` do the checking; monsters and any character with no `class_index` are treated as proficient with anything, since they never reach this path (monsters attack via their own stat-block actions, not equipped-weapon lookup). This removes a "documented, deliberate simplification" that had stood since Day 7 (`turn_engine.py`'s own module docstring used to say "Proficiency with equipped weapons is assumed for PCs... attack_bonus always includes proficiency_bonus" - now it doesn't need to say that, because it's no longer true).

Worth being honest about the actual impact: since equipment is now creation-gated to proficient choices, and there's no loot/mid-game-equip mechanic in this engine, a PC can only ever end up with non-proficient gear through hand-authored data (a companion YAML) - exactly the Sister Mira scenario above, now caught by construction with a real mechanical consequence (worse rolls) instead of silently doing nothing. Hand-computed fixture tests cover both mechanics directly (`test_turn_engine.py`'s `test_pc_attack_params_drops_proficiency_bonus_when_not_proficient`/`test_attack_gets_disadvantage_from_non_proficient_armor`, `test_turn_engine_new_verbs.py`'s DEX-check-gets-disadvantage/WIS-check-doesn't pair) plus focused unit tests on the two new `rules.py` functions - live-verified with a full `--autoplay` run afterward to confirm ordinary proficient-gear play is completely unaffected.

**Live playtesting - a real, structural gap, found by the user simply starting a campaign: a live session dropped straight into "Kobold Ambush - Round 1" combat with zero DM narration, no hook, nothing about what/where/how.** `api/ws/session.py`'s own `_build_real_session_game_state` docstring had already confessed this: "Still only ever the *first* combat encounter... a live session doesn't yet advance past one encounter's victory into the rest of the scene chain... not covered by Day 22's autoplay-only verify gate" - Day 22 built `campaign_runner.advance_to_next_encounter` and wired it into `cli.play.run_autoplay`, but never into the live WebSocket path, so every real session silently skipped every narrative_beat/skill_challenge scene (a campaign's "hook" before the fight) and, just as importantly, never continued past one encounter's victory into the rest of a multi-combat campaign (`kobold_warren_full` has two).

Ported the same chaining live: `_build_real_session_setup` (replacing `_build_real_session_game_state`) now walks from `campaign.first_scene()` via `advance_to_next_encounter` before ever building a GameState, collecting the narration for whatever it passes through; `Session` gained `campaign`/`party`/`srd`/`current_scene_id` fields (`party` reuses the same Character objects across encounters - HP/conditions genuinely carry over, matching `run_autoplay`) and a new `_advance_campaign_after_victory`, called right after `_autoplay_non_human_turns` in both the connect flow and `_handle_client_message`: on victory, it walks the chain from `campaign.next_scene(...)`, narrates what it finds, and rebuilds `session.game_state` for the next combat encounter if there is one - a `while` loop, not a single step, since companions alone can occasionally finish a trivial fight before the human's own turn ever comes up.

The one real design decision: pre-combat scene text needed a *new* message type (`scene_narration`), not the existing `narration` type - an existing test (`test_non_human_turns_auto_resolve_before_awaiting_input`) asserts an *exact* count of per-turn `narration` messages, and folding scene-setting text into the same type would have silently inflated that count. Turned out to be the right call anyway: the frontend renders `scene_narration` with distinct italic/bordered styling (`.narration-scene`), so it reads like a DM chapter beat rather than an action's outcome. Live-verified end to end, not just via tests: created a character, started `kobold_warren_full` in an actual browser session, watched the hook + investigation-check narration arrive *before* the kobold fight, played the fight to victory (one companion knocked unconscious and stabilizing mid-fight), and watched the session narrate the rising-action beat + stealth check and land directly in "Bandit Hideout - Round 1" with the same companion still shown unconscious at 0 HP - state genuinely carried across the encounter boundary, not reset.

**Live playtesting - "How could this happen if map pictures player and kobold on the distance one from another?" Because this engine had never validated attack range at all, since Day 7.** The combat grid rendered real distances, but `_resolve_attack`/`_resolve_cast_spell` only checked hit/miss math - a melee weapon could land from across the whole map. New `rules.weapon_range_feet`/`monster_action_range_feet`/`spell_range_feet` give every attack source a real range (weapons via the SRD's structured `range.normal`/`range.long`, plus a manual +5ft for the two SRD-wide reach weapons - glaive/whip - since the data's own `range.normal` doesn't reflect the `reach` property at all; monster stat-block actions only have a free-text `desc`, parsed with a regex keyed on the reliable "Melee"/"Ranged" prefix, falling back to 5ft melee for anything unparseable; spells are a plain string with no long-range tier, "Touch" mapped to 5ft). `turn_engine.py` now rejects an attack beyond max range outright, and - matching the SRD rule that only ranged weapons have this tier - imposes disadvantage rather than rejection for a ranged attack beyond normal-but-within-long range.

Wiring this in immediately broke monster turns, which is the more interesting part: `monster_ai.py` had never moved a monster in its life, always attacking the nearest living PC regardless of actual distance - once range became enforced, every monster more than 5ft (or its weapon's range) from its target would fail its attack forever, every single turn. Rewrote it to check range first and, if out of range, generate a greedy step-by-step approach path (`_best_step`/`_approach_path` - explicitly not real pathfinding, matching `movement.py`'s own documented stance, since this project's battle maps are small and mostly open) and return a `move` action instead, falling back to attacking anyway only when there's no `battle_map` to path against (turn_engine's own range check is then the honest final word). This is why the fix needed monster AI to move at all: the engine's one-action-per-turn model already supports "move this turn, attack next turn" for free (every verb unconditionally advances the turn), so no new combat-mechanics complexity was needed - just teaching the AI to actually use movement.

That combination - strict range rejection plus a monster AI that could newly fail to close distance (blocked by walls, or simply not enough speed in one turn) - triggered a **real infinite loop** the first time the full offline suite ran: `uv run pytest` hung indefinitely and had to be force-killed. Root cause was two compounding gaps, not one: the greedy approach path can legitimately fall short of range in a single turn, and the `consecutive_invalid >= 3` circuit-breaker fallback (added Day 15 for companions going unconscious mid-turn) lived inside the `actor.is_pc` branch in both `_autoplay_non_human_turns` (session.py) and `run_autoplay` (cli/play.py) - monsters had no circuit breaker at all, so a monster that could never quite reach range would retry the same rejected attack forever with nothing to stop it. Fixed by reordering both functions so the invalid-count check runs first, unconditionally, before the PC/non-PC branch - closing a latent gap in a mechanism that had existed since Day 15 but had never been exercised by a monster before.

Live-verified afterward with a full fresh playthrough (`goblin_ambush_oneshot`): three goblins narrated as actually closing the distance ("slinks forward," "shuffled cautiously... to a more sheltered position") instead of attacking from across the map, an out-of-range attack attempt surfaced a clear rejection ("goblin_1 is 30ft away - out of range for Battleaxe (max 5ft)") instead of silently landing, and the following round showed a real melee exchange once both sides had closed to 5ft (Rian's longsword killing a goblin, a goblin's scimitar glancing off Grom's shield, Grom's battleaxe landing back). 236 offline tests pass, plus a real 121s LLM-backed `tests/llm/test_autoplay.py` run with no hangs or invariant violations - the circuit-breaker fix specifically needed that live-LLM coverage, since the infinite loop only reproduced under the interaction between a real (imperfect) approach path and range rejection, not any offline fixture.

**Live playtesting - "we have 3 kobolds, but only 2 appear on the map. also there is no identification of kobold's id on the map." Two separate bugs, both surfaced by the same encounter, neither masking the other.** (1) `monster_to_character` sets `Character.name` to the raw SRD stat-block name ("Kobold"), identical for every instance of the same monster type - both the sidebar (`CharacterSheet.tsx`, renders `character.name` directly) and the combat grid's token label (`initials(character.name)`) had no way to tell kobold_1/2/3 apart even though their `character_id`s were unique the whole time. Fixed with a `_display_name` helper that appends the character_id's trailing "_<n>" to the SRD name ("Kobold 2") - every authored encounter already names monsters this way, so no data changes were needed, just reading the id that was already there. (2) The genuinely more interesting bug: two kobolds were standing on the exact same grid square, stacked exactly on top of each other - which is why only 2 tokens were visible for 3 actual characters. Root cause was a direct consequence of the range-enforcement/monster-movement work from earlier the same live session: `monster_ai._best_step` only ever avoided walls, never other characters, so two kobolds independently pathing toward the same nearest PC from different starting squares could pick the identical "best" intermediate square. Fixed by having `choose_monster_action` snapshot every other living character's position once at the start of the acting monster's own turn, and excluding those squares from `_best_step`'s candidates the same way walls already are - a one-time snapshot is correct here since a monster only ever moves once per turn, so nothing about its own path needs to react to a square becoming occupied mid-path. Live-verified: created a fresh character, started `kobold_warren_full`, and confirmed directly via the combat grid's own SVG DOM (not just a screenshot - a 40px grid is too small to trust by eye, same lesson as Day 20/21's screenshot-ambiguity notes) that all three kobolds rendered as `K1`/`K2`/`K3` at three distinct positions. 238 tests passing.

**Live playtesting - immediately after the two kobold-overlap fixes above, the user spotted a third instance of the same underlying class of bug: "in this run, I don't see token for player Rangetest." A different root cause from the kobold case, found by ruling things out in order rather than guessing.** First reproduced `_build_real_session_setup`'s exact flow directly against the real dev-DB row for that session (same character/companion ids, same campaign) in an isolated script - `build_encounter_state` placed the human and companion at their correct, distinct spawn points every time, ruling out a spawn-assignment bug like the kobolds had. The narration line told the real story: "Fenwick shifts silently to a crouch beside the nearest crate" is what a companion's own declared `move` action narrates as - and `turn_engine._resolve_move` (shared by both `move` and `dash`) had only ever validated speed budget and terrain, never whether the destination square was already occupied by anyone. A companion's free-text-declared move (player_agent -> intent_parser, no spatial awareness of allies any more than the already-documented "closest goblin" gap has for enemies) landed exactly on the human player's own square, stacking their tokens identically to the kobold case - but via a completely different mechanism (a move nobody validated, not two AI paths converging). Fixed by rejecting a move/dash whose final destination is already occupied by another living character - deliberately only the destination, not every square passed through, since this engine has no opportunity-attack mechanic or ally/enemy pass-through distinction for a deeper check to actually serve. Verified with a hand-computed fixture reproducing the exact scenario (an adjacent, already-occupied square in the Day-7 demo encounter) confirming the new rejection fires with a clear message. 239 tests passing.

**Rules-completeness initiative (Phase 9), started 2026-09-13 - the user asked for the reverse of live playtesting this time: audit the SRD 5e ruleset directly against what's implemented, find everything missing, and plan how to close it.** A code-verified sweep (not general D&D knowledge) turned up a long list: 14 of 15 SRD conditions were schema-only (state.ConditionName enumerated them since Day 3, but nothing outside "unconscious" ever checked one); saving throw proficiencies were sitting unused in the vendored SRD data; cast_spell only ever resolved attack-roll spells; no resting, no action economy beyond one action per turn, no leveling. Presented as a phased plan (9A-9J, in `radiant-waddling-hellman.md`) and the user picked the most ambitious of three offered scopes - fill every gap *and* build a real action economy *and* build character leveling, explicitly the largest initiative in the project's life. Phase 9A (this entry) is the foundation everything else depends on.

**Phase 9A - saving throw proficiencies + real condition mechanics.** `Character.saving_throw_proficiencies` now derives from `srd.classes[class_index]["saving_throws"]` at creation (same pattern as `skill_proficiencies`) and a new `rules.saving_throw_bonus` applies the proficiency bonus only when it should - unused by any call site yet (the only saving throw ever rolled today, death saves, is correctly flat/unmodified per SRD), proven correct in isolation for Phase 9D's save-based spells to consume. Exhaustion moved from a boolean `ConditionName` tag (confirmed via grep to have zero real usages) to `Character.exhaustion_level: int` (0-6), since it's a leveled effect with per-level rules, not an on/off status.

The real substance: five new `rules.py` functions give the other 14 conditions actual teeth - `condition_attack_advantage`/`condition_attack_disadvantage` (blinded/paralyzed/petrified/restrained/stunned/unconscious targets are easier to hit; a prone target only from melee range, harder from range; poisoned/frightened/restrained/prone/blinded attackers roll worse; an invisible attacker rolls better, an invisible target is harder to hit), `condition_check_disadvantage` (poisoned/frightened/exhaustion-1+ on any ability check), `effective_speed` (grappled or exhaustion-5+ zeroes movement, exhaustion-2+ halves it), and `set_exhaustion_level` (clamped 0-6, level 6 kills per SRD). Wired into the exact same OR-chains `_resolve_attack`/`_resolve_cast_spell`/`_resolve_skill_check`/`_resolve_move` already used for help/dodging/non-proficient-armor/range - no new dispatch shape needed, just more terms in existing boolean expressions. A charmed character being unable to attack their charmer (`Condition.source` holds the charmer's id) rode along in the same `_validate_attack_target` guard that already blocks same-side attacks.

Deliberately left out of this pass, both documented inline: exhaustion level 4's "hit point maximum is halved" (would need a second, original max_hp preserved somewhere to undo cleanly when exhaustion later drops - more bookkeeping than worth taking on silently right now), and the "can this actor act at all" side of paralyzed/petrified/stunned/incapacitated (forcing an automatic end_turn) - that's Phase 9B, a different kind of check than an attack-roll modifier.

Nothing in the current game actually *applies* any of these conditions yet - no verb, spell, or monster action grants restrained/prone/charmed/etc. to anything (that's what Phases 9D/9E build). So this phase is proven entirely via hand-computed fixtures - one per condition in `test_rules.py` for the pure functions, plus four end-to-end `test_turn_engine_conditions.py` tests that apply a condition directly (`conditions.apply_condition`, not scripting a real cause - same "poke state" pattern this project already uses for downed-actor/dodging tests) and confirm `resolve_action` rolls with the SRD-correct (dis)advantage or rejects the action. 254 tests passing (up from 239), lint/mypy clean.

**Phase 9B complete and pushed - the first of the Phase 9 sub-phases resolved via a parallel worktree agent (issue #1), not this session directly.** Extends resolve_action's guard so an actor carrying paralyzed/petrified/stunned/incapacitated/unconscious has their declared action silently rewritten to end_turn instead of resolving normally - not a raised error, matching the "invalid" verb's philosophy that a condition applied to the actor isn't their mistake, so it shouldn't cost extra or read as a system failure. Composes for free with the pre-existing hp<=0/unconscious death-save guard (excluded via a verb != "death_save" check), so a PC at 0 HP is unaffected - it either gets forced through to death_save or rejected before this new check is ever reached. 258 tests passing (up from 254).

**Phase 9E complete and pushed - resolved by a parallel worktree agent (issue #4).** Adds `grapple`/`shove` as new `ActionVerb`s, resolved as an SRD contested check via a shared `_grapple_shove_contest` helper: the actor's Athletics check against the higher of the target's own Athletics or Acrobatics (always 3 d20s consumed in a fixed order - actor Athletics, target Athletics, target Acrobatics - regardless of outcome, documented precisely so fixture tests know what to feed a `_FixedRandom`). A successful grapple applies `grappled` (Phase 9A's `effective_speed` already zeroes movement for it, no further wiring needed); a successful shove applies `prone` only, no push-5ft, matching the issue's explicit scope.

Worth recording: the agent caught a real self-contradiction in the issue's own draft spec - it said the attacker wins on `total >= target's total` but also said "ties go to the defender," which can't both be true. Checked the actual SRD rule (a tie in a contested check leaves the situation unchanged) and implemented a strict `>` instead of the draft's literal `>=`, with a dedicated regression test locking in the corrected behavior. A good reminder that an issue spec written ahead of implementation can still have the same kind of internal inconsistency hand-authored content does elsewhere in this project (Sister Mira's armor, Day 25's placeholder actor id) - worth verifying against the source rule, not just implementing literally. 268 tests passing (up from 258).

**Phase 9G complete and pushed - resolved by a parallel worktree agent (issue #6).** No short/long rest existed anywhere in the engine, so a multi-encounter campaign (`kobold_warren_full` has two fights) never let HP, spell slots, or exhaustion recover between them. `Character` gains `hit_die_sides` (from the SRD class's `hit_die` at creation) and `hit_dice_remaining` (starts at 1 - level-1-only project, Phase 9J's leveling will need to grow this). New `src/engine/resting.py` holds `apply_short_rest` (spends all remaining hit dice, each healing `1d(hit_die_sides) + CON mod`, floored at 0, clamped at max_hp - SRD lets a player choose how many dice to spend, simplified to "all of them") and `apply_long_rest` (full HP, spell slots reset via the existing `LEVEL_1_SPELL_SLOTS` table rather than a new snapshot field - it never changes for a level-1-only project, so caching it would just be a second place to drift - hit dice back to 1, exhaustion down one level via `rules.set_exhaustion_level`).

Wired into the campaign scene chain as two new `Scene` types (`short_rest`/`long_rest`, matching how `narrative_beat`/`skill_challenge` are already distinguished), resolved by `campaign_runner.advance_to_next_encounter` the same deterministic, no-LLM way skill challenges already are. A real `short_rest` scene (`warren_rest`) was inserted into `data/campaigns/kobold_warren_full.yaml` between its two combat encounters, so the fix is demonstrably real in existing content, not just covered by synthetic fixtures - exactly the gap the Phase 9 audit flagged. 278 tests passing (up from 268).

**Phase 9C complete and pushed - resolved by a parallel worktree agent (issue #2), and it found a real latent bug along the way.** Closes the exact, already-documented Day-22 gap: a monster's attack against an already-downed PC previously just rolled to hit and dealt damage clamped at 0, no special effect - confirmed live to stretch one combat to 353 turns/119 rounds. `rules.resolve_attack` gained an optional `force_critical` flag (dice-math stays in one place - it only affects damage-dice doubling, never whether the attack hits at all, so a natural 1 still always misses); `_resolve_attack` checks the target's `unconscious` condition *before* rolling (so the hit that first knocks someone out doesn't get the helpless treatment on that same hit) and, on a landed hit against an already-unconscious target, applies 2 automatic death-save failures through a `_check_death_save_failure_threshold` helper factored out of `_resolve_death_save` - both paths hitting 3 failures now share one threshold check instead of two copies that could drift.

**The latent bug**: `_apply_damage_and_handle_downing` was unconditionally resetting `death_save_successes`/`failures`/`is_stable` on *every* hit against a target already at 0 HP - which would have silently wiped the very 2 automatic failures this new feature applies, immediately after applying them. Not a bug the codebase had ever been able to notice before, since nothing previously did anything special on a repeat hit against an unconscious character - implementing the SRD rule correctly is what surfaced it. Fixed by only running the "just went unconscious" reset the first time a PC drops to 0 HP.

A new `"stabilize"` verb lets an ally spend their action on a DC 10 Medicine check (WIS, the actor's own stats/proficiency) to set a dying target's `is_stable` directly, without the target rolling anything - the same field 3 self-rolled death-save successes already sets.

Live-verified, not just tested: a full `--autoplay --campaign kobold_warren_full` run (real Ollama teacher model) that previously would have run the Day-22-documented 353 turns/119 rounds now ended in **defeat after round 8/41 turns** - narration reflects the mechanic directly ("Companion Grom's life fades away as he fails his death save, succumbing to the deadly blow while unconscious"). Judge score unchanged from prior runs (overall 7), confirming this didn't degrade play quality, just fixed pacing. 288 tests passing (up from 278). One unrelated, pre-existing issue surfaced in the same live run and flagged for separate follow-up, not fixed here: a companion's free-text turn referenced "silvered longbow" and failed with "not a valid weapon" - likely a naming mismatch in a companion spec YAML.

**Phase 9J complete and pushed - resolved by a parallel worktree agent (issue #9), and the first Phase 9 merge that needed real manual reconciliation, not just a clean auto-merge.** `Character` gains `level: int = 1`. Leveling is milestone-based (no XP/CR table - the SRD doesn't vendor one any more than it vendors class-level tables). New hardcoded tables in `character_creation.py` (same "basic SRD 5.1 fact, not in vendored JSON" precedent as `LEVEL_1_SPELL_SLOTS`): proficiency bonus by level, HP-per-level (`hit_die // 2 + 1 + con_mod`, matching level-1's own average-not-rolled formula), a full spell-slot-by-character-level table through level 5 (including Warlock's genuinely different Pact Magic progression - fewer slots, higher spell level, not the full-caster shape), Extra Attack eligibility (level 5+ Fighter/Barbarian/Paladin/Ranger), and ability score improvement at level 4 (validated as a legal +2/one-ability or +1/two-ability allocation). `level_up(character, srd, ability_score_increase=None)` applies one level's worth of these at a time - calling it repeatedly takes a level-1 character to level 5.

**The interesting part was integration, not implementation.** This agent's worktree branched before Phase 9C merged, so both independently rewrote the entire body of `_resolve_attack` - 9C wrapped the single attack roll with `force_critical`/unconscious-death-save handling, 9J wrapped it in an Extra-Attack loop. Git's auto-merge correctly flagged this as a real conflict (unlike every other Phase 9 merge so far, which auto-merged cleanly) rather than silently picking one side. Resolved by hand: moved `already_unconscious`'s computation *inside* the loop, recomputed fresh every iteration rather than once before it - an Extra Attack's first roll can itself knock the target unconscious, so the second roll of that same action needs to see the *updated* state and correctly get the auto-crit/automatic-failure treatment the first roll didn't get. The 2-failures application moved inside the loop too, gated per-iteration on that roll's own hit.

Worth being honest about scope: this specific interaction (two attack rolls in one action against a target that goes unconscious partway through) is currently unreachable in practice - Extra Attack is PC-only, and a PC can't target another PC (`_validate_attack_target`'s same-side guard), while monsters go straight to `is_dead` at 0 HP, never unconscious. It becomes real the moment Phase 9F's Multiattack can roll multiple attacks against a PC in one monster action - flagged directly in the merge commit as something that phase's review should add a dedicated regression test for, not just trust this reasoning was right. 295 tests passing (up from 288).

**Phase 9F complete and pushed - resolved by a parallel worktree agent (issue #5), and the trickiest Phase 9 integration yet, including a silent bug the merge tool would have shipped without any warning.** Three independent additions: monster **Multiattack** (a monster whose chosen action is "Multiattack" now actually resolves - `rules.multiattack_sub_actions` parses the free-text `desc` for named sub-actions/counts, e.g. giant-badger's "makes two attacks: one with its bite and one with its claws," and each is rolled as its own full attack via a newly-factored `_resolve_single_attack` helper); **ranged-while-engaged disadvantage** (a ranged attack now rolls with disadvantage while a hostile creature is within 5ft of the attacker, per SRD's "Ranged Attacks in Close Combat" - never enforced before); and **hazard terrain** (finally given a real effect - a small fixed piercing-damage roll on entry, its exact amount borrowed directly from the SRD's own Caltrops item, since the one hazard square this project has authored, `wolf_den.yaml`'s "littered with bones," reads as exactly that kind of debris).

The genuinely hard part was integration, not any single piece of implementation. This branch predated both Phase 9C and 9J's merges, and all three had independently rewritten `_resolve_attack`'s core body - 9F specifically factored the whole per-roll body out into a standalone `_resolve_single_attack`, exactly the refactor 9J's own commit message had already flagged as "would be nice to share once it exists." Git correctly flagged the textual overlap as a real conflict and it was resolved by combining all three: `_resolve_single_attack` now carries 9A's condition effects, 9F's ranged-engaged disadvantage, AND 9C's force_critical/unconscious-death-save handling together, since every attack roll needs all of it regardless of which multi-roll mechanism (Extra Attack or Multiattack) invokes it.

**The genuinely dangerous part was what git did NOT flag.** 9F's version of the top-level `_resolve_attack` dispatcher was written before Extra Attack existed, so it had no Extra-Attack branch at all - and because that region of the diff didn't textually overlap with 9J's changes, the automatic merge picked 9F's whole dispatcher wholesale, silently, with no conflict marker and no warning. `is_eligible_for_extra_attack` became an imported-but-never-called dead import - confirmed by grepping for it after the "clean" merge and finding only the import line. A clean auto-merge is not the same as a *correct* one when two branches touch the same function for different reasons; this is exactly why every Phase 9 merge in this initiative gets manually reviewed and test-verified rather than trusted on git's word alone. Fixed by restoring the Extra Attack loop alongside the Multiattack branch in one unified dispatcher.

Also closed the loop on a note left in the Phase 9J merge commit: whether "two attack rolls in one action against a target that goes unconscious partway through" actually works was flagged as untestable at the time (Extra Attack is PC-only, and PCs can't target PCs) and deferred to whenever Multiattack landed. It landed here - a new hand-computed fixture confirms a monster's Multiattack correctly gives its second sub-attack both the auto-crit rule AND (a real SRD stacking effect the fixture caught while being written) the advantage an unconscious target already grants attackers per Phase 9A - the two apply simultaneously, not as alternatives, which cost an extra d20 in the RNG sequence the first draft of the test hadn't accounted for. 306 tests passing (up from 295).

**Phase 9D complete and pushed - resolved by a parallel worktree agent (issue #3), and the most extensive Phase 9 integration of them all.** `cast_spell` (Day 14) only ever resolved single-target attack-roll spells; this closes the gap for the other two SRD shapes plus multi-target and concentration. **Save-based** spells (`dc` present: Fireball, Hold Person) now roll each target's saving throw - via Phase 9A's previously-unused `rules.saving_throw_bonus` for PCs, or a new `rules.monster_saving_throw_bonus` for monsters (their stat block's own `saving_throws` field is always `null` in the vendored data; the real per-ability bonus lives in `proficiencies`, already precomputed as a total, not a bare modifier - confirmed against Adult Black Dragon's DEX save matching mod+proficiency exactly) - and apply full/half/no damage per `dc_success`. **No-roll** heal spells (`heal_at_slot_level`, e.g. Cure Wounds) resolve as a straight heal clamped at max_hp - genuinely machine-readable data this time (the "MOD" token means the caster's spellcasting ability modifier), unlike Day 14's healing potion which needed a hardcoded constant. **Multi-target**: `ParsedAction.targets` (dead since Day 3) finally gets read - a spell resolves against every id in it independently, falling back to `[target]` so existing single-target behavior is unchanged. **Concentration**: `Character.concentrating_on` tracks a sustained spell; taking damage while concentrating forces a CON save (`DC = max(10, damage // 2)`) that clears it on a failure - this engine still has no ongoing-effect mechanic for any concentration spell to actually remove, a documented boundary, same spirit as the "apply the actual condition on a failed no-damage save" gap for spells like Hold Person.

**Integration was the hard part, again** - this branch predated Phases 9C, 9E, 9F, and 9J, all four of which touched the same region of `turn_engine.py` for unrelated reasons. Three conflicts: `state.py` (purely additive - `Character.level` and `Character.concentrating_on`, kept both), an alphabetical import list (kept both), and the real one - `_apply_damage_and_handle_downing` gained `rng`/`srd` parameters to hook the new concentration check in, and every call site added by 9C/9F since 9D's branch was created needed updating. **Git's auto-merge silently left `_resolve_single_attack`'s call at the old 4-argument form** - a stray, non-conflicting line that would have been a `TypeError` at runtime the moment any attack landed, not caught by git and not something `ruff`/`mypy` alone would have flagged either (a missing positional argument mypy *should* catch, actually - worth remembering this is exactly the kind of thing type-checking exists to catch, and it did, once run). 9D's full replacement of the Day-14-only spell functions also landed in the same file region as 9E's grapple/shove, so reconciling meant keeping grapple/shove intact and splicing 9D's whole new attack/save/heal dispatch chain in around it, not just picking one side.

Extended `_cast_attack_spell_at_target` (9D's per-target spell-attack resolver, written before 9C existed) with the same unconscious-hit auto-crit/automatic-failures handling `_resolve_single_attack` already has - the SRD rule is "any attack roll," not "any *weapon* attack," so a spell attack against a downed ally should behave identically to a sword. 314 tests passing (up from 306).

**Phase 9H complete and pushed - a narrow slice of real action economy, done solo rather than via a parallel worktree agent since it's the one Phase 9 sub-phase that specifically needed turn_engine's shared dispatch loop to itself (every other sub-phase this initiative touched it too - see the 9F/9D merge writeups above for what happens when that's not true).**

**Bonus-action spells**: a spell with the SRD's `casting_time` field set to "1 bonus action" (e.g. Healing Word) no longer ends the actor's turn - `resolve_action`'s dispatch tracks a new `ends_turn` flag per verb (defaults True; `_resolve_cast_spell` returns it explicitly, False only for a successful bonus-action cast), so the actor still gets their real main action afterward. `Character.bonus_action_used` rejects a second bonus-action cast the same turn with a clear error.

**Opportunity attacks**: the one reaction this engine models. When a character moves out of a hostile creature's 5ft reach without having disengaged this turn, that hostile gets one free attack - resolved against the mover's *pre-move* position (still within reach at that instant), not their destination, so the attack-range check that would otherwise immediately reject it (the whole point of the attack existing is that they're about to no longer be in range) doesn't fire on its own trigger condition. Capped at one reaction per reactor per round (`Character.reaction_used_this_round`, reset when the round number advances - not once per mover, matching SRD's real reaction economy rather than letting one creature get free attacks against every ally that disengages past it in the same round). `"disengage"` finally does something mechanically (`Character.disengaged_this_turn`) - closing a gap flagged since Day 13's own module docstring.

**A real design bug was caught mid-implementation, not left for review to find.** The first draft reset `bonus_action_used`/`disengaged_this_turn` at the top of every `resolve_action` call, mirroring the existing `is_dodging` reset - which is exactly wrong the moment a verb can leave the turn open (precisely what this phase introduces): the actor's own follow-up call for their main action, still the *same* real turn, would hit that same reset and wrongly clear both flags before they could ever matter - silently allowing unlimited bonus-action spells in one turn, and wiping disengage protection before the character's own next move could ever check it. Fixed by moving `bonus_action_used`'s reset into `_advance_turn_skipping_dead` (fires only when a turn genuinely advances *to* a character, not on every dispatch) and `disengaged_this_turn`'s into `_resolve_move` itself, consumed the next time this specific character actually tries to move - however many other actors' turns happen in between, which is the only way to approximate SRD's real same-turn-only protection given this engine's one-verb-per-turn architecture. A dedicated regression test (disengage, then a different actor's whole turn happens, then move on this actor's own later turn) fails immediately under the original design and passes under the fix - caught by writing that exact test, not by code review alone.

Live-verified with a real Ollama-backed `--autoplay` run (`goblin_ambush_oneshot`) on top of the hand-computed fixtures - completed cleanly in 12 rounds/50 turns to a real defeat, no hangs, no crashes, the existing invalid-action circuit breaker handling an unrelated pre-existing companion weapon-naming issue exactly as it already did before this phase. 321 tests passing (up from 314).

**Fixed the pre-existing companion weapon-naming bug flagged above (and first noted on Phase 9C's live run) - not a YAML data problem, and not an LLM hallucination out of thin air either.** A real `--autoplay --campaign kobold_warren_full` run failed a companion's turn with `"'silvered longbow' is not a valid weapon"`. Checked the actual data first, not general D&D knowledge: `data/companions/silvana_wren.yaml` (Silvana, a Ranger) has no weapon in its own `chosen_equipment` at all - her longbow comes entirely from Ranger's SRD *fixed* `starting_equipment` (`character_creation.py` appends `cls["starting_equipment"]` to inventory unconditionally, before any chosen items), confirmed directly against `5e-SRD-Classes.json`. And the vendored `5e-SRD-Equipment.json` has exactly one bow entry, indexed `"longbow"` - no "silvered" variant exists in the SRD at all. So Silvana's data was correct throughout; the "silvered" adjective was the persona-driven `player_agent` node's own flavor text (matching her YAML persona - "trusts tracks and instincts," a quiet watchful ranger, exactly the kind of companion likely to describe her gear rather than name it flatly), passed verbatim into `item_or_spell` by `intent_parser` (whose prompt only ever says "set item_or_spell to a weapon... name" - it has no instruction, and no schema constraint, requiring an exact SRD index).

The actual bug was one level deeper: `_pc_attack_params` (`src/engine/turn_engine.py`) did an exact-index `srd.equipment.get(weapon_index)` lookup with zero fallback - the same class of exact-string mismatch Day 14 already hit and fixed for `use_item`'s "healing potion" vs. `potion-of-healing`, just never applied to the weapon-lookup path even though attack is the far more common case (every autoplay round has attacks; healing potions are rare). Fixed the same way: a new `_match_weapon_by_name` tries every SRD weapon whose own name (word-split) is fully contained in the given phrase's words - `{"longbow"} <= {"silvered", "longbow"}` matches, an unrelated weapon name still won't accidentally match since real weapon names don't share single-word overlaps with each other. Deliberately global (matches against the whole SRD weapon list, not scoped to the actor's own inventory) to match `_pc_attack_params`'s pre-existing exact-match behavior exactly - an already-passing hand-computed fixture (`test_pc_attack_params_drops_proficiency_bonus_when_not_proficient`) intentionally names a weapon the wizard doesn't own to test the no-proficiency-bonus branch, so adding an inventory-ownership check now would have been unrelated scope creep breaking an existing, deliberately-designed test. Two new hand-computed fixtures added: the exact Silvana/"silvered longbow" repro (verified attack_bonus/damage/range against the real Longbow SRD entry by hand) and a rejection case ("my fireproof toaster" still correctly raises `TurnEngineError`, confirming the fuzzy match doesn't silently accept anything). 323 tests passing (up from 321), lint/mypy clean.

**Phase 9I complete and pushed - the last sub-phase of the rules-completeness initiative, done solo since it depends directly on 9H's action-economy mechanism.** Four representative level-1 class features, each gated on `Character.class_index` so they only ever apply to the class that grants them.

**Fighting Style** (Fighter/Ranger/Paladin, chosen at creation via a new `create_character(..., fighting_style=...)` parameter, validated against a small implemented set - Great Weapon Fighting/Protection/Two-Weapon Fighting are real SRD styles but deliberately rejected rather than silently accepted and doing nothing): Archery (+2 ranged attack rolls) and Dueling (+2 damage, a one-handed melee weapon with no other weapon anywhere in inventory - this engine has no worn-vs-carried distinction, matching the existing `has_non_proficient_armor` precedent) apply in `_pc_attack_params`; Defense (+1 AC while wearing armor - a shield alone doesn't count, per the SRD's literal text) is baked into `Character.ac` directly at creation, since this engine computes AC once rather than deriving it per-attack.

**Second Wind and Rage**: new bonus-action verbs sharing Phase 9H's `ends_turn=False` mechanism and a shared `_use_class_resource` helper for the "out of uses" error. Second Wind heals `1d10 + level`. Rage sets a transient `is_raging` flag (same pattern as `is_dodging`) granting resistance to bludgeoning/piercing/slashing damage and a flat melee-STR damage bonus. This engine doesn't model Rage's real 1-minute duration/maintenance conditions (ends early if you don't attack or take damage) - it persists until a rest clears it, a documented simplification rather than a silent one.

**Sneak Attack** (Rogue): automatic, not its own verb - once per turn, on a hit with a finesse or ranged weapon, when the roll had advantage or an ally is adjacent to the target. Rolled separately from the weapon's own damage dice inside `_resolve_single_attack` (its d6 doesn't share the weapon's die size, so it can't be folded into the existing `AttackParams` shape) and doubled on a crit, matching how `resolve_attack` already doubles the weapon's own dice.

**Two real fixture bugs surfaced while writing tests, not during implementation** - worth remembering as its own lesson: several early Dueling/Rage/Sneak-Attack test assertions expected a specific damage total but got a smaller number every time, tracing back to the SRD goblin's real 7 HP being less than the bonus-inflated damage - `apply_damage` correctly clamps its reported "actual loss" at the target's remaining HP, silently hiding the exact bonus amount a test wanted to verify. Fixed by giving the shared test-fixture goblin generous HP, not by second-guessing the (correct) production code. Separately, a "does Sneak Attack correctly refuse to trigger twice in one turn" test needed calling `_resolve_single_attack` directly rather than through `resolve_action` - the top-of-function reset that clears `sneak_attack_used_this_turn` on every call (safe today, since nothing lets a Rogue's plain attack produce more than one `resolve_action` call per real turn) would otherwise silently undo the test's own setup before the guard could ever be exercised.

343 tests passing (up from 323). Live-verified with a real Ollama-backed `--autoplay` run - clean defeat in 10 rounds/47 turns, no hangs or crashes. **This closes out the entire Phase 9 rules-completeness initiative** (9A through 9J, 10 sub-phases, tracking issue #10) - started as an audit-first reversal of this project's usual live-playtesting pattern, grown into the single largest initiative in the project's history.

**Character-sheet + generated-portrait feature (2026-09-13) - a proactive UX request, not a bug: a dedicated detailed sheet for the player's own PC, built live during character creation and shown during play, plus a pre-generated portrait library (Gemini/Imagen, external) reused as the combat-grid token image.** Planned across 5 phases; Phase 1 (the actual multi-hour batch-generation job) is blocked on `GEMINI_API_KEY` becoming visible to this session (see below) - Phases 2-5 (all code) are complete and live-verified against the not-yet-generated library's graceful fallback path.

**The single most useful finding, from two parallel research passes before any code was written: almost none of this needed new backend wiring.** `state_update`'s `_state_update_message` and the REST character routes already `model_dump()`/`response_model=Character` the *entire* `Character` object - every Phase 9 field (`class_resources`, `spell_slots`, `is_raging`, `fighting_style`, `level`, `concentrating_on`...) was already reaching the browser; the frontend's hand-written `LiveCharacter`/`Character` TS interfaces just never declared most of them, so nothing rendered. Widening those two interfaces (Phase 3) turned out to be the majority of "show current resources" - a typing gap, not a wiring gap. The two genuinely new things were a "known cantrips per class" concept (never existed - `cast_spell` accepts any free-text spell name against the whole SRD list, no restriction to the caster's class) and portrait/appearance fields (`race_index`/`gender`/`hair_color` - SRD races themselves have no gender concept at all, confirmed against the vendored race JSON, so these are purely project-invented, portrait-selection-only fields with zero rules weight, documented as such on every new field).

**Phase 2 (backend)**: `Character` gains `race_index`/`gender`/`hair_color`; `ClassDetail` gains a `cantrips: list[SpellSummary]` field, filtered from `srd.spells.values()` by `level == 0` and class membership (confirmed live first: a spell's `classes` field is `[{index, name, url}]`, not assumed from memory - Wizard has 14 cantrips, Fighter 0). `CreateCharacterRequest` also picked up `fighting_style`, a pre-existing gap since Phase 9I: `create_character` already accepted it, the REST layer never exposed it. New Alembic migration adds `race_index`/`gender`/`hair_color` columns to `CharacterRecord` - deliberately including `race_index` even though the plan's own draft had only named `gender`/`hair_color`, since skipping it would have silently broken `portraitUrl` the instant a character reloaded via `GET /characters/{id}` rather than being read straight from its own create response. A new `/media/portraits` `StaticFiles` mount reuses the exact pattern the existing `/media/scene-images` mount already established - nothing about that mechanism was specific to SD-Turbo. All 5 companion YAML specs needed `gender`/`hair_color` added as new *required* `CompanionSpec` fields (every companion needs a real portrait) - a deliberate, persona-matched choice per companion (e.g. Grom's gruff-mercenary persona directly implies male; Sister Mira's persona uses "she" explicitly), not an automated default.

**Phase 3-5 (frontend)**: a single `web/src/utils/portraits.ts` centralizes the portrait filename convention (`{race}_{class}_{gender}_{hair_color}.png` for PCs/companions, `{monster_index}.png` for monsters) so `CombatGrid`, `PartySetup`, and both new sheet components can't drift against each other. `CombatGrid`'s tokens swap the old `<circle>`+initials for a shared `<clipPath>`-clipped `<image>`, falling back to the original circle+initials via a small per-token component's own `onError`-driven React state (not CSS) - deliberately built to degrade gracefully with zero portraits generated yet, so this code could ship and be fully verified before Phase 1's batch job ever runs. New `CharacterPreviewSheet.tsx` (in the character-creation wizard, live-updating every step) and `CharacterDetailSheet.tsx` (in live play, third column) both derive their "available actions" list purely from data already on the wire plus the new `cantrips` endpoint - no new "legal actions" backend endpoint, a deliberate non-goal, since every input already exists.

**Live-verifying the finished live-play sheet surfaced a real, useful confirmation of an already-known, already-out-of-scope gap - not a new bug.** A freshly-created Fighter's `fighting_style`/`class_resources` were present in the character-creation POST's own response, but `null`/`{}` after a session was built from that same character reloaded out of the DB - exactly the pre-existing "~20 `Character` fields silently dropped by `_character_to_record`/`_record_to_character`" gap this project has documented twice already (Day 17, Phase 9-era). This plan's own two new fields (`gender`/`hair_color`, plus `race_index`) round-tripped correctly - confirming Phase 2's persistence fix is real and is the thing actually being exercised - while deliberately *not* touching the older, larger, already-flagged gap (an explicit non-goal, still a good candidate for its own future pass).

**A live-browser testing-tool gotcha, same shape as Day 20/21's but worth re-noting since it hit buttons this time, not just the SVG grid or a resized viewport**: `computer{action:"left_click"}` on a `<select>`'s sibling `<button>` (e.g. wizard "Next") repeatedly registered zero-effect clicks even at coordinates independently confirmed correct via the button's own `getBoundingClientRect()` - no navigation, no console error, `button.disabled` false the whole time. Switching to a JS-dispatched `new MouseEvent('click', {bubbles:true})` via `javascript_tool` worked immediately and every time afterward, for the rest of the session. Treated as a testing-tool limitation exactly like Day 20/21's - not investigated further, since a real DOM click event (what an actual user click produces) is the ground truth here, and it worked. Worth defaulting straight to the JS-dispatch fallback the next time a `computer` click on a small/precise element silently no-ops, rather than re-diagnosing coordinates first.

`uv run pytest` unaffected throughout (343 passing), `ruff`/`mypy`/`tsc -b` clean; `npm run lint` shows one new warning in `CharacterDetailSheet.tsx` mirroring an already-accepted pattern already present in `CharacterCreator.tsx` (a `setState` inside an effect's early-return branch), not a regression.

**Phase 1 (portrait batch generation) - the plan's Gemini/Imagen assumption turned out to be wrong, confirmed live rather than argued from docs.** `GEMINI_API_KEY` did become visible once the session was genuinely restarted (a `claude.exe` process-tree relaunch via Task Manager, not just resuming - a plain "restart" left stale `claude` processes from two days earlier still running, which is why the first two restart attempts didn't work). But `client.models.generate_images` - the call shape `ai.google.dev`'s own docs show - raised "This method is only supported in Gemini Enterprise Agent Platform mode, not in Gemini Developer API mode" for every Imagen model. Falling back to `generate_content` with `response_modalities=["Image"]` (Gemini's own native image models: `gemini-2.5-flash-image`, `gemini-3.1-flash-image`, `gemini-3-pro-image`) hit a *different* wall: a hard 429 with `limit: 0` on every one of them, not a rate limit - the free tier of this account has zero image-generation quota at all, confirmed by the same key working fine for plain text generation (`gemini-3.6-flash`) in the same session. Enabling billing would fix this in principle but wasn't pursued (see the pivot below).

**Pivoted to a local FLUX.1-schnell pipeline instead** - this project already has the "run a local diffusion model" muscle (SD-Turbo, Day 16+), and FLUX.1-schnell is Apache-2.0 licensed (commercially fine) and dramatically higher quality than SD-Turbo at a similar few-step budget. Getting it running on this specific machine took three real, live-confirmed obstacles, not assumptions:

1. **Disk space**: the account's HF token could *list* the gated `black-forest-labs/FLUX.1-schnell` repo's files but not download them (403 `GatedRepoError`, "you are not in the authorized list") until the repo's access terms were accepted on the model page directly - listing and downloading are gated differently, worth remembering next time a "can list but can't fetch" error looks like a token problem. Separately, this machine's `huggingface_hub` cache falls back to full file *copies* instead of symlinks on Windows without Developer Mode enabled, roughly doubling on-disk usage for anything cached that way - free space dropped from 29.9GB to 6.7GB downloading what should have been ~17GB of unique data. Worth using `local_dir=` (a direct, non-duplicated download) for anything sizeable on this machine going forward, not the default cache path.

2. **System RAM (not VRAM) crashed the standard load path**: this machine has only 15.9GB total RAM, and loading FLUX's standard bf16 T5-xxl text encoder (~8.9GB) alongside whatever else was already running reproducibly segfaulted `from_pretrained`'s weight-loading step (a `MemoryError` inside a diagnostic fallback path was actually masking the real crash - `diffusers.models.model_loading_utils.load_state_dict`'s `except Exception` handler calls `f.read()` unbounded trying to write a nicer error message, itself failing under the same memory pressure that caused the original failure; calling the real loader function directly, bypassing that masking handler, was what surfaced the actual cause). Fixed with the standard low-resource FLUX community pattern: a pre-quantized fp8 T5 (`comfyanonymous/flux_text_encoders`, ~4.9GB, confirmed to share exact tensor key names with `transformers.T5EncoderModel` so no remapping was needed) loaded via `accelerate.init_empty_weights()` + `load_state_dict(assign=True)`, since `T5EncoderModel` has no `from_single_file` (that's diffusers-only, not a plain `transformers` model method). Loading the GGUF-quantized transformer *before* the T5 and moving it to `cuda` immediately (freeing its CPU-RAM footprint before T5's own load needs headroom) was the other half of the fix - order-of-operations mattered as much as the fp8 swap itself; loading T5 first (same total memory, same components) reliably failed even with the smaller fp8 file.

3. **Race-identity accuracy**: an undescribed "dragonborn" rendered as a bearded human/dwarf, or in one case incorrectly grew tiefling-shaped horns and bat wings - neither is in the SRD. A small `RACE_DESCRIPTORS` override (only for the two races whose SRD look diverges most from a plain human: dragonborn's scales/reptilian head/no hair, tiefling's horns/tail) fixed this in most regenerated samples, confirmed against a real side-by-side regeneration - though not every sample: one dragonborn cleric retry still rendered a split "two heads" composition (a floating dragon head plus a separate human-looking face with hair), read as ordinary low-step-count generation variance rather than a prompt-accuracy problem, since the same fixed prompt produced a clean result for a different race/class/gender combination moments earlier.

**Live-verified the actual batch script (not just isolated single-image tests) for the one thing a fresh-process-per-image smoke test can't answer: does memory grow across many sequential generations in one long-running process?** A 12-image real run through `src.cli.generate_portraits` showed no leak trend (generation time stayed flat to slightly declining, 297-410s/image, no upward drift) and survived a genuinely alarming moment mid-pipeline-assembly where available system RAM bottomed out at 72MB free before recovering to 1.4GB+ once `enable_model_cpu_offload()`'s hooks took over - worth knowing this dip happens and is apparently survivable, not necessarily a sign of imminent failure, at least on this exact hardware/model combination. Sustained per-image time (300-340s) came out meaningfully slower than either isolated single-process smoke test (148s and 319s) - most likely GPU-side resource pressure during actual generation (VRAM dropped as low as ~680MB free mid-step) compounding across a long-running process in a way two independent one-shot processes didn't exhibit both times, not fully root-caused. At this rate the full ~550-image set (216 PC/companion + 334 monster, after the hair-color-permutation cut below) would take roughly 46-52 hours - deferred to a later session, run in smaller chunks the script's existing resumability already supports natively (skip-if-exists, no separate manifest or `--resume` flag needed).

**Mid-session scope cut: dropped hair color from the portrait key entirely** (race x class x gender x hair_color -> race x class x gender), a deliberate ~3x reduction in the PC/companion portrait count (648 -> 216) to make the local batch job's wall-clock time tractable, now that "a paid API absorbs the per-image cost" was no longer the operating assumption. Since hair_color's only purpose anywhere in this codebase was portrait selection, removed it end-to-end rather than leave inert UI - `Character.hair_color`, `VALID_HAIR_COLORS`, `CompanionSpec`'s required field (+ all 5 companion YAMLs), the DB column (the migration was edited in place rather than paired with a new drop-migration, since it had only ever been applied to this project's own local dev DB and had never been pushed - the live dev DB's actual `hair_color` column was dropped directly via SQLite 3.50's native `DROP COLUMN` support, verified against both that DB, which holds 22 real characters accumulated across this project's whole playtesting history, and a genuinely from-scratch migration run), the REST request/response fields, and every frontend touchpoint (`portraitUrl`, `LiveCharacter`, `Character`, the wizard's hair-color `<select>`, both new sheet components).

**Combat log, enforced equipped-weapon tracking, and a stale-prompt fix (2026-09-14) - driven by playtesting the finished character sheet, not a plan.** Playing a Ranger then a Wizard through full encounters surfaced five concrete gaps: no visibility into equipped gear or spell-slot counts, no exact damage/heal numbers (the narrator's prose isn't reliable for that - one heal was narrated as "restoring his vitality without a drop in his health points," no number at all), no way to tell at a glance whose narration text was whose, and no natural-20/1 callouts. Planned in 4 phases; the user's own clarifications during planning turned "show equipped gear" into a real, enforced rules feature rather than a display tweak (a character can own multiple weapon sets - e.g. a greataxe and a pair of daggers - and swap which is active; attacking with something not in the active set is now rejected), and surfaced weapon-combination legality and a once-per-turn equip limit as additional requirements mid-review (tracked as GitHub issues #11-#13 alongside the already-deferred armor-swapping and Two-Weapon-Fighting-bonus-attack follow-ups).

**The single most useful research finding, again: requirements 2-5 (damage/heal/crit/coloring) needed zero new backend data.** `src/engine/events.py`'s `Event` model already carries everything (`attack_roll.natural`/`critical`, `damage_dealt.amount`, `hp_change.amount`...), and `session.py`'s `_state_update_message` already dumps the *entire* `GameState` - including `events` - on every message; `LiveGameState` just never declared the field. The narration/events correlation turned out to be exact, not fuzzy: `session.py`'s per-action-resolution loop always broadcasts one `narration` message immediately followed by one `state_update` for that same resolution, so `useSessionSocket` can reliably pair "this narration text" with "these mechanical events" just by tracking an event-count high-water mark across the two messages - no correlation id, no text-parsing of LLM prose needed.

**Equipped-weapon tracking was the one genuinely new engine feature.** `Character.equipped_weapons` (at most 2, a two-handed weapon must be alone, 2 together must both be `"light"` per the SRD's real dual-wielding rule - confirmed live against the vendored equipment `properties` field, e.g. `dagger` -> `finesse, light, thrown, monk`) is auto-populated at creation by greedily trying `chosen_equipment` (the player's own deliberate pick) *before* the rest of a class's fixed starting kit - a real bug caught while writing this: without that ordering, a Barbarian who explicitly chose a battleaxe ended up auto-equipped with javelins instead, purely because javelins are listed first in the class's fixed SRD kit. A new `"equip"` verb (doesn't end the turn, same free-object-interaction treatment as Second Wind/Rage, gated by its own `equip_used_this_turn` flag reset on the identical "turn actually advances TO this character" schedule Phase 9H already established for `bonus_action_used`, for the identical reason) lets a player switch. `_pc_attack_params`/`_match_weapon_by_name` now only ever match a weapon from the equipped set, not the whole inventory (previously: literally the whole SRD weapon list, regardless of ownership) - closing a gap that had stood since Day 14.

**A second real, live-caught DB persistence bug, this time one that had to be fixed immediately, not flagged as a follow-up.** Live-verifying the equip feature in the actual browser: a freshly-created character's equipped set showed correctly in the create response, but came back empty after any reload - `CharacterRecord` had no column for it and neither mapper function passed it through, the exact same bug shape already documented three times now (Day 17, and twice in this session for gender/hair_color/race_index). Unlike those, this one couldn't be waved off as "one of the ~20 pre-existing dropped fields, out of scope" - a live WS session builds its `GameState` from a *reloaded* character, so an always-empty equipped set would have silently forced every real attack through the unarmed-strike fallback regardless of what the player actually equipped, making the whole feature nonfunctional the moment it left the character-creation response. Fixed the same way as the earlier ones: a new column, a migration with `server_default='[]'` (SQLite's own not-null-needs-a-default requirement, by now a running theme in this file), both mapper functions updated.

**A confusing debugging session with two compounding, unrelated causes, worth remembering the shape of.** Live-verifying in the browser, `CharacterDetailSheet` crashed with "Cannot read properties of undefined (reading 'length')" and a React "hook order changed" warning pointing at `LivePlay`. Both looked like real bugs in the new code and both were red herrings: (1) the frontend dev server's Vite HMR had accumulated inconsistent state across many rapid edits to `sessionClient.ts`'s hook count - a full server restart (not just a page reload, which wasn't enough) cleared it; (2) the *backend* dev server, running the whole session without `--reload`, was still serving the `Character` Pydantic model from *before* any of this session's engine changes - so `equipped_weapons` was silently absent from every API response, not merely empty, until that server was restarted too. Confirmed directly rather than guessed: fetching `/src/ws/sessionClient.ts`'s raw served text showed my current source was there (ruling out a stale frontend bundle), then fetching a live character's raw JSON showed `equipped_weapons` completely missing from the payload (not `[]` - actually absent), which only makes sense for a Python process still running pre-edit code. Once both dev servers were restarted with a clean character, the feature worked exactly as designed on the first real try - actor-distinct colors matching exactly between the combat grid and party sidebar, an equip-then-attack sequence narrating correctly without ending the turn, a `-7 HP` damage badge and two separate natural-1 "Critical Miss!" badges rendering with the right colors, an illegal weapon combination and a same-turn double-equip both correctly rejected. Also directly caught, not something to fix here (already-documented, explicitly out-of-scope territory): `class_resources` is part of the same ~20-field DB round-trip gap, so Second Wind read as 0 uses remaining on a freshly-created-then-reloaded character despite starting with 1 - a good illustration of exactly why the equipped-weapons fix above couldn't be deferred the same way.

**Also fixed, discovered while writing the `equip` verb's own prompt description**: `src/llm/prompts/intent_parser.md` and `player_agent.md` both predated Phase 9's later verbs entirely - `stabilize`, `death_save`, `grapple`, `shove`, `second_wind`, `rage` were fully engine-implemented and tested (via hand-constructed `ParsedAction` fixtures that bypass the LLM) but never described in either prompt's verb legend, meaning free text like "I use my second wind" almost certainly misparsed as `invalid` in every real playthrough this whole project has ever run, for both the human and every AI companion - not caught until now because no existing test exercises these verbs through the actual LLM. Live-confirmed the fix directly against the real Ollama teacher model: all six verbs plus the new `equip` now parse correctly from natural phrasing.

353 tests passing (up from 343 - the whole trio of features), `ruff`/`mypy`/`tsc -b`/`npm run lint` clean throughout.

**Narration pacing, stuck-while-unconscious, and starting healing potions (2026-09-16) - three more gaps surfaced by live playtesting, not planned work.** (1) The narration log dumped several entries at once (a burst of resolved companion/monster turns arriving as separate WebSocket messages within milliseconds of each other) instead of reading as a sequence - fixed client-side (`sessionClient.ts`) with a reveal queue that paces entries by real elapsed time since the last reveal, gating the action input (`isMyTurn`) on the queue having fully drained, per explicit user choice, rather than just on the server's `awaiting_input`. (2) The human's own character being unconscious (still rolling death saves) left the UI waiting on free-text input for a decision that isn't actually a decision - mirrored the exact fix `player_agent_node` already has for an unconscious *companion* (`src/graph/nodes/player_agent.py`) into `_autoplay_non_human_turns` (`session.py`): force a `death_save` `ParsedAction` server-side instead of returning to wait on the human. Confirmed via `turn_engine._skip_this_turn` that this can't double-fire or loop once stable - that check already makes `_advance_turn_skipping_dead` skip a stable-unconscious character's turn entirely, for the human same as any companion. (3) `use_item`'s hardcoded healing-potion path (Day 14) was permanently unreachable in real play - nothing ever put `"potion-of-healing"` in a fresh character's inventory (the wizard's equipment picker can't offer it either, since a synthetic non-SRD index has no `weapon_category`/`armor_category` for `GET /characters/equipment` to key off of) - fixed with a one-line `DEFAULT_STARTING_CONSUMABLES` grant in `create_character()`, covering both PCs and all 5 companions for free since both go through the same call.

**A real bug in the pacing fix's own first draft, caught by testing with actual browser sampling rather than trusting the logic by eye.** The initial "idle" check (`queue.length === 0 && timer === null`) was evaluated *at enqueue time* - but since `drainNext()` runs synchronously within the same call, a burst of messages arriving as separate `ws.onmessage` events (each a distinct macrotask) each found the queue already empty by the time their own handler ran, so every entry revealed instantly with zero pacing between them, the opposite of the intended fix. Confirmed live via a `performance.now()` sampling loop against the actual DOM before and after. Fixed by tracking *real elapsed time since the last reveal* (`lastRevealTimeRef`) instead of queue occupancy - `scheduleDrain()` computes `wait = max(0, DELAY - (now - lastReveal))`, so an isolated entry (naturally spaced further apart than the delay) still reveals with zero added latency, while a genuine burst gets correctly staggered regardless of how many separate WebSocket messages it arrived as.

**Verifying the unconscious-turn fix hit a real environment hang, not a code bug - worth recording the diagnostic shape.** A standalone offline script (and later a throwaway pytest file) built to exercise `_autoplay_non_human_turns` directly against a hand-built unconscious `GameState` reproducibly stalled for hours with the process pinned at ~11MB memory and, confirmed via `Get-Process ... CPU`, **0.03 seconds of accumulated CPU time after 3 hours** - not slow, genuinely idle/blocked, unlike ordinary resource contention (which still burns real CPU cycles, just interleaved slower). Killing it and retrying with a clean process table reproduced the identical stall at the identical point (right after `transformers`'s import warnings), ruling out leftover contention from the concurrently-running dev servers as the cause - something about that specific standalone import path blocks indefinitely, not chased further since it's isolated to the diagnostic script itself (the *production* code path was never in question - the full offline suite, which imports the same `session.py`/`turn_engine.py`/`graph_builder.py` modules, ran clean in under 20s throughout). Abandoned the standalone-script approach entirely rather than keep debugging tooling instead of the feature.

**The real live confirmation came from talking to the actual running server directly over a raw WebSocket, bypassing both the flaky script and the multi-step wizard UI.** `POST /sessions` (already a plain REST endpoint) started a real session for an existing test character with its DB-persisted `hp` set to 1 beforehand (a legitimate way to force "one hit from unconscious" without touching any in-memory engine state), then a `new WebSocket(...)` opened directly in the browser console drove and observed the entire encounter - no React UI needed to prove the backend behavior. Real Ollama-backed play showed the exact intended sequence with zero human input at any unconscious turn: knocked unconscious by a goblin hit, a second goblin's hit applying the existing 2-automatic-failures-against-a-helpless-target rule (Phase 9C), the character's *own* next turn auto-resolving a death save (narrated, `death_save_successes` incrementing) with no `awaiting_input` ever sent for it, then a further critical hit finishing them off (4 total failures, `is_dead=True`) into a correctly-detected `defeat` - the solo party's legitimate loss condition, not a bug. Confirms the fix end-to-end against the real graph/LLM stack, something the synthetic script was never able to do anyway.

**Cleared the GitHub issue backlog (2026-09-16, #12-#15), plus a real bug caught along the way - not planned work, just working through the tracked issues from earlier sessions.** Closed #11 as stale documentation first (already-shipped feature, confirmed by reading its own body). Four commits:

- **#12 - Two-Weapon Fighting's off-hand attack.** New `"offhand_attack"` verb, gated by the same `bonus_action_used` flag as Second Wind/Rage. Widened `_pc_attack_params` with `include_ability_damage_bonus: bool` (drops the ability modifier from damage only, per SRD - the attack roll itself is unaffected) rather than duplicating the whole weapon-lookup/attack-math function for one flag's worth of difference. Always resolves against `actor.equipped_weapons[1]` - the "other" weapon - and since `weapon_combo_is_legal` already guarantees any 2-weapon equipped set is light+light, no separate eligibility check was needed beyond `len(equipped_weapons) == 2`.
- **#13 - Armor swapping, real dynamic AC.** `Character.ac` was computed once at creation by scanning the whole inventory for any armor piece ("last non-shield armor wins, not a real scenario" - a documented hack). Added `equipped_armor`/`equipped_shield` (mirroring `equipped_weapons`, but single slots - no combinatorial legality question between them), extracted the AC formula out of `character_creation._compute_ac` into `rules.armor_ac(equipped_armor, equipped_shield, dex_mod, fighting_style, equipment)` so both creation and the widened `"equip"` verb can call it, and made `has_non_proficient_armor` check the equipped slots instead of the whole inventory (the exact fix the original equipped-weapons feature already made for the Dueling check, now applied to armor). New DB columns + migration - a live session's `GameState` comes from a *reloaded* character, so skipping persistence would have made every equip-armor action pointless the moment a real session started, the same lesson `equipped_weapons` itself already taught.
- **#14 - Surface initiative rolls in the log.** `"initiative_rolled"` had been sitting in the `EventType` Literal, fully declared, never emitted - `roll_initiative` computed a real d20+DEX roll per combatant but threw away everything except the final sorted order. Now returns `InitiativeRoll(character_id, natural, modifier, total)` per combatant; `build_encounter_state` turns each into an Event. Frontend needed only one new `formatEvent.ts` case - the existing event-count high-water-mark pairing in `sessionClient.ts` already attaches whatever's newly in `GameState.events` to the next narration entry, with zero wiring changes.
- **#15 - Item characteristics in the inventory list.** `EquipmentSummary` only ever carried `{index, name, category}` - real weapon damage/property and armor AC/stealth data was already being read server-side for actual mechanics (`turn_engine.py`, `rules.armor_ac`) but never reached the frontend. Widened the endpoint, added `itemDetail`/`nameWithDetail` helpers to `CharacterDetailSheet.tsx` rendering e.g. "Dagger (1d4 piercing, finesse, light, thrown, monk)" / "Leather Armor (AC 11 + Dex)" / "Shield (+2 AC)" inline wherever an item name already appeared.

**A real, pre-existing bug surfaced live while verifying #13, unrelated to any of the four issues' own scope - found because this was the first live test in the project's history to choose a single *light* weapon via `chosen_equipment`.** The weapon auto-equip loop (`character_creation.py`, since the original equipped-weapons feature) scanned `[*chosen_equipment, *inventory]` as two back-to-back ranges - but `chosen_equipment` is always appended into `inventory` a few lines earlier, so a chosen item's own inventory slot got visited a second time. For a non-light weapon this was invisible (`weapon_combo_is_legal` rejects two non-light weapons, so the accidental duplicate silently failed the legality check and never showed). For a single chosen dagger, though, the duplicate passed the "2 weapons, both light" check against *itself*: `GET /characters/{id}` for a character with exactly one dagger reported `equipped_weapons: ["dagger", "dagger"]`, confirmed directly against `inventory.count("dagger") == 1`. A plain dedup would have overcorrected - Rogue's own fixed kit legitimately grants 2 real daggers (`quantity: 2` in the vendored SRD data), which must still offer both as candidates. Fixed with a `Counter` that reorders `inventory` (chosen items first) without ever changing how many times any index appears - two new regression tests lock in both halves (a single chosen light weapon isn't doubled; Rogue's genuine two daggers still both get equipped).

**Live-verified the whole arc via two complementary paths**, each proving something the other couldn't: a raw WebSocket driven directly from the browser console (`debug_action` messages against a real session) confirmed the off-hand attack's reduced damage and non-turn-ending behavior against the real engine math (main-hand dagger hit for 6, the identical weapon's off-hand follow-up for 3 - exactly the ability-modifier difference, nothing else), and a full wizard-to-live-play run through the actual React UI confirmed the frontend rendering: `"Goblin 1 rolls initiative: 20 (d20 18+2)"` badges in the narration feed, `"Equipped: Dagger (1d4 piercing, finesse, light, thrown, monk)... Leather Armor (AC 11 + Dex), Shield (+2 AC)"` on the character sheet, and `AC 15` matching the hand-computed value (11 base + 2 Dex + 2 shield) exactly - which is also what caught the double-equip bug above, visible as two Daggers in the Equipped line for a character who'd only picked one. 370 tests passing (up from 353), `ruff`/`mypy`/`tsc -b`/`npm run lint` clean throughout, dev DB cleaned of every test character/session created along the way.

**Narrator/companion/campaign-gen output occasionally leaked non-English (CJK) text into narration (issue #16, live-observed, fixed) - qwen2.5:7b-instruct is itself of Chinese origin, and none of the three free-text-generating prompts (`narrator.md`, `player_agent.md`, `campaign_gen.md`) ever told it what language to respond in.** Added an explicit "Write only in English - no other language, script, or mixed-language text" instruction to all three. Live-verified by calling `narrator_node` directly against the real model 8 times with realistic multi-actor movement events (the same shape of event that produced the original live report) - 0/8 runs produced any non-ASCII output. A prompt-level mitigation, not a hard guarantee; worth revisiting if it recurs. Issue #17 filed (not implemented) for a related, separately-reported UX request: the Inventory list renders as one `.join(', ')` string rather than a real vertical list, which reads worse now that #15 added real per-item stat text to each entry.

**Issue #17 done - Inventory now renders as a real `<ul>`/`<li>` list, reusing the same style Class resources/Spell slots already use.**

**The prompt-only fix for #16 turned out not to be enough - a fresh CJK leak showed up in the very next live playtest, right after the fix landed.** Confirmed the user's own instinct was right: stripping the offending characters after the fact isn't a real fix, since a sentence can span both languages (the observed leak was literally "its步伐轻巧而敏捷" - removing the Chinese half leaves "its" dangling with nothing after it, corrupting the sentence a different way instead of fixing it). Added `providers.contains_cjk()` (a narrow CJK-Unicode-range check, deliberately not "any non-ASCII" - legitimate English prose can have curly quotes/em dashes/accented names, and a blanket check would cause needless retries) and `chat_english_only()`, a `chat()` wrapper that retries up to 3 times if the response contains CJK text - the same validate-and-retry shape this project already uses for structured output (`chat_structured_best_effort`, `campaign_generator`'s own retry loop), just applied to plain text. `narrator_node` and `player_agent_node` now call `chat_english_only` instead of `chat`; `campaign_generator._validate_generated` got a matching CJK check folded into its *existing* retry loop (with a problem description the model can act on: "narrative_intro contains non-English text") rather than a separate mechanism, since every generated-text field already flows through that one validator.

**Fixing this surfaced two real, unrelated bugs found by actually running the code, not just reading it.** (1) `tests/unit/test_player_agent_node.py` monkeypatches the name `chat` as imported into `player_agent.py`'s own namespace - renaming that import to `chat_english_only` silently broke every one of those mocks (they'd have kept "passing" while no longer testing anything, since the patched name was no longer referenced anywhere) - caught by tracing exactly how monkeypatch-by-module-attribute resolves names, not by the offline suite itself (it was still green), and fixed by updating every `monkeypatch.setattr(player_agent_module, "chat", ...)` to `"chat_english_only"`. (2) Running `tests/llm/test_narrator.py` live (something the default `uv run pytest` never does, since `-m llm` tests are deselected) turned up a real, already-existing regression from issue #14 earlier this same session: `test_narrator_returns_empty_for_no_new_events` hardcoded `events_before: 0` against a freshly-built `GameState`, which used to have zero events at creation but now starts with 4 real `initiative_rolled` events - the test was quietly asserting the wrong thing (that narrator produces no narration when there ARE new events, just ones it hadn't accounted for) until this session finally ran it live. Fixed by computing `events_before` from `len(game_state.events)` instead of assuming zero. A good reminder that `-m llm`-marked tests need to actually be run occasionally, not just trusted to still pass.

Also added: `tests/unit/test_llm_providers.py` (new, `contains_cjk` hand-picked cases - the one pure/deterministic piece of this fix, tested offline; `chat`/`chat_structured`/`chat_english_only` themselves stay live-verified only, matching this project's established "LLM code gets live verification, not mocking" stance) and two new `contains_cjk` assertions in `tests/llm/test_narrator.py`'s existing live tests. 374 offline tests passing (up from 370), plus live confirmation via `tests/llm/test_narrator.py` and `tests/llm/test_player_agent.py` (both green against the real model) - `ruff`/`mypy` clean throughout.

**A full `--autoplay` run afterward (goblin_ambush_oneshot, zero human input, real Ollama) confirmed all of today's changes together didn't break the core simulation loop** - clean exit, zero CJK leaks across 45 turns/8 rounds of real narration, death saves/unconscious/death and scene-image generation all resolving normally, ending in a legitimate (if unlucky) party defeat. Judge scores: mechanical_consistency 8, narrative_quality 6, overall 7. Surfaced one small, real, pre-existing data bug along the way: Grom Ironfist's persona declares him swinging a "battleaxe" every turn, but `grom_ironfist.yaml`'s `chosen_equipment` actually gave him a `longsword` - his own free-text declarations were rejected 3 times a turn (correctly - the equipped-weapon enforcement from earlier in this project's history was doing its job), forcing the existing consecutive-invalid circuit breaker every single turn rather than letting him actually attack. Not a code bug, just a companion authored with a persona/gear mismatch (the same class of issue Sister Mira's chain-mail was, back in Phase 9's live playtesting) - fixed by swapping his `chosen_equipment` to `battleaxe` (also versatile, same proficiency, one-handed-compatible with his shield) instead of adding special-case code. Verified live: `build_companion` now auto-equips `["battleaxe"]` with `ac=18` unchanged (only the weapon slot was ever wrong).

**SRD mechanics audit (2026-09-16) - a full pass comparing the actual engine code against real SRD 5.1 content (races, classes, ~250 CR≤5 monsters), not general D&D knowledge, to find what's still missing after Phase 9.** Filed as 9 GitHub issues (#18-#26): monster damage resistance/immunity/condition-immunity (#18), Pack Tactics (#19), a real bug in `resting.apply_long_rest` restoring spell slots from the level-1-only table instead of the level-aware one Phase 9J introduced (#20), Divine Smite + Cunning Action as the cheapest high-value class-feature adds (#21), basic monster spellcasting (#22), one-line racial traits like Halfling Lucky/Half-Orc Relentless Endurance (#23), Monk/Wild Shape as larger deferred efforts (#24), a lower-priority batch (#25), and a real bug the user spotted live in the same session - a character equipped with 2 one-handed weapons *and* a shield simultaneously, since `equipped_weapons` and `equipped_shield` have never been cross-validated against each other for hand occupancy (#26). Started implementing in the audit's own priority order.

**#18 (monster resistances/immunities/condition-immunities) done.** New `rules.monster_damage_multiplier`/`monster_is_immune_to_condition` - both always no-op (1.0 / False) for a non-monster target. Damage resistance/immunity/vulnerability entries in the vendored SRD data aren't a closed vocabulary - some are a bare type ("poison"), others a compound clause ("bludgeoning, piercing, and slashing from nonmagical weapons", e.g. Ghost) - matched by substring containment rather than exact equality so both shapes work correctly, with the nonmagical-weapons clause treated as an unconditional resistance since this project has no "magical weapon" concept to gate it on. Wired into the single existing damage chokepoint (`_apply_damage_and_handle_downing`, alongside Rage's own resistance halving) and into `_resolve_grapple`/`_resolve_shove` (both gained a new `srd` parameter) to reject an attempt outright against a condition-immune target, per SRD's "can't be grappled/knocked prone if immune" rule, rather than letting the contest roll and silently do nothing on a "success." Hand-computed regression tests at both the pure-function level (Ghost's compound clause, Skeleton's bludgeoning vulnerability, Shadow's grapple/prone immunity) and end-to-end through real `resolve_action` calls (an actual longsword swing correctly halved by Ghost's resistance; grapple/shove against Shadow correctly rejected before any dice roll). 382 tests passing (up from 374), `ruff`/`mypy` clean.

**#19 (Pack Tactics) done.** New `rules.monster_has_pack_tactics` checks a monster's SRD `special_abilities` for the exact `name` "Pack Tactics" (a free-text trait list, not an indexed vocabulary like `condition_immunities` - matched by literal name string since that's what the vendored data actually gives). Wired into `_resolve_single_attack`'s existing advantage computation, combined with `_ally_adjacent_to` - the identical "an ally is within 5ft of the target" helper Sneak Attack's own alternate trigger already uses, so no new adjacency logic was needed. Deliberately only wired into the weapon-attack path (`_resolve_single_attack`), not the separate spell-attack resolver - no CR<=5 Pack-Tactics monster in the curated roster casts spells, so scope stayed tight. Two end-to-end regression tests prove real advantage: feeding `_FixedRandom([5, 18])` at a wolf with an adjacent ally-wolf and asserting the recorded natural roll is 18 (the higher of two dice, proving advantage rolled twice), versus `_FixedRandom([5])` with no adjacent ally - only one value consumed, would fail loudly on a second roll if advantage wrongly applied. 385 tests passing, `ruff`/`mypy` clean.

**#20 (resting.apply_long_rest used the level-1-only slot/hit-die table) done.** `resting.py` predates Phase 9J's real leveling and was never revisited: `apply_long_rest` restored spell slots from `character_creation.LEVEL_1_SPELL_SLOTS` (a `class_index -> {spell_level: count}` table with no level dimension at all) and hardcoded `hit_dice_remaining = 1`, so a level 3-5 caster who rested was silently knocked back down to their level-1 slot layout and a single hit die. Fixed to read `character.level` and look up `character_creation.SPELL_SLOTS_BY_LEVEL[class_index][level]` (already built and correct - `level_up` has used it since Phase 9J, only `resting.py` had the stale reference) and set `hit_dice_remaining = character.level`. `apply_short_rest` was unaffected (it only ever spends whatever `hit_dice_remaining` currently holds, never resets it to a level-derived max). New regression test builds a level-5 wizard and asserts a long rest yields the real `{1:4, 2:3, 3:2}`/5 hit dice, not the level-1 `{1:2}`/1; the pre-existing level-1 test needed no change since level 1's row is identical in both tables. 387 tests passing, `ruff`/`mypy` clean.

**#21 (Divine Smite + Cunning Action) done.** Paladin wasn't in `SPELL_SLOTS_BY_LEVEL` at all, so smiting was impossible until a real half-caster progression was added (`{}` at level 1, then the standard PHB half-caster table through level 5 - deliberately absent from `LEVEL_1_SPELL_SLOTS`, since a level-1 Paladin really does have zero slots). Divine Smite itself is declared upfront on the `attack` action (`params["smite_slot_level"]`), not as a separate post-hit prompt - this engine has no mid-resolution pause to ask "it hit, smite now?", so `_pc_attack_params` validates eagerly (Paladin class, slot available, melee weapon only) but the slot is only actually spent/rolled in `_resolve_single_attack` if that specific swing hits and a slot is still available at that exact moment - an Extra Attack's second swing can smite again off a second slot even if the first swing already spent one, proven by a dedicated two-swing regression test. Damage: 2d8 for a 1st-level slot +1d8 per level above 1st (capped 5d8), +1d8 vs. undead/fiends (new `rules.monster_is_undead_or_fiend`, matching on the SRD `type` field, e.g. "imp" is a fiend), doubled on a crit like Sneak Attack's own dice (extra damage on the same weapon attack, not a separate spell attack roll). Cunning Action reuses Phase 9H's bonus-action machinery end-to-end: new `cunning_action` verb with `params["action"] = "dash" | "disengage"` delegates to the exact same `_resolve_move`/`_resolve_disengage` a full-action `dash`/`disengage` already use (a synthetic `verb="dash"` copy of the action so `_resolve_move`'s speed-doubling check fires), gated on `class_index == "rogue"` and `level >= 2`. Hide is deliberately not offered - this engine has no stealth/hidden-state mechanic at all, a separate feature issue #21's cheap-add scope didn't take on. 14 new tests (7 Divine Smite, 6 Cunning Action, 1 `monster_is_undead_or_fiend`), 400 tests passing, `ruff`/`mypy` clean.

**#22 (basic monster spellcasting, Innate Spellcasting only) done.** `cast_spell` was entirely PC-`class_index`-driven (`_spellcasting_ability_mod` derives attack bonus/DC from an ability score + proficiency bonus, which a monster has no formula for) - a monster could never reach it regardless of its stat block. The vendored SRD data's "Innate Spellcasting" `special_abilities` entry is fully structured (`{ability, dc, modifier?, spells: [{name, level, usage: {type: "at will"|"per day", times?}}]}`) with `dc`/`modifier` as flat numbers the stat block already computed - used as-is, not re-derived. New `rules.monster_innate_spellcasting` (the raw lookup), `rules.spell_mechanic` (moved here from turn_engine's private `_spell_mechanic`, now returning `str | None` instead of raising, so monster_ai's spell-selection can share the exact same attack/save/heal/unsupported classification a real cast later uses rather than a second copy), and `rules.normalize_spell_name`. New `Character.innate_spell_uses_remaining` (seeded at creation by `encounter.monster_to_character` for each "per day" spell only - "at will" is never tracked, unlimited). `turn_engine._resolve_cast_spell` branches immediately to a new `_resolve_monster_innate_spell` for any monster actor, which validates spell-known/mechanic-supported/target/range/uses-remaining and then reuses the *exact same* `_cast_attack_spell_at_target`/`_cast_save_spell_at_target` a PC's cast already resolves through - only `_monster_innate_attack_params`/`_monster_innate_save_params` (new, parallel to `_spell_attack_params`/`_spell_save_params`) differ in how the params get built. `monster_ai.choose_monster_action` now tries `_choose_innate_spell` (first castable spell in stat-block order, real range check, real usage check) before falling back to its existing melee-or-approach heuristic - a spellcasting monster finally differs from "walk up and swing." Real SRD coverage gap found and documented, not hidden: no monster in the entire vendored dataset (any CR, not just this project's CR<=5 roster) has an attack-roll innate spell with usable damage data (every `attack_type`-tagged innate spell is either a no-damage utility effect like Ray of Enfeeblement, or simply absent) - the attack-mechanic path is still implemented and unit-tested directly (a hand-built `innate` dict paired with the real SRD Fire Bolt), just has no live monster+spell pairing to exercise it end-to-end; the save mechanic has full real-data coverage both at-will (green-hag's Vicious Mockery) and 1/day-with-depletion (magma-mephit's Heat Metal). 14 new tests, 414 tests passing, `ruff`/`mypy` clean.

**#23 (Halfling Lucky, Half-Orc Relentless Endurance, Elf Keen Senses, Half-Elf Skill Versatility) done.** Lucky: `dice.roll_d20` gained `reroll_on_natural_1` (a fresh d20 replaces a kept natural 1, once - the reroll itself is never rerolled again, per SRD), threaded through `resolve_attack`/`resolve_saving_throw`/`resolve_skill_check`'s new `lucky` param and wired at every call site resolving a PC's own d20 (attack rolls, concentration/death saves, skill checks, stabilize, and a spell-save *target's* own save, since Lucky applies to a forced saving throw too) via new `rules.has_lucky_trait` (`race_index == "halfling"`). Deliberately NOT wired into `_grapple_shove_contest` - that function's own docstring already guarantees callers/tests exactly 3 d20 draws every time, and a conditional reroll would break that contract; documented, not silent. Relentless Endurance: new `Character.used_relentless_endurance_this_rest` (reset by `apply_long_rest`) and `rules.has_relentless_endurance`, hooked into `_apply_damage_and_handle_downing` right after `apply_damage` - a Half-Orc PC reduced to 0 HP (and not already unconscious from an earlier hit) drops to 1 instead, once per long rest, emitting a new `relentless_endurance` event (small `formatEvent.ts` badge added too). Keen Senses/Skill Versatility: `create_character` gained `chosen_racial_skills` (required, exactly 2, any real skill - validated eagerly, same pattern as `fighting_style`) for Half-Elf, and Elf unconditionally gets `skill-perception` folded into the same `skill_proficiencies` dedup chosen_skills/background proficiencies already share. Real regression caught and fixed by running the full suite: `_two_person_party()`'s Elf wizard fixture (used pervasively across turn_engine tests) is now also Perception-proficient, which changed one existing skill-check test's expected roll total (11 -> 13) - a correct behavior change, not a bug, fixed the same way issue #14's stale initiative-event-count assumption was. `CreateCharacterRequest`/`CompanionSpec` and the character-creation wizard (`CharacterCreator.tsx`) both gained the new field end-to-end, including a real 2-skill picker shown only for Half-Elf - verified live in the browser (created a real Half-Elf Fighter through the actual wizard UI, confirmed both chosen racial skills landed in the final sheet's skill list alongside class/background skills; confirmed Elf shows no picker at all and still lists Keen Senses as a trait chip). 19 new tests (5 dice.py reroll cases, 2 rules.py trait-helper cases, 6 character_creation.py racial-skill cases, 1 resting.py reset case, 5 turn_engine.py end-to-end Lucky/Relentless-Endurance cases) plus 2 pre-existing tests fixed for the new-correct Elf Perception proficiency, 432 tests passing, `ruff`/`mypy`/`tsc` clean.

**#24 part 1 (Monk: Unarmored Defense, Martial Arts, Ki, Flurry of Blows) done.** Monk had zero implemented class identity (mechanically an unarmed commoner with a shortsword) - the SRD data itself made this cheaper than expected: every simple melee weapon plus the shortsword already carries a `"monk"` property tag in the vendored equipment JSON (confirmed live), so new `rules.is_monk_weapon` is a one-line membership check, no hand-derived "simple, not heavy/two-handed" logic needed. `rules.monk_martial_arts_die_sides(level)` (1d4 through level 4, 1d6 from level 5 - only one tier boundary exists within this project's roughly-level-1-5 scope). `armor_ac` gained optional `class_index`/`wis_mod` params (every pre-existing caller unaffected by the defaults) for Unarmored Defense: 10 + DEX + WIS while unarmored *and* unshielded - a Monk holding a shield falls back to the ordinary formula, per SRD's literal "not wielding a shield" gate, still getting the shield's own flat bonus like anyone else. `_pc_attack_params` widened for both the unarmed-strike branch (the scaling die *replaces* the flat "1 + STR mod" entirely, DEX usable via `max(str_mod, dex_mod)`) and the weapon branch (DEX allowed for any monk-tagged weapon via a new `force_unarmed`-adjacent `is_monk_weapon_for_actor` check, and the weapon's own die bumped up to the Martial Arts die only when that's actually bigger - a level-5 Monk's dagger rolls 1d6, but a quarterstaff's own 1d6 is never shrunk to a level-1 Monk's 1d4). New `flurry_of_blows` verb/`_resolve_flurry_of_blows`: same bonus-action shape as Second Wind/Rage, spends 1 Ki via the existing `_use_class_resource`, two full unarmed strikes (unlike Two-Weapon Fighting's off-hand attack, neither drops the ability modifier) via a new `_pc_attack_params(..., force_unarmed=True)` escape hatch - needed because a Monk's starting kit auto-equips darts, so a plain `weapon_index=None` call would've picked those up instead of forcing unarmed (caught live by a failing test before this fix, not assumed). Ki points (`class_resources["ki"]`) scale every level via `level_up` (level = ki points, starting at 2 - unlike Second Wind/Rage's documented level-1-fixed simplification, freezing Ki would make Flurry unusable for any leveled Monk) - `level_up` also now recomputes a Monk's AC on an ASI, since WIS-dependent Unarmored Defense is the one case in this project where an ability score bump actually changes the AC formula's inputs (a general "AC recomputes on ASI for everyone" gap is real but pre-existing and out of this issue's scope). Explicitly deferred, matching the issue's own "further gaps" list: Patient Defense, Step of the Wind, Deflect Missiles, Slow Fall, Unarmored Movement, Stunning Strike. 15 new tests (2 rules.py die/weapon-tag cases, 3 more rules.py armor_ac cases, 3 test_leveling.py Ki/AC-on-ASI cases, 7 turn_engine.py end-to-end Martial Arts/Flurry cases), 448 tests passing, `ruff`/`mypy` clean. `class_resources` already renders generically in `CharacterDetailSheet.tsx`, so Ki needed no frontend change.

**#24 part 2 (Druid Wild Shape) done - issue closed.** Design: a wild-shaped Druid becomes, for attack-resolution purposes, exactly a monster character that still happens to have `is_pc=True` - `_resolve_attack`/`_monster_attack_params`/`_resolve_multiattack` already branch purely on `monster_index is not None`, never on `is_pc`, so temporarily setting `Character.monster_index` to the beast's index reuses that *entire* existing path for free (a real wolf Bite attack, real monster AC/resistances) instead of building a parallel one - confirmed end-to-end with a live wolf attack roll in the tests, not just asserted structurally. New `Character.wild_shape_beast_index`/`pre_wild_shape_snapshot` (a small nested `WildShapeSnapshot` model - hp/max_hp/ac/stats/speed/equipped_weapons/monster_index, the same nested-model precedent as `Condition`). New `rules.max_wild_shape_cr(level)` (0.25 at levels 2-3, 0.5 from level 4 - the real SRD table within this project's scope) and `wild_shape_beast_is_allowed` (adds the no-fly-ever/no-swim-below-4 gates, confirmed against real CR<=0.5 no-fly beasts like `wolf`/`black-bear` and rejection cases like `giant-poisonous-snake`/`giant-owl`). New `wild_shape` verb (full action) and `revert_wild_shape` (bonus action, restores the snapshot exactly - hp included, since a *voluntary* revert doesn't cost HP per SRD). The interesting part: SRD's "if the beast form drops to 0 HP, you revert and any excess damage carries over to your normal form" is implemented for real, not glossed over - `_apply_damage_and_handle_downing` gained a forced-revert branch (checked before Relentless Endurance, same "intercept before the standard 0-HP handling" pattern issue #23 established) computing `overflow = damage - actual_loss` (the same math Relentless Endurance's hook already needed) and applying it to the Druid's snapshotted real HP, then *falling through* (no early return) so the ordinary is_pc/unconscious logic - and even a second Relentless Endurance check, if the Druid also happens to be a Half-Orc - runs correctly against the real, reverted character rather than the beast; two dedicated tests prove both outcomes (overflow survived vs. overflow enough to also knock the reverted Druid unconscious). Real bug found and fixed along the way, not specific to Wild Shape: `apply_long_rest` was unconditionally rebuilding `class_resources` from the level-1-only table every time, which would have silently wiped a leveled Monk's Ki to nothing on every long rest (issue #20's exact bug class, just never triggered before because Part 1 had no test exercising a long rest against a leveled Monk) - fixed with a new shared `resting._max_class_resources(class_index, level)` used by both rest functions, and `apply_short_rest` widened the same way for Wild Shape's short-or-long-rest recharge. `level_up` also now grants Wild Shape's 2 uses starting at level 2, mirroring Ki's own pattern. Explicitly out of scope, documented not silent: real elapsed-time duration (persists until reverted/forced/a rest, the same simplification already accepted for Rage), and resting mid-combat while transformed (not a scenario `campaign_runner`'s rest flow can actually reach). 18 new tests (2 rules.py CR/speed-gating cases, 4 resting.py Ki/Wild-Shape-resource cases including the long-rest-Ki-wipe regression, 12 turn_engine.py end-to-end Wild Shape cases including both overflow-damage scenarios), 466 tests passing, `ruff`/`mypy` clean.

**#25 (lowest-priority batch) done - partial by design, issue closed with the rest explicitly deferred.** The audit's own grab-bag of 8 items, several already flagged in the issue body as needing real new architecture rather than a quick add - implemented only the two that reuse existing patterns cleanly: Arcane Recovery (Wizard) and Bardic Inspiration (Bard, deliberately narrowed - see below). Arcane Recovery slotted into `CLASS_RESOURCES_AT_LEVEL_1` as a genuine level-1 entry (`{"wizard": {"arcane_recovery": 1}}`) since - unlike Ki/Wild Shape from #24 - it never scales or changes availability, only the *budget* it grants does (new `arcane_recovery_slot_budget(level)`, half rounded up); `apply_short_rest` triggers it automatically whenever available (this engine has no "decline a beneficial effect" interaction anywhere, so auto-triggering is consistent, not a new kind of simplification) and recovers the lowest missing spell-slot level first, deterministically, since there's no per-slot choice UI anywhere in this project (same documented gap as equipment/skill option-trees). Bardic Inspiration needed real new plumbing but got it properly: `resolve_attack` itself gained a `bardic_die_sides` param that rolls and adds the bonus die *before* the natural-1/hit checks - not a bolt-on after the fact - because a boosted total can genuinely turn a miss into a hit in real play, proven by a dedicated test (natural 8, banked 1d6 rolling 4, clears an AC the raw roll wouldn't have). Deliberately narrowed to attack rolls only (`_resolve_single_attack`/`_cast_attack_spell_at_target`), not also ability checks/saving throws like real SRD allows - re-threading a second "banked bonus" concept through every d20 call site issue #23's Lucky trait already touched would have been its own issue-sized effort inside the *lowest-priority* one; documented in `Character.bardic_inspiration_die`'s own docstring, not silently narrower. Uses = CHA modifier (minimum 1) via new per-creation/per-level_up computation - the one class_resource in this project keyed off an ability score instead of a flat table, recomputed on level_up (after the ASI, unlike Ki/Wild Shape's level-only formulas) since an ASI could be the very thing that raises it. **Explicitly deferred, not attempted**: Action Surge (needs a real "second main action this turn" hook - this engine's existing `ends_turn=False` pattern only ever grants a second *bonus* action, never a second main one); Channel Divinity/Turn Undead (needs a new "turned" condition this engine's `monster_ai.py` has no forced-flee behavior to honor); Lay on Hands (needs a spendable-point-pool resource type `class_resources` doesn't have - it's uses-only); cover rules (this engine's `BattleMap.terrain` has no geometry/line-of-sight concept at all); known/prepared-spell restriction (a real new subsystem - per-class spell lists, a spellbook/preparation mechanic - `cast_spell` still matches the entire SRD spell list regardless of caster class/level, slot-level still gates affordability); exhaustion level 4 was already documented as deferred elsewhere, restated here for completeness only. 17 new tests (3 rules.py die-table/bardic-bonus-on-resolve_attack cases, 5 character_creation.py/test_leveling.py resource-computation cases, 4 resting.py Arcane Recovery cases, 5 turn_engine.py end-to-end cases across both features) plus 1 pre-existing test fixed for the new-correct Wizard class_resources, 483 tests passing, `ruff`/`mypy` clean.

**#26 (equip hand-occupancy tracking) done.** Real bug the user spotted live, not part of the SRD audit's own priority order - a Human Fighter ended up with 2 one-handed weapons *and* a shield simultaneously equipped, since `equipped_weapons` and `equipped_shield` (added independently by issue #13) had never been cross-validated against each other at all. `rules.weapon_combo_is_legal` gained a `shield_equipped: bool = False` param (every pre-existing call site defaults to the old, unaffected behavior) that folds a shield's own hand cost into the same check: 2 already-legal one-handed weapons plus a shield is 3 hands (illegal); a lone two-handed weapon plus a shield is also illegal (it already needs both hands by itself); a single one-handed weapon plus a shield is fine (1 + 1 = 2). Both the real bug sites got fixed: `character_creation.py`'s armor/shield auto-populate loop now only fills `equipped_shield` when the weapons the earlier weapon loop already chose leave a hand free (weapon-loop priority kept, not reordered - a player who explicitly chose 2 daggers wants to dual-wield, not wear a shield they happened to also be carrying); `turn_engine._resolve_equip` now validates the *combined* resulting loadout, not just whatever's newly named in one call - the original live bug's actual shape (`equip` called with `params["items"] = ["shield"]` alone, silently equipping it with zero check against the 2 weapons already worn, since the old check only ever looked at newly-resolved weapons from that same call). 11 new tests (5 `weapon_combo_is_legal` direct cases in `test_rules.py`, 1 auto-populate case reproducing the exact live loadout, 5 `_resolve_equip` end-to-end cases including the single-call three-item reproduction), all 5 bundled companions' auto-equipped loadouts re-verified live as unaffected (none were actually broken by the bug - it only ever showed up on a live player-created character), 494 tests passing, `ruff`/`mypy` clean.

**Found live, fixed immediately (no tracking issue filed - same precedent as `equipped_weapons`'s own fix) - `CharacterRecord` was missing `class_resources`/`level`/`hit_die_sides`/`hit_dice_remaining`/`saving_throw_proficiencies`/`fighting_style` and 4 more fields, so every single one silently reset on every DB round-trip.** Caught by the user's own manual testing right after #18-#26 landed: a fresh level-1 Barbarian rejected "I fly into a rage" with "no rage uses remaining," despite never having raged. Root cause: `src/api/db/models.py` already had a comment (added back when `race_index`/`gender` were persisted) flagging "~20 other Character fields (class_resources, ...) silently drop on a DB round-trip... not fixed here, out of scope" - and separately, right below it, the exact reasoning for why that stance was wrong for a field a live session actually needs immediately: "a live WS session builds its GameState from a *reloaded* character," which is exactly why `equipped_weapons`/`equipped_armor`/`equipped_shield` got fixed on the spot rather than deferred. `class_resources` (and `level`/`hit_die_sides`/`hit_dice_remaining`/`saving_throw_proficiencies`/`fighting_style`, all populated at creation and needed from the moment a session starts) hit the exact same "deferred" bucket by oversight, and `src/api/ws/session.py:163` confirmed the exact live path (`_record_to_character(human_record)` builds the session's player Character straight from the DB row, never from the in-memory create response) - meaning Rage/Second Wind/Ki/Wild Shape/Arcane Recovery/Bardic Inspiration (this whole session's #18-#25 work) were unusable in any real live session, invisible to the test suite because pytest builds `Character` objects directly in memory and never round-trips through the DB. Fixed by adding all 10 missing-but-load-bearing fields to `CharacterRecord` plus both `_character_to_record`/`_record_to_character` conversion functions (a real Alembic migration, `ac48025c80e6`, with `server_default`s on the NOT-NULL ones so SQLite's existing rows don't break) and backfilling every one of the 23 pre-existing dev-DB rows with their real class-derived values (hit_die/saving_throws from the real SRD class data, `class_resources` from the same `CLASS_RESOURCES_AT_LEVEL_1`/CHA-modifier logic `create_character` itself uses) rather than leaving them stuck on the migration's own generic fallback (`hit_die_sides=8`, which is only correct for 2 of the 6 SRD classes). `fighting_style` couldn't be backfilled for old rows (a player's original choice, never stored anywhere to recover it from) - left `None`, a real but narrow, honestly-acknowledged loss limited to pre-migration characters; every new character created after this fix persists it correctly. The remaining 4 fields (`used_relentless_endurance_this_rest`, `wild_shape_beast_index`, `pre_wild_shape_snapshot`, `bardic_inspiration_die`) are rarer mid-rest-cycle/mid-transformation state, fixed in the same pass since it cost nothing extra rather than leaving a second copy of this exact bug for later. New regression test (`test_api_characters.py`, same shape as the existing Day-17 class_index/skill_proficiencies guard) creates a Barbarian through the real API and asserts `class_resources`/`level`/`hit_die_sides`/`saving_throw_proficiencies` all survive a real create-then-refetch round-trip, not just an in-memory check. The other ~15-field gap the original comment still describes (`is_dodging`, `bonus_action_used`, `death_save_successes`, `position`, etc.) is genuinely combat-turn-scoped and *should* reset to its Pydantic default for a fresh encounter build - correctly left alone, not the same bug shape. 496 tests passing, `ruff`/`mypy` clean.

**Small live-found fixes from the same manual-testing pass.** `CharacterDetailSheet.tsx`'s hardcoded "Available actions" list (a frontend-only summary, not server-computed - see its own docstring) was missing `Stabilize` and `Death Save` entirely, despite both being real, unconditional verbs since Phase 9C - the user spotted this directly; added both list entries, verified via `tsc`. Separately, a real content bug crashed a live WS connection outright: `data/campaigns/encounters/wolf_den.yaml` only defined 2 `party_spawn_points`, but `PartySetup.tsx` lets a player pick up to 5 total party members (1 PC + `MAX_COMPANIONS = 4`) with zero validation against what any specific encounter's map supports - `build_encounter_state` raised `GameStateBuildError` for a 3-person party, and since that exception happens before `create_session(...)` ever runs, it crashed the whole ASGI WebSocket handler with no message reaching the client at all, leaving `LivePlay.tsx` stuck on "Connecting to the game server..." forever. Fixed the immediate content gap directly (added a `party_3` spawn point at an empty floor tile) - `load_encounter` reads the YAML fresh every call, no caching, so no backend restart was needed. The systemic gap (any future party-size/spawn-point mismatch will crash a session the exact same silent way) is tracked as #29 rather than fixed now, at the user's own call - not a data problem, an error-handling one (the WS handler needs to catch and report a real error instead of letting an unhandled exception crash the connection).

**Live-found frontend desync: the party sidebar/defeat banner jumped ahead of the paced combat log.** User report from live play: "after I do my move, a bunch of log narrations being fired instantly, looks like that it calculates that my character already dead" plus "right bar all players displayed as dead, but log continue to fire some messages" - both symptoms of the same root cause, confirmed by reading `sessionClient.ts` directly rather than guessing. `useSessionSocket` already paces narration-log reveals at one entry per `ENTRY_REVEAL_DELAY_MS` (3000ms) so a burst of resolved turns doesn't dump the whole log at once - but the live `gameState` (which drives the sidebar's HP/dead markers, the current-turn ring, and the victory/defeat banner) was applied via `setGameState` *immediately* on every `state_update` message, completely unpaced. Once the human PC drops to 0 HP, `_autoplay_non_human_turns` (`src/api/ws/session.py`) starts auto-issuing the human's own forced death-save turns too and keeps looping through every other actor with no pause until `status` leaves `"in_progress"` - so a whole encounter's remaining turns (here, an entire losing fight) can resolve server-side in one unbroken burst reaching the browser within milliseconds. The sidebar/banner jumped straight to the final "everyone dead / Defeat" state almost instantly, while the narration log was still slowly draining the backlog of what actually led there over the next tens of seconds - exactly matching both complaints. Not a backend bug (turn-skip/victory-defeat logic in `turn_engine.py` is correct - `_check_victory_defeat` keys off `is_dead`, not `hp<=0`, and `_skip_this_turn` correctly still gives an unconscious-but-unstable character a turn to attempt death saves). Fixed purely client-side in `sessionClient.ts`: each queued `NarrationEntry` now carries the `game_state` it arrived with (`gameStateSnapshot`), and `setGameState` is called at the same moment that entry is actually revealed into `narrationLog` (inside `scheduleDrain`'s timeout), not when the WS message lands - a `state_update` with no new narration/events (nothing pacing it) still applies immediately, unchanged. Verified via `tsc --noEmit` (clean) and `oxlint` (only pre-existing warnings, none new); live-verified by reloading the already-terminal Defeat session (nothing left to lose there) and confirming the app still boots cleanly with the change live.

**#29 (WS session-build crash hangs the client forever) done.** `session_websocket` now wraps `_get_or_create_default_session` in a `try/except GameStateBuildError` - the one exception type that could actually reach this point uncaught (`_build_real_session_setup`'s own docstring already covers every *other* missing-data case by returning `None`, falling back to the demo encounter). On catch, sends a real `{"type": "error", "detail": ...}` message and closes the socket (code 1011) instead of letting the exception crash the whole ASGI handler - matches the existing `TurnEngineError`/`NotImplementedError` handling pattern already used later in the same file, just one level earlier (before `create_session` ever runs, so nothing gets left in `_sessions` for a later reconnect to trip over - asserted directly in the new test). `LivePlay.tsx`'s `!connected && !gameState` early-return branch (previously just "Connecting to the game server...", unconditionally) now checks `error` first, since a setup failure's `onclose` can land before any `state_update` ever arrives - without this the error message was already being received and stored but never actually rendered, still showing an infinite spinner. New test (`test_ws_session_real_start.py`) monkeypatches `build_encounter_state` to raise `GameStateBuildError` and drives a real WS connection through `TestClient`, asserting the client receives the error message, the socket then disconnects cleanly (`WebSocketDisconnect`, not a hang or a raised server exception), and the session_id never lands in `_sessions`. The longer-term suggestion in the issue (validating party-size-vs-spawn-points earlier, e.g. at `POST /sessions` time) is explicitly not attempted here - this fix is the "at minimum" graceful-failure path the issue itself called out as the priority; earlier validation stays open if it recurs for a different encounter. 497 tests passing, `ruff`/`mypy` clean.

**#28 (party rest during live play) done.** `apply_short_rest`/`apply_long_rest` were only ever reachable via the campaign author's own scripted `short_rest`/`long_rest` scene nodes - a party had zero way to recover HP/spell slots/class resources (Rage, Ki, Wild Shape, Arcane Recovery, Bardic Inspiration - this session's own #18-#25 work) between fights the campaign didn't happen to script a rest between. The real blocker wasn't just "no rest verb" - `_advance_campaign_after_victory` auto-chained straight from one encounter's victory into the next scene *synchronously, within the same server-side call that produced the victory*, before the client could ever observe (let alone act on) the intermediate state. Fixed at the root: `_advance_campaign_after_victory` is no longer called automatically - "victory" is now a real, client-visible stop the party explicitly leaves via a new `continue_campaign` WS message, with `rest` available first. `_handle_rest_request` (new) is a party-wide action deliberately kept *outside* the turn_engine/ParsedAction pipeline entirely - it acts on the whole party at once, not one actor's turn, so it doesn't belong anywhere near `resolve_action`. Gated on `session.game_state.status == "victory"` (rejects with a real `error` message otherwise, verified live against the running server, not just the test suite - mid-combat and post-defeat both correctly refused). Mutates `session.party`'s `Character` objects in place, which `build_encounter_state` already reuses by reference (not copies) into `game_state.characters` - confirmed via `encounter.py:149` - so the very next `state_update` reflects recovered HP/slots with zero extra plumbing. Frontend: `LivePlay.tsx` shows Short Rest / Long Rest / Continue buttons whenever `gameState.status === 'victory' && logCaughtUp` (the same log-catch-up gate `isMyTurn` already uses, so the choice isn't offered before the paced log has actually shown the victory - see the sidebar-desync fix from earlier this session). 3 new tests in `test_ws_session_real_start.py` (rest recovers party resources through a real WS round trip, rest rejected mid-encounter with the party left untouched, continue_campaign chains into `kobold_warren_full`'s next scene/encounter through the real message wiring, not just a direct function call) - all pass, plus the two pre-existing test files (`test_ws_session.py`, `test_ws_session_real_start.py`) confirmed unaffected by removing the automatic chain call (neither exercised victory->next-encounter through a live WS round trip; the one such test already called `_advance_campaign_after_victory` directly). Verified live against the real running backend (not TestClient): a fresh real session connects and reaches `awaiting_input` normally, and a `rest` request sent mid-combat is correctly rejected with `"The party can only rest between encounters."` - reaching a genuine live *victory* through real combat wasn't exercised live (would need a full real fight with live LLM calls), left to the offline WS-round-trip tests instead. 500 tests passing, `ruff`/`mypy`/`tsc`/`oxlint` clean.

**#27 (class-resource UI affordance) done.** Rage/Second Wind/Ki/Wild Shape/Bardic Inspiration were only ever visible as a passive stat line in `CharacterDetailSheet.tsx`'s sidebar - real information, but nothing told a player *what to type* to actually use one, or that it was usable *right now*. New `web/src/utils/resourceQuickActions.ts` (`computeResourceQuickActions`) derives a per-resource suggested free-text phrasing, gated by the exact same conditions `turn_engine.py`'s own resolvers check (read directly from `_resolve_rage`/`_resolve_second_wind`/`_resolve_flurry_of_blows`/`_resolve_wild_shape`/`_resolve_bardic_inspiration`, not guessed) - `bonus_action_used`/`is_raging`/`wild_shape_beast_index` - so a suggestion is never shown for something the engine would actually reject this turn. `wild_shape_beast_index` needed adding to `LiveCharacter` first (already on the wire via `Character.model_dump()`, just never declared - same "data already on the wire" pattern as the rest of that interface). Two resources need a real target to suggest correctly: Flurry of Blows picks the first living enemy, Bardic Inspiration the first living ally (not self) - both from `gameState.characters`, so the suggested phrasing is genuinely submittable, not a generic template. `arcane_recovery` is deliberately excluded - it isn't a turn action at all (`apply_short_rest` triggers it automatically, see #25), so there's nothing to suggest typing. `LivePlay.tsx` renders these as a prominent pill-button row above the action-form (`.resource-quick-actions`, new CSS, visually distinct from the sidebar's plain stat list), only when `isMyTurn` - clicking *fills* the input rather than submitting outright, since Flurry/Wild Shape/Bardic Inspiration's suggestion is one reasonable choice the player may want to change (a different beast, a different ally) before committing. Verified live end-to-end against the real running server (not just `tsc`/`oxlint`, both clean): created a real Barbarian, reached their own turn, confirmed the "Rage (2 left)" button rendered, clicking it filled "I fly into a rage" into the input, submitting it went through the real intent-parser/narrator LLM call and actually raged (`Rage: 1 remaining (already raging) - bonus action already used this turn`), and the button correctly disappeared afterward since the gating conditions no longer held. No backend/engine changes - pure frontend presentation on top of already-correct #18-#26 mechanics, as the issue itself scoped it.

**#31 (Character Creator equipment detail) done.** `CharacterCreator.tsx`'s Equipment step rendered each proficient weapon/armor checkbox with just `item.name` - no damage dice/type/properties or AC/Dex-cap/stealth, despite the full `EquipmentSummary` data (issue #15) already being fetched right there via `api.listEquipment()`. `CharacterDetailSheet.tsx` already had this exact formatting logic (`itemDetail`) but it was private to that component. Extracted into `web/src/utils/equipmentDetail.ts` (`equipmentDetail`/`nameWithEquipmentDetail`, taking an `EquipmentSummary` directly rather than an index+lookup-map, since `CharacterCreator` already has the summary objects in hand) - `CharacterDetailSheet.tsx`'s own `itemDetail` now just delegates to the shared function instead of duplicating it. `CharacterCreator.tsx` renders the detail inline next to each checkbox label (e.g. "Dagger (1d4 piercing, finesse, light, thrown, monk)"). Pure frontend, no backend/API changes - verified live by walking a real Wizard through the wizard to the Equipment step and confirming every weapon shows its real SRD stats (Dagger/Quarterstaff/Crossbow-light/Dart/Sling all correct) and the empty-armor-proficiency case still renders its existing "No armor proficiencies for this class" message unchanged. `tsc`/`oxlint` clean, no new warnings.

**#32 (starting-kit visibility in Character Creator) done.** The wizard's persistent preview sheet only ever showed "Extra gear" (the player's *optional* chosen equipment) - a class/background's *fixed* starting kit (a Barbarian's explorer's pack + 4 javelins, an Acolyte's clothes/pouch) was never shown anywhere until the post-creation success screen, after `POST /characters` had already run. Confirmed a real backend gap first, not just a missing render: `ClassDetail` exposed `equipment_options` (the optional proficiency-gated picker) but never `cls["starting_equipment"]` itself, and `BackgroundSummary` had no equipment field at all. Widened both: new shared `StartingEquipmentItem` model (`{index, name, quantity}`) and `_starting_equipment_items()` helper in `src/api/routes/characters.py`, reading the exact same `cls`/`background["starting_equipment"]` shape `character_creation.create_character`'s own inventory-building loop already consumes (`[{equipment: {index, name}, quantity}]`) - exposing existing data, deriving nothing new. `ClassDetail.starting_equipment`/`BackgroundSummary.starting_equipment` added to both `GET /characters/classes/{id}` and `GET /characters/backgrounds`. Frontend: `CharacterPreviewSheet.tsx` combines `classDetail.starting_equipment` + the selected `background.starting_equipment` (same class-then-background order `create_character` itself uses) into a new "Starting kit" section, shown before "Extra gear" - reused #31's `nameWithEquipmentDetail` for weapon/armor items (falls back to the plain name for general gear like Explorer's Pack/Pouch, which aren't in the weapon/armor-only `EquipmentSummary` list, same documented simplification as #15). `backgroundName: string | null` prop replaced with the full `background: BackgroundSummary | undefined` object, since the name alone was no longer enough. 2 new backend tests (`test_get_class_detail_exposes_starting_equipment`, extended `test_list_backgrounds_returns_only_acolyte`) confirm real Barbarian/Acolyte SRD data round-trips correctly - 501 tests passing, `ruff`/`mypy` clean. Verified live end-to-end (real server restarted to pick up the new fields, not just `tsc`/`oxlint`): selecting Barbarian in a real wizard session showed "Starting kit: Explorer's Pack, Javelin (1d6 piercing, thrown, monk) x4, Clothes, common, Pouch" - class and background kits correctly combined with real weapon detail, before character creation was ever submitted.

**#30 (known-spell list for "Spells Known" casters) done - scoped to Bard/Sorcerer, per the issue's own explicit narrowing.** This engine had no concept of "which spells a character knows" beyond cantrips - `cast_spell` matched the entire vendored SRD spell list regardless of class/level. Confirmed via `srd.classes[x]["spellcasting"]["info"]` that Bard/Sorcerer ("Spells Known" - a fixed list chosen at creation, growing on level-up) and Cleric/Druid/Wizard/Paladin ("Prepared" - the whole class list, re-selected each long rest, Wizard adding a spellbook layer) are genuinely different mechanics; this pass covers only the former, the latter explicitly deferred to a second phase. New `SPELLS_KNOWN_BY_LEVEL` table (`character_creation.py`, same hardcoded-SRD-fact precedent as `SPELL_SLOTS_BY_LEVEL`): bard 4/5/6/7/8, sorcerer 2/3/4/5/6 across levels 1-5. `create_character` gains `chosen_spells`, required/validated for bard/sorcerer (mirrors `chosen_racial_skills`'s exact half-elf-only shape) via new `_validate_spell_choices`; `level_up` gains `spells_learned`, applied only at a level where the table's count actually increases and the caller supplied one (mirrors `ability_score_increase`'s "gated by level, caller's choice, not invented here" pattern exactly). `Character.known_spells: list[str]` persisted end-to-end from the start this time (DB column + migration `0cab8bc682f5` with `server_default="[]"`, `_character_to_record`/`_record_to_character` widened) - not found live after the fact like the `class_resources` gap earlier this session. `turn_engine._resolve_cast_spell` now rejects a level-1+ spell (cantrips stay unrestricted, out of scope) a Bard/Sorcerer doesn't have in `known_spells`, right after the existing "unknown spell" check. New `rules.class_spell_indices`/`rules.spell_damage_notation` shared helpers (the latter extracted from `_spell_attack_params`'s own inline logic, now reused so a spell's *displayed* damage can never drift from what actually resolves) back both the creation-time validation and the new enriched `SpellSummary` API response (level/casting_time/range/components/material plus whichever of damage/heal/DC the spell actually has - independently checked, not tied to `spell_mechanic`'s single bucket, since e.g. Vicious Mockery shows damage *and* a save together). Found and fixed a real data-shape gap while building this: `sleep`/`prismatic-spray` have a SRD `damage` block with dice but no `damage_type` at all (Sleep's is hit-points-of-creatures-affected, not real damage) - `_spell_summary` now treats that key as optional rather than crashing. `ClassDetail` gains `spells_known`/`known_spells_pool` (this class's real level-1 spell list with full detail) - `CharacterCreator.tsx`'s Class & Skills step shows a spell-choice fieldset (mirrors the skill-choice checkbox UI exactly) only for bard/sorcerer, gated on the right count before "Next" enables; `CharacterPreviewSheet.tsx` shows the resolved picks; `CharacterDetailSheet.tsx` shows a new "Known spells" section and the *existing* cantrip list now shows the same enriched detail too (closing the issue-comment ask about cantrip casting-time never being shown). New `web/src/utils/spellDetail.ts` (`spellDetail`/`nameWithSpellDetail`) mirrors #31's `equipmentDetail.ts` shape. Companions: `CompanionSpec.chosen_spells` threaded through `build_companion`; Pip Larkspur (the one companion Bard - Sister Mira, the other caster, is a Cleric, out of scope) authored with `[healing-word, thunderwave, sleep, charm-person]`. Deliberately deferred, matching the issue's own scope cut: prepared casters + Wizard's spellbook layer (a real second phase); injecting the known-spell list into `intent_parser.md`/`player_agent.md` (these templates are also used verbatim by the Day-26/27 LoRA fine-tuning data generator, a materially bigger blast radius than this issue needs - not required for correctness either, since an unknown cast already surfaces as an ordinary `TurnEngineError`); validating a level-up-learned spell's level against currently-available slots (the runtime slot check already makes an over-ambitious pick harmless, just not castable yet). 18 new tests (table sanity, creation-time validation including the wrong-class-rejects-chosen_spells case, level_up growth/rejection cases, the turn_engine restriction with a cantrip-stays-unaffected case, API enriched-detail exposure, a real create-then-refetch persistence round trip, Pip's companion spec) - 519 tests passing, `ruff`/`mypy`/`tsc`/`oxlint` clean. Live-verified extensively against the real running server: walked a real Sorcerer through the wizard, confirmed the spell-choice fieldset rendered all 17 real level-1 sorcerer spells with correct detail (e.g. "Magic Missile (1st, 1 action, 120 ft: 3d4 + 3 Force)", "Sleep (1st, 1 action, 90 ft: 5d8)" - no damage type, matching the defensive fix above), the count-gate correctly blocked/unblocked "Next", the preview sheet showed the resolved picks, and the live character sheet showed the same detail in a real combat session (Zylara, Round 1). The one thing *not* live-verified was an actual WS round-trip casting a spell through the real LLM intent-parser - blocked by this environment's Ollama being intermittently unresponsive/slow during this session (confirmed via a direct `httpx.ReadTimeout` in the server log, unrelated to this change - `_resolve_cast_spell`'s new check is pure deterministic Python already covered by the turn_engine tests using the identical code path a real cast reaches).

**Live-found gameplay bug fixed: moving no longer ends the turn.** User report: moving up to a monster ended their whole turn with no chance to attack, and the same happened to monsters - a wolf/goblin that moved adjacent took a second real turn to actually attack. Root cause confirmed directly in `turn_engine.resolve_action`'s dispatch: `ends_turn = True` is the default before the verb dispatch, and the `move`/`dash` branch never overrode it - every move consumed the character's *entire* turn, not just their movement, collapsing real SRD 5e's two separate per-turn resources (a movement budget up to speed, and a separate action) into one. `monster_ai.choose_monster_action` needed no logic changes to benefit: it already decides `move` (out of range) or `attack` (in range) fresh on each call, and the live autoplay loop (`_autoplay_non_human_turns`) already re-invokes it for the *same* actor whenever `current_turn` hasn't advanced - exactly what happens once move stops ending the turn. Same story for AI companions via `player_agent_node`. Scoped deliberately narrow: only `move` changes to `ends_turn=False`; `dash` keeps `ends_turn=True` (real SRD Dash *is* an action - it trades your action for extra movement, so a dashing character still can't also attack that turn); every other verb (attack, cast_spell, grapple, etc.) is unchanged, so attack-then-move (also real SRD) stays out of scope, matching the specific reported gap. New `Character.movement_used_feet: int = 0` (combat-turn-scoped, like `bonus_action_used`/`equip_used_this_turn` - correctly excluded from DB persistence per this session's own #29 writeup) tracks the budget across possibly-multiple `move` calls in one real turn, resetting in `_advance_turn_skipping_dead` on the same "only when the turn actually advances to this character" schedule those two fields already use. `_resolve_move`'s affordability check now nets out `movement_used_feet` from the turn's total budget instead of granting a fresh one on every call. `choose_monster_action` also nets out `movement_used_feet` before computing its approach path, avoiding a wasted invalid-move attempt on a second within-turn call. One real edge case handled: if an opportunity attack triggered by the move itself kills or downs the actor, the turn still has to advance past them (`ends_turn = actor.is_dead or actor.hp <= 0` for the move branch specifically) - they can't keep acting either way, matching the old unconditional-ends_turn behavior for exactly that case. Required updating `cli/play.py`'s `SCRIPTED_ACTIONS` (Thorin's Round-1 move used to implicitly end his turn before Elrond's scripted attack) with an explicit `end_turn` right after it - `end_turn` emits no event and consumes no RNG, so the hand-computed fixtures in `test_turn_engine_scripted.py` needed no other changes. Two pre-existing tests updated to match the new reality rather than the bug: `test_reaction_used_this_round_caps_a_reactor_at_one_opportunity_attack` (needed an explicit `end_turn` between two different movers' moves, which is the test's actual point) and `test_non_human_turns_auto_resolve_before_awaiting_input` (loosened from exact-equality to `>=`, since a monster that now moves-then-attacks contributes two narrations instead of one). 5 new tests in a new `tests/unit/test_turn_engine_move_and_act.py` (move-then-attack in one turn, two partial moves correctly sharing one speed budget, an over-budget second move rejected with a clear error, `movement_used_feet` resetting on the actor's own next turn, `dash` still ending the turn) plus a direct `test_monster_ai.py` regression test resolving a monster's move for real and confirming a second `choose_monster_action` call for the same actor returns `attack` - the literal "wolf moves but doesn't attack" scenario. 524 tests passing, `ruff`/`mypy` clean. Verified live against the real running server via `debug_action` (bypassing the LLM, which was intermittently timing out in this environment today) on a real goblin-ambush session: my own move correctly kept my turn active (position/`movement_used_feet` updated, re-prompted as me, not passed to a monster), and the session's own real event log showed goblin_1 and goblin_3 each resolving `move` -> `attack_roll` -> `damage_dealt` within a single `turn_index` in Round 1 - the exact reported bug, confirmed fixed end-to-end.

**Live-found bug fixed: casting any spell at a named target failed with "cast_spell action requires a target."** User report: even a straightforward "I cast acid splash at goblin_2" was rejected. Root cause confirmed directly in `src/llm/prompts/intent_parser.md`: every OTHER targeted verb ("attack", "help", "grapple", "bardic_inspiration", etc.) explicitly told the model "Set 'target' to...", but the `cast_spell` line only ever said "Set 'item_or_spell' to the spell name if given" - never mentioned `target` at all, confirmed also missing from `ParsedAction`'s own per-verb docstring in `src/engine/actions.py`. Fixed the prompt to explicitly instruct setting the top-level "target" field for cast_spell. Diagnosed and iterated by calling the real teacher LLM directly (not guessing): the first fix (`target` instruction added) revealed a *second*, more interesting problem - the model reliably understood there *was* a target, but nested it as `params["target"]` instead of the top-level field, confirmed via 3 repeated direct calls all showing the identical shape. Tried strengthening the instruction (explicitly forbidding nesting in params, plus adding multi-target guidance for `targets`) - the anti-nesting wording helped, but the added multi-target guidance actively *regressed* the single-target case in the same test batch, and live WS testing showed real, inconsistent variance (3 live failures in a row despite an 8/8 direct-call success streak moments earlier with what looked like an identical setup) - concluded this specific field is a genuinely fragile spot for this model, and further prompt-wording iteration was hitting diminishing/negative returns rather than a stable fix. Added a deterministic code-level safety net instead: `intent_parser_node`'s new `_normalize_cast_spell_target` (`src/graph/nodes/intent_parser.py`) promotes a stray `params["target"]`/`params["targets"]` to the real top-level field whenever a `cast_spell` action comes back with neither set - the data the model extracts is already correct, just occasionally in the wrong place in the JSON, so this closes the gap regardless of which "shape" the model happens to answer in on a given call. Applied uniformly across all three intent-parser backends (teacher/finetuned/finetuned_ollama). 6 new offline unit tests for the pure normalizer function (promotes a stray target/targets, leaves an already-correct or no-target action alone, doesn't touch other verbs' own params, prefers a real field over a stray leftover) plus a new golden-set case in the existing live-model test file (`tests/llm/test_intent_parser.py`, `pytest.mark.llm`, not run by default). 530 tests passing (21 deselected, incl. the new `.llm`-marked case), `ruff`/`mypy` clean. Verified live end-to-end against the real running server after the fix: the exact repro ("I cast acid splash at goblin_2") that previously failed 3/3 times now resolves correctly, with real narration confirming the spell actually landed on the intended target.

**Live-found bug fixed: a ranged weapon user's own free-text turn ("I nock an arrow and fire") was rejected as an equip mismatch.** User-observed live: `companion_silvana` (a Ranger with a Longbow equipped) hit `'arrow' isn't in companion_silvana's equipped weapon set (currently: Longbow) - use 'equip' to switch weapons first` before the battle had even produced a single attack. Root cause confirmed in `turn_engine._pc_attack_params`: `item_or_spell="arrow"` (the AI-generated free text's most concrete noun - the ammunition, not the bow) failed both the exact-index match and the existing fuzzy name-match against `equipped_weapons` (`_match_weapon_by_name`'s word-subset check correctly refuses to treat "arrow" as any kind of match for "Longbow" - they share no words), so it fell into the same strict rejection path designed for a genuinely different, unequipped weapon (e.g. naming a longsword while only a dagger is equipped) - a real, deliberate check, just never designed to tell "named different weapon" apart from "named the ammunition/a flavor word instead of the weapon." Fixed narrowly: before rejecting, checks whether `weapon_index` resolves to a real SRD entry that's genuinely a weapon (`weapon_category` present) - only then does it raise the existing clear error; a real SRD entry that exists but isn't a weapon (confirmed "arrow" is real SRD data, `equipment_category: Adventuring Gear`, `gear_category: Ammunition` - not a weapon at all) instead falls back to whatever's already equipped, same as naming no weapon. Deliberately narrow: complete gibberish that matches no real SRD item either way (e.g. the existing `test_pc_attack_params_rejects_a_name_matching_no_real_weapon`'s "my fireproof toaster") still raises - that's a genuinely confused declaration, not a same-hand-different-noun case like ammunition, and the existing test for it needed no changes. 2 new tests in `test_turn_engine_equip.py`: the exact live repro (a Ranger-shaped fighter with a Longbow, `item_or_spell="arrow"`, resolves against the Longbow) and an explicit contrast case confirming gibberish still rejects. 532 tests passing, `ruff`/`mypy` clean. Live verification was attempted via `debug_action` against a real `wolf_den_short_arc` session but got derailed by the encounter's own combat RNG (the test companion went unconscious from wolf damage before her turn came back around) rather than reaching the fix itself - not chased further, since this is pure deterministic Python (unlike the cast_spell/intent-parser fix earlier, there's no LLM-variance concern here) and the exact scenario is already proven via the identical `resolve_action` code path in the passing offline test.

**Live-found UI bug fixed: same-species enemy tokens (three wolves) were visually indistinguishable on the combat grid.** User report: impossible to tell Wolf 1 from Wolf 2 from Wolf 3 on the minimap. Root cause confirmed in `web/src/components/CombatGrid.tsx`: `TokenFace` only rendered the distinguishing initials text (`"W1"`, `"W2"`) in its *fallback* branch, when no portrait image was available or the `<image>` failed to load - once a portrait successfully loads, it fully covers that space with no label at all. Since Wolf 1/2/3 are the same SRD monster type, they all load the identical wolf portrait, making all three tokens visually identical circles with the same image and the same shared `ENEMY_COLOR` (enemies deliberately don't get individual colors, unlike PCs/companions - see `actorColors.ts`). Fixed by restructuring: `TokenFace` now only renders the face (portrait or plain circle), and the caller always draws a small dark label badge (`initials(character.name)`, e.g. "W1"/"W2"/"G") on top afterward, regardless of which face branch rendered - along with moving the turn/self highlight ring to the same always-on-top layer. Verified live: created a real character, started a live Wolf Den encounter, and confirmed via a real screenshot that all three wolf tokens now show distinct "W1"/"W2"/"W3" badges despite sharing the identical portrait image.

**Live-found UX gap fixed: the intent parser couldn't resolve "the closest enemy" or "the enemy to my left."** User report, alongside the token-identity issue above (the same underlying "can't tell which is which" theme). Confirmed the prompt gave the model nothing but raw `(x, y)` coordinates for every visible character and the actor's own position - correctly identifying "closest" or a screen-relative direction from that alone requires real spatial arithmetic the model has no reliable way to do inline. Fixed by moving that computation into Python, where it's trivial and exact: `build_intent_parser_prompt` (`src/graph/nodes/intent_parser.py`) now sorts visible characters by `distance_feet` from the current actor and annotates each line with its distance, an ordinal closeness rank ("closest", "2nd closest", "3rd closest", then "Nth closest"), and an 8-way compass direction relative to the actor (computed from the sign of dx/dy on this project's own x-right/y-down grid convention) - e.g. `"- wolf_1 (Wolf 1, monster): HP 11/11, position (1, 2), 5ft away (closest), east of you"`. The model's job shrinks from "do grid math" to "read the label that already says closest/west/etc." - the same "give the model pre-computed facts instead of asking it to reason" approach that fixed the cast_spell target bug earlier this session. Verified directly against the real model: 6/6 consistent correct resolutions of "the wolf to my left" in a scenario with one wolf actually west and one actually east of the actor (previously untested - confirmed distinct from "closest", which the model already got right without this by chance in an earlier, accidentally-all-one-direction test scenario). 4 new golden-set cases added to `tests/llm/test_intent_parser.py` (closest/nearest/left/right, all passing live). 532 tests passing (25 deselected, incl. new `.llm`-marked cases), `ruff`/`mypy`/`tsc`/`oxlint` clean.

**Live-found layout issue fixed: the combat grid was capped tiny while an often-empty scene-image placeholder dominated the screen.** Follow-up to the token-identity fix above, from the same user conversation - once tokens were labeled, the user pushed back (with a real screenshot) on an initial "hold off" recommendation, correctly pointing out the grid itself was disproportionately small for how information-dense it is during combat. Confirmed directly: `CombatGrid`'s `<svg>` set explicit pixel `width`/`height` attrs derived straight from the battle map (`battleMap.width * CELL_SIZE`, e.g. 320px for an 8-wide map) - `.combat-grid`'s own CSS only had `max-width: 100%` (a ceiling, not a growth mechanism), so the grid could shrink to fit a narrow container but never grow to fill a wide one. Meanwhile `.scene-image-panel` sat right next to it with `flex: 1` and no width cap, so it soaked up every pixel of the row the fixed-size grid didn't use - including when it had nothing to show but placeholder text (`SceneImagePanel`'s `.empty` state), which is the common case since a scene image only appears after that turn's generation completes. Fixed: `CombatGrid.tsx`'s `<svg>` now only sets `viewBox` (the internal coordinate system tokens/clicks are computed against, untouched); actual rendered size is CSS-driven (`width: 100%`, `flex: 3`, `max-width: 640px`) so it scales up to fill its share of the row, capped at a sane ceiling rather than staying pinned to its map-derived pixel size. `.scene-image-panel` now caps at 320px (240px min) when sharing the row with a grid during combat, but widens to 480px via a `:has()`-scoped rule when it's the row's only child (travel/narrative moments with no active encounter) - preserving its current prominence there, matching the user's own "images on the side during battle, current place at travel time" framing. Verified live via direct DOM measurement in a real Wolf Den session at a 1280px viewport: the grid grew from its old fixed 320px to 407px (a real, visible increase - proportional to more, not just a marginal pixel bump), while the image panel's rendered width dropped to 234px, confirmed via a live screenshot showing all three wolf tokens' new identity labels clearly readable at the larger size. Noted as a real, separate follow-up (not implemented this pass, pending user feedback on whether it's still needed): the outer `.live-play` 3-column grid (`grid-template-columns: 1fr 280px 280px`) caps the entire main content area - including the narration log below the scene row, which the user also called out as one of the two most important elements - to roughly 650-825px depending on viewport width regardless of this fix, since both side columns are fixed-width; widening the main column further would need trimming those, a decision better made after seeing whether this pass's rebalance is already enough.

**Live-play layout, round 2 - the side-by-side scene row was the wrong shape entirely, not just wrongly proportioned.** The user pushed further on the fix above: they wanted the grid vertically aligned with the narration log (same column width, not sharing a row with anything), and initially proposed moving the scene image to a new column on the left, sized to visually balance the character-stats column on the right. Built a to-scale mockup artifact (real CSS values - 280px side columns, 1.5rem gaps, 1480px page max-width) comparing that against the current layout before writing any code, since the proposal had a real, non-obvious cost: a 4th fixed column doesn't just take its own width, it also adds a 4th gap, and at a realistic 1280px window the main column (grid + log) would have dropped to ~360px - narrower than the grid was *before either* of the last two rounds of fixes. The user looked at the numbers and rejected it.

Their actual next idea was better, and simpler to build: don't add a column at all - reuse the *same slot* for the grid and the scene image, showing whichever one is relevant to the current game phase (image before a fight starts, grid for its duration, image again on victory/defeat, grid again once a new fight starts). This maps onto state that already exists with zero new plumbing: `GameState.battle_map` is only ever populated for an actual encounter (confirmed via `src/api/ws/session.py` - `session.game_state` is only rebuilt via `build_encounter_state`, with a fresh `battle_map`, when a new encounter's session-setup or `continue_campaign` chain-walk reaches the next fight), and `GameState.status` flips to `"victory"`/`"defeat"` while that same `game_state` object - `battle_map` included - is still the current one, since issue #28 made `_advance_campaign_after_victory` a client-triggered step rather than an automatic one. So `showCombatGrid = battle_map is present AND status === "in_progress"` is already exactly the right condition, sourced from data the frontend already had.

`LivePlay.tsx`'s `.scene-row` now renders `<CombatGrid>` when that condition holds and `<SceneImagePanel>` otherwise - never both. `index.css`'s `.combat-grid` and `.scene-image-panel` both lost their old shared-row sizing (`flex: 3`/`flex: 1`, the mutual width caps, the `:has()`-scoped exception for "no grid sibling") since neither ever shares the row with the other anymore - each just takes the main column's own full width (grid capped at 900px, image at 480px so a still-generating scene doesn't read as an oversized empty box) when it's the one being shown, landing it at the same left edge and width as `.narration-feed` below it for real vertical alignment.

Live-verified end to end against the real running server, not just the mockup: created a throwaway character through the actual wizard (the in-app browser's `computer` click action intermittently no-ops on this app's buttons/checkboxes, the same Day 20/21/Phase-9-era tooling gotcha already documented several times in this file - worked around by dispatching real `MouseEvent('click', {bubbles:true})` via `javascript_tool` for every step), started a real one-shot session, and confirmed via direct DOM measurement that the grid rendered at the narration log's exact width (777px, same left edge, at a 1400px viewport) the moment combat began. Opened a second raw WebSocket to the same session and drove the fight with `debug_action` messages (bypassing the LLM - this is pure deterministic layout logic, no model variance to worry about) through to a real defeat; confirmed the real app's own DOM swapped from `<svg class="combat-grid">` back to the scene-image `<div>` within the same render that showed the "Defeat... the party has fallen." banner, with a real generated scene image rendering at the same full width. Cleaned up the throwaway character/campaign-progress rows from the dev DB afterward. `tsc -b`/`oxlint` clean, no backend changes.

**Live-found follow-up bug in the grid/image swap above: the grid rendered before the pre-combat hook narration had even been shown.** User report after trying it live, on a campaign with a real narrative hook before its first fight (`wolf_den_short_arc`, unlike the goblin one-shot this feature was first verified against, which turns out to have no gap between its own hook and combat, so the previous live-verification pass never actually exercised this path). Root cause was one level below `LivePlay.tsx`'s swap logic (which is correct - `battle_map` present + `status === "in_progress"` really is the right condition) - it's in how `gameState` itself gets updated in `sessionClient.ts`'s reveal-pacing mechanism from the earlier "narration pacing" fix (see its own writeup above): a `state_update` with no narration text and no new events of its own was treated as safe to apply to `gameState` immediately, since "nothing paces it." That reasoning only checks whether *this* message needs pacing, not whether the queue already has earlier entries - the hook narration - still waiting to be revealed.

Confirmed live, not just from reading the code: instrumented the real dev server's own WebSocket connection (wrapped `window.WebSocket` in a `Proxy` before the app's `useSessionSocket` effect ran, logging every raw message with a timestamp) alongside a polling snapshot of the live DOM (`.scene-row`'s child tag, narration entry count), then drove a real character through the actual wizard and into a live session. The connect flow's own final `state_update` (sent once encounter setup finishes, right after `_autoplay_non_human_turns` may have already resolved a monster's turn - wolves went first in every repro run) showed up as a genuine duplicate of the one broadcast moments earlier - identical event count, no accompanying narration - which is exactly the shape that qualified for the "apply immediately" fast path. Since it carries the fresh encounter's `battle_map`/`status: "in_progress"`, applying it immediately flipped the grid on instantly while the 4 queued hook-narration entries were still being paced through one at a time behind it.

Fixed by widening the immediate-apply condition to also check `entryQueueRef.current.length > 0` - if anything is already queued, this update queues behind it too (even with empty text), rather than being judged safe purely on its own content. This closes the gap without weakening the original "apply immediately when truly nothing is pending" fast path (a `rest`/other out-of-combat update with an actually-empty queue behaves exactly as before). Re-ran the identical instrumented live test after the fix: the WS stream still contains the same duplicate `state_update`, but the DOM timeline now shows the scene image correctly held through all 4 hook narration reveals (paced ~4s apart), with the grid only appearing on the encounter-start entry's own turn in the queue - narration and game state back in lockstep. `tsc -b`/`oxlint` clean.

**Issue #38 - a "debug mode" combat-log toggle showing each roll's full modifier breakdown, added proactively (not a bug fix) to make the exact class of gap this session kept finding by hand - a proficiency bonus/fighting-style bonus silently missing or wrongly included - visible by eye during ordinary play instead of needing a debug script every time.** User's own framing, verbatim: "roll x, + modifier y + proficiency bonus z - total a," scoped to "all" d20 roll types and gated behind a debug toggle rather than shown by default.

The real work was plumbing, not design: `AttackParams` (`turn_engine.py`) gained `attack_bonus_breakdown: list[tuple[str, int]]`, populated at all 6 places that build one (PC weapon/monk/plain-unarmed attacks, monster stat-block attacks, PC spell attacks, monster innate-spell attacks) and attached to the `attack_roll`/`spell_cast` event payloads at their one shared chokepoint each (`_resolve_single_attack`, `_cast_attack_spell_at_target` - the same shared-chokepoint structure Phase 9C/9F/9J already established, so this only needed 2 payload-injection sites despite 6 builders). Skill checks and saving throws got the same treatment via two small new helpers (`_ability_check_breakdown`, `_target_saving_throw_breakdown`) covering their own smaller set of real call sites (2 skill-check sites, 2 saving-throw sites with an actual roll - death saves are flat/unmodified per SRD and a forced 2-failure hit-while-unconscious has no roll at all, so neither gets a breakdown, correctly).

**A monster's own attack bonus is one precomputed SRD number with no ability-mod/proficiency split available in the vendored data** (same fact `monster_saving_throw_bonus`'s own docstring already established) - reported as a single `[("attack bonus (stat block)", value)]` entry rather than fabricating a decomposition that doesn't exist, matching `_target_saving_throw_breakdown`'s identical monster-side handling. A **non-proficient** case reports an explicit `("proficiency (none)", 0)` entry rather than just omitting the line - the whole point is telling "correctly withheld" apart from "silently missing" at a glance, which a blank space can't do.

Frontend: `formatEvent.ts` gained a `debugMode` param and a shared `formatBreakdown(natural, breakdown, total)` helper appending `" [14 + STR mod 3 + proficiency 2 = 19]"` to a badge's label only when debug mode is on - reusing the exact same bracket-breakdown shape the initiative badges (issue #14) already established, not a new display convention. `natural` wasn't previously in the `skill_check`/non-death-save `saving_throw` payloads at all (only `roll_total` was) - added it to those 4 remaining real-roll sites so the debug breakdown can show the full `natural + modifiers = total` chain, not just the modifier half. `LivePlay.tsx` gained a `localStorage`-persisted checkbox (`dm-debug-mode`) threaded through `NarrationFeed` into `formatEvent` - a per-viewer convenience setting, not shared session state, matching this project's own established browser-storage-scoping stance.

Hand-computed regression tests extend (not duplicate) existing proficient/non-proficient/monster attack-param tests and the two skill-check tests in `test_turn_engine_new_verbs.py`, asserting the exact breakdown list alongside each test's existing hand-computed total. 532 tests still passing (no new test functions, only new assertions on existing ones), `ruff`/`mypy`/`tsc -b`/`oxlint` clean throughout. Live-verified end to end against the real running server (not just the unit tests): created a real character, started a live Goblin Ambush encounter with debug mode on, and confirmed via the actual rendered DOM that a monster's attack badge read `"Goblin 3 attacks Debugtest... - hit (natural 15) [15 + attack bonus (stat block) 4 = 19]"` - toggling debug mode off instantly stripped the bracketed breakdown from every already-rendered badge (computed at render time from React state, not baked into stored entries, so the toggle is fully retroactive) while leaving the ordinary hit/miss text untouched, and the setting survived in `localStorage` as expected.

**Backlog cleared: issue #37 (Produce Flame's real 30ft hurl range was read as 5ft).** Root cause exactly as filed: the SRD's structured `range` field for this spell is literally `"Self"` (describing where the conjured flame appears), so `spell_range_feet`'s regex/5ft fallback under-ranged the actual attack. Widened `spell_range_feet` to take the whole spell dict (not just the range string) so it can check a new `_SPELL_RANGE_OVERRIDES_FEET` dict (`{"produce-flame": 30}`) first - same override-table precedent as the weapon-reach fix for glaive/whip. Confirmed via the issue's own scope check that `vampiric-touch` (the only other attack-roll spell with `range: "Self"`) doesn't need an entry - its real range genuinely is Touch, so the fallback was already correct for it. All 4 call sites (`turn_engine.py` x3, `monster_ai.py` x1) updated to pass the spell dict through instead of pre-extracting the range string. 533 tests passing (up from 532), `ruff`/`mypy` clean.

**Backlog cleared: issue #36 (Monk's Martial Arts bonus-action strike was a genuinely missing mechanic, not just a UI gap).** The issue's own follow-up comment scoped it precisely: real SRD Martial Arts has a third component beyond the already-correct DEX-option/scaling-die - a free bonus-action unarmed strike after attacking, no Ki cost, available from level 1 - and only Flurry of Blows (Ki-gated, level 2+) existed as a bonus-action follow-up. Added `martial_arts_strike` as a new `ActionVerb`/resolver (`_resolve_martial_arts_strike`), same `bonus_action_used` gate shape as Flurry/Second Wind/Rage, but no `_use_class_resource` call and no level gate - a single unarmed strike via `_pc_attack_params(..., force_unarmed=True)`, reusing Martial Arts' existing damage-die/DEX-option math rather than building new attack logic. Deliberately doesn't enforce SRD's "immediately after the Attack action" prerequisite, matching the same documented simplification this engine already accepts for Flurry's identical real prerequisite (no "which verb fired earlier this turn" tracking exists to check it against). Added to both `intent_parser.md` and `player_agent.md`'s verb legends so free text actually reaches it, distinguishing it from Flurry by "no ki cost, single strike" language in both.

Also closed the issue's original UI-visibility ask: `CharacterDetailSheet.tsx` gets a new Monk-only info paragraph (mirroring Fighting Style's own dedicated line) stating the current Martial Arts die/DEX option and confirming Unarmored Defense is active, plus a "Martial Arts Strike" entry in the Available Actions list gated on `bonus_action_used` the same way Equip already is. `monkMartialArtsDieSides` is a small TS duplicate of `rules.monk_martial_arts_die_sides` (same precedent as `abilityModifier` mirroring `rules.ability_modifier` in the same file).

3 new hand-computed tests (lands at level 1 with no Ki resource present at all, rejected for a non-Monk, rejected if bonus action already used) - 536 tests passing (up from 533), `ruff`/`mypy`/`tsc -b`/`oxlint` clean. Live-verified end to end: created a real level-1 Monk (confirmed AC 15 = 10 + DEX 3 + WIS 2, proving Unarmored Defense is genuinely active, not just claimed), confirmed the sheet's new Martial Arts paragraph and Available Actions entry rendered, then drove `martial_arts_strike` via `debug_action` against a real session - the resulting `attack_roll` event showed `source: "unarmed strike"`, the correct `[["DEX mod", 3], ["proficiency", 2]]` breakdown (via the debug-mode feature from the previous entry), `bonus_action_used` flipped to `true` while `current_turn` stayed on the Monk's own turn (didn't end it), and the sheet's action-list entry correctly flipped to its "already used this turn" unavailable state afterward.

**Backlog cleared: issue #35 (Magic Missile, and any no-roll auto-hit spell, couldn't be cast at all).** `cast_spell` only ever classified a spell into "attack"/"save"/"heal" via `rules.spell_mechanic`; Magic Missile has none of those SRD fields (no `attack_type`, no `dc`, no `heal_at_slot_level` - just `damage`), so every cast attempt hit the explicit "not supported" rejection. The issue's own suggested fix was a 4th mechanic inferred the same way as the other three ("has `damage` but no `attack_type`/`dc`") - checking that against the real vendored data first turned up a real problem with that approach: `scorching-ray`, `call-lightning`, `flaming-sphere`, and a few others share that *exact* field shape without being genuine no-roll spells - they're real attack-roll/save spells whose vendored SRD entry simply omits the `attack_type`/`dc` field (a data gap, not a game rule). Inferring "auto_hit" from field absence would have silently made all of those always-hit instead of correctly staying unsupported.

Fixed with an explicit per-spell allowlist instead (`rules._AUTO_HIT_SPELLS = {"magic-missile"}`), the same pattern already established for `_SPELL_RANGE_OVERRIDES_FEET` (issue #37) - a spell only joins the bucket after being individually confirmed to genuinely have no roll, not because it happens to lack two fields. `magic_missile_dart_count(spell_level) -> spell_level + 2` is a closed-form formula, not a hardcoded table - verified against every one of the SRD's 9 real `damage_at_slot_level` rows (each really does factor as `(level+2)` separate `1d4+1` rolls, not just assumed from the prose "one more dart per slot level above 1st").

The genuinely new design decision was what `action.targets` means for a no-roll spell: unlike attack/save/heal (where each list entry is an independently-resolved *target*), a dart-based spell's list is one entry per *dart* - repeating an id sends multiple darts at the same creature, and a bare `target` with no explicit list sends every available dart at that one target (SRD's own "one creature or several" default). `_resolve_cast_spell` branches its `target_ids` computation specifically for `mechanic == "auto_hit"` before falling into the shared per-target validation/range-check loop every other mechanic already uses - the loop itself needed no changes, since "validate this id, check its range" is correct whether the list represents targets or dart-destinations. New `AutoHitSpellParams`/`_cast_auto_hit_dart_at_target` mirror `_cast_save_spell_at_target`'s shape (its own `spell_cast` event, then `_apply_damage_and_handle_downing` for the actual HP/downing/concentration-break/Relentless-Endurance handling) - called once per dart, so a target hit by 2 darts gets 2 independent events, matching how discrete dart impacts should read in the log.

An existing Day-14 regression test (`test_cast_spell_rejects_still_unsupported_no_roll_spells`) asserted Magic Missile specifically was unsupported - now genuinely wrong, since it's supported. Repointed it at `scorching-ray` instead, which usefully doubles as a direct regression guard on the allowlist decision itself (proving the field-shape-alone inference the issue originally suggested would have been wrong). 8 new tests (2 in `test_rules.py` for the classification/dart-count math, 3 in `test_turn_engine_spells.py` for single-target/split-target/over-budget-rejection) - 541 tests passing (up from 536), `ruff`/`mypy` clean. Also added a short intent-parser prompt note for the multi-target dart-split phrasing, and widened `ParsedAction`'s own docstring to explain the targets-means-darts distinction.

Live-verified end to end against the real running server, not just the unit tests - and this needed a real kill to prove out, so a throwaway Wizard's HP was boosted directly in the dev DB first (the goblins kept winning initiative and downing every low-HP test character created this session before their first real turn). Cast Magic Missile at a single named goblin: 3 separate `spell_cast`/`damage_dealt` event pairs (2, 4, 4 damage), one spell slot consumed total, the goblin correctly died on the third dart. Then cast again with an explicit split (`targets: ["goblin_2", "goblin_2", "goblin_3"]`): confirmed 2 darts landed on the first goblin and 1 on the second, exactly as the repeated-id design intends, second slot consumed. Dev DB cleaned of both throwaway characters afterward.

**Backlog cleared: issue #34 (narration contradicting the death save it was describing, e.g. "passed their death save" over a badge reading "fail") - and it turned out to be a more concrete, deterministic bug than the issue's own "suspected causes" guessed.** The issue speculated the narrator wasn't being given the real roll outcome. Reading the actual `saving_throw`/`death_save` event payload (`turn_engine._resolve_death_save`) confirmed something more specific: the payload only ever carried `natural` and the *cumulative* `successes`/`failures` counts - never a per-roll `success` boolean for the roll that just happened. The narrator (`narrator.py`'s `_event_line` dumps the raw payload dict) had no choice but to infer pass/fail from `natural >= 10` itself, silently, with nothing telling it that's even the rule.

That inference gap turned out to have a second, more directly verifiable symptom: `web/src/utils/formatEvent.ts`'s `saving_throw` badge already reads `p.success === true` for its "success"/"fail" label - since the payload never set `success`, every single death-save badge in the UI (not just occasionally) rendered "fail" regardless of the real outcome. This means the *badge* the issue's own report pointed to as ground truth ("the mechanical event badge... read 'aefweff's death save - fail'") could itself have been the wrong half of the pair, not necessarily the narration - the true roll may well have been a success that the narrator correctly described, with the badge lying instead.

Fixed at the root: both `saving_throw`/`death_save` payload sites (`_resolve_death_save`'s real roll, and `_apply_unconscious_hit_death_save_failures`'s always-a-failure forced case) now include an explicit `"success": bool`. This fixes the frontend badge unconditionally (no frontend code change needed - it already read the field, it just never existed) and gives the narrator the actual fact directly instead of a number to do arithmetic on - the same "give the model pre-computed facts instead of asking it to reason" fix shape already applied twice earlier this project (cast_spell's target field, intent_parser's spatial reasoning). Also widened `narrator.md`'s own instructions with a general rule (not death-save-specific): an event's own boolean/outcome fields are ground truth and must never be guessed or inferred from the numbers - a robustness improvement that covers every event type with a hit/success/critical flag, not just this one.

2 new/extended hand-computed tests locking in the `success` field for both the real-roll and forced-failure payload shapes. 542 tests passing (up from 541), `ruff`/`mypy` clean. Live-verified against the real teacher model (not just the deterministic payload fix): called `narrator_node` directly with a hand-built failing death-save event and a passing one, 5 trials each - 5/5 correctly narrated as a failure ("health plummets," "fails to stave off"...) and 5/5 correctly narrated as a success ("surge of life returning," "secures one success"...), 10/10 correct outcomes with the field present, closing the loop on both the deterministic badge bug and the narrator's own reliability.

**Backlog cleared: issue #33 (narrator hallucinating an unrelated monster - "the drow's poison courses through..." - mid-fight in a wolves-only encounter).** The issue's own top suspicion was context-window accumulation by round 12+. Reading `providers.chat`/`chat_english_only` ruled that out directly rather than guessing: each call is one stateless `httpx.post` to `/api/chat` with a single user message - no conversation history, no session id, nothing carried between calls. A narrator call at round 12 sees literally nothing from round 1 through 11; if hallucination correlates with how deep into a fight it happens, it isn't because of growing context, since there isn't any.

The prompt already said "do not invent... characters... not listed below," but that's a prohibition with nothing concrete for the model to check itself against - it has to remember the *entire* rule for the whole generation rather than compare against a fact sitting right in front of it. Fixed by giving `narrator_node` a `cast_names` grounding line (`state["game_state"].characters` - already available, just unused before) built the same way `intent_parser`'s own "visible characters" list already anchors that prompt to real data instead of an unenforced rule. `narrator.md` now explicitly says the listed cast is the complete cast, and lists it plainly at the top of the prompt, before the events themselves.

Framed honestly, same as the CJK mitigation (issue #16): this can't *guarantee* a small local model never hallucinates - it's a stronger anchor, not a hard constraint. Live-verified against the real teacher model with a new regression test: 8 trials of a goblins-only encounter, asserting no mention of any unrelated monster species (drow, dragon, orc, demon, vampire, zombie) - all 8 clean. `tests/llm/test_narrator.py`'s pre-existing "game_state is unused beyond events" comment was already stale before this change in spirit (the whole point of this fix is that it *should* be used) - corrected alongside the new test. 543 offline tests passing (up from 542, +1 `.llm`-marked test not run by default), `ruff`/`mypy` clean, all 4 live narrator tests passing against the real model including the new one.

**All 5 backlog issues from this session's live-playtesting/audit history are now closed: #33, #34, #35, #36, #37.**

**Collapsed the Background wizard step - user question, not a bug.** The user asked whether it was redundant. Checked another model's claim that SRD 5.1 has 6 backgrounds (Acolyte, Criminal, Folk Hero, Noble, Sage, Soldier) against both the local vendored data and the live upstream source directly rather than trusting either memory - both agree the real count is 1 (Acolyte). The other 5 are real 5e content, just Player's Handbook, not SRD/OGL - confirms this project's own Day-1 log entry was right all along, a common SRD-vs-PHB mix-up.

Given there's only ever one legal value, `CharacterCreator.tsx` already auto-selected it the moment the background list loaded (`if (b.length === 1) setBackgroundIndex(b[0].index)`) - the "Background" step was a full wizard page just to click "Next" past a single pre-filled dropdown option. The underlying mechanic (Acolyte's skill proficiencies + starting kit, both genuinely applied in `character_creation.py`) wasn't touched - only the pointless step. Removed it from the step list and the render, renumbered Equipment from step 4 to step 3 and fixed its "Back" button accordingly. `CharacterPreviewSheet` already displays the background name/starting-kit contribution (it's rendered for every step, not just a background-specific one), so nothing was lost - if anything, it's now visible from step 0 instead of only after reaching the old step 3. Deliberately not deleted outright: the user wants it back if the project ever adds more backgrounds (more SRD content, or original/homebrew ones - already flagged as a real possibility in this file's Day-1 entry), and collapsing rather than removing keeps that cheap.

`tsc -b`/`oxlint` clean (same 3 pre-existing warnings, none new). Live-verified: walked a real character through the full wizard - preview sheet showed "Human - Acolyte" from step 0 onward, Ability Scores' "Next" went straight to Equipment with no dead page in between, and the finished character correctly had Acolyte's skills (Insight, Religion) and starting kit (clothes-common, pouch) - identical mechanical outcome, one fewer click.

**Issue #40 (make frontend/backend URLs configurable for non-localhost hosting) done - the first step of a new internet-hosted-multiplayer initiative, not a bug fix.** Prompted by the user asking how to host this app for multiplayer over the internet; scoping that out first (`human_character_id` is singular, confirmed via grep - this project has only ever supported one human player) split the work into "multiplayer is a missing feature" (tracked separately, issues #44-#46) versus "hosting is separate plumbing" (this issue, plus #41-#43 for security). The GitHub issue's own suggested approach leaned toward deriving a second frontend origin from `window.location` plus CORS-widening; chose a same-origin-via-proxy design instead - simpler for the actual end goal (one Cloudflare Tunnel hostname, zero CORS friction in the common case) even though it meant implementing the CORS widening too, as a secondary/fallback path (`EXTRA_CORS_ORIGINS` in `src/config.py`) rather than the primary mechanism.

`web/vite.config.ts`'s dev-server `server.proxy` forwards every backend route prefix (`/characters`, `/companions`, `/campaigns`, `/sessions`, `/media`, `/ws`) to `localhost:8000`, so a new shared `web/src/api/baseUrl.ts` can default `API_BASE_URL` to `''` (same-origin, relative requests) instead of a hardcoded absolute URL - the same code path then works unmodified whether accessed via plain `localhost:5173` or through a single tunneled hostname, with the browser never issuing a cross-origin request either way. `WS_BASE_URL` is built explicitly from `window.location` instead (upgrading to `wss://` under `https:`, since a relative WS URL doesn't resolve the way `fetch()` does and a plain `ws://` from an `https:` page is blocked as mixed content). Grepping `localhost:8000` across the whole `web/src` tree (rather than trusting the issue's own list as exhaustive) found two more hardcoded instances beyond what the issue named - `SceneImagePanel.tsx` and `portraits.ts` - both switched to the same shared `baseUrl.ts` constant.

**A real bug, not just a config gap: the WS proxy entry as first written (`{ target: 'ws://localhost:8000', ws: true }`) hung every WebSocket connection indefinitely** - Vite's dev server logged `ws proxy error: Error: write ECONNABORTED` on the backend side, and a direct `new WebSocket(...)` test in the browser never fired open/error/close within a 4s timeout. REST calls through the same proxy config worked fine throughout (confirmed via `read_network_requests`), isolating the bug to the WS path specifically. `WebSearch` against Vite's own documented `server.proxy` examples confirmed the syntax itself matched the docs - so the fix wasn't a syntax correction, it was empirical: changing the target scheme from `ws://` to `http://` and adding `changeOrigin: true` (`{ target: 'http://localhost:8000', ws: true, changeOrigin: true }`) fixed it immediately, confirmed via the same direct WebSocket test now returning "opened". `vite.config.ts` changes aren't hot-reloadable, unlike source file edits - needed a full dev-server restart to take effect.

`allowedHosts: true` in the same config disables Vite's own dev-server host check entirely, since a free Cloudflare quick-tunnel gets a fresh random hostname every restart with no fixed value to allowlist - explicitly not real security on its own, just what's needed for a tunnel hostname to load at all (issues #41/#42 track the actual security gate work).

Live-verified end to end against a clean dev-server restart (not just the pieces in isolation): walked a real character through the full wizard, into Party Setup, campaign selection, and a live WebSocket combat session - initiative rolled for all 4 combatants, the action list and debug-mode breakdown UI rendered correctly, all entirely through the new same-origin/proxy configuration with zero direct `localhost:8000` requests from the browser. 543 tests unaffected (no backend engine logic touched - `src/config.py`'s `EXTRA_CORS_ORIGINS` and `src/api/main.py`'s widened `allow_origins` are the only backend changes, both purely additive), `ruff`/`mypy`/`tsc -b`/`oxlint` clean.

**Issue #41 (close the debug_action WebSocket backdoor before any internet exposure) done - the next step in the same plumbing-before-hosting sequence #40 started.** `debug_action` (this session's own live-verification tool throughout its whole history - injects a fully-formed `ParsedAction` directly, bypassing intent_parser's LLM call and, as the issue itself flagged, any check that the sender controls the named actor) is now gated behind a new `config.ALLOW_DEBUG_ACTIONS` env flag, off by default - matching the issue's own suggested approach rather than trying to infer "looks like local dev" from the request itself, which would be trivial to spoof. When disabled, a `debug_action` message gets a real `{"type": "error", "detail": "debug_action is disabled..."}` response (not a silent no-op, so a dev relying on it locally gets a clear signal to set the flag rather than a mysterious hang) - and, following the same Day-19 lesson already documented for the `TurnEngineError` path in the same function, a `_send_awaiting_input` re-prompt afterward too, so the rejection doesn't leave the frontend's optimistic "it's my turn" flag stuck.

Every existing test in `test_ws_session.py` relies on `debug_action` to drive the engine deterministically without a real LLM call (the module's own docstring says so) - added an autouse `monkeypatch.setattr(config, "ALLOW_DEBUG_ACTIONS", True)` fixture so the whole file keeps working unchanged, plus one new test overriding it back to `False` to prove the rejection path (clear error detail, turn stays with the same actor, matching the existing rejected-action test's own assertion shape). 544 tests passing (up from 543), `ruff`/`mypy` clean.

Live-verified against two real running backend processes, not just the offline `TestClient` tests: with the flag unset (the ordinary `preview_start`-managed dev server), a raw WebSocket `debug_action` against a real demo session was rejected with the exact clear-error text and the turn correctly stayed on the untouched actor; a second, manually-started backend with `ALLOW_DEBUG_ACTIONS=1` set resolved the identical message normally (narration + state_update + next awaiting_input), confirming the flag's opt-in path genuinely works end to end, not just in the unit test's monkeypatched form.

**Issue #42 (shared invite-passphrase gate) done - the third and last of the plumbing/security steps before the actual multiplayer feature work (#44-#46).** A single `SHARED_ACCESS_PASSPHRASE` env var, unset by default (local dev unaffected), matching the issue's own explicitly-scoped "a few friends playing D&D, not a real user/account system" threat model - no per-user accounts, no passwords-per-player.

Backend: a new `@app.middleware("http")` in `src/api/main.py` rejects any request lacking a matching `X-Access-Passphrase` header with a 401, except `/health` (deliberately left open, the ordinary "a health check shouldn't need auth" convention) - registered *before* `app.add_middleware(CORSMiddleware, ...)` so CORS ends up outermost and still attaches `Access-Control-Allow-Origin` to a rejected 401, not just a 200 (confirmed by actually reading Starlette's `build_middleware_stack` source rather than assuming the registration-order convention, since getting this backwards would have reproduced the exact "CORS hides the real error" gotcha already documented elsewhere in this file, just for a 401 instead of an unhandled exception). Since middleware wraps the whole ASGI app, this covers the `/media` StaticFiles mounts too, not just the router. `/health` widened with a `passphrase_required` field so the frontend can decide whether to show the gate at all without guessing from a failed request; a new `GET /auth/check` (gated like everything else) exists purely so the frontend can validate a just-typed candidate passphrase before committing to storing it - reaching the handler at all already proves the header was correct, so the handler itself has nothing to check. The WS endpoint can't carry a custom header from browser JS, so `session_websocket` does the equivalent check itself via a `?key=` query param, rejecting *before* `websocket.accept()` (confirmed via Starlette's own source that `WebSocket.close()` is valid pre-accept - it's just another legal ASGI response to the initial `websocket.connect` event) so the handshake itself fails rather than opening a connection just to immediately close it.

**A real bug caught live, not by inspection: the Vite dev-server proxy (issue #40) never forwarded `/health` or `/auth`, so the frontend's own gate silently failed open the first time it was tested in a real browser.** Vite's SPA fallback answered both with `index.html` (200 OK, wrong content) instead of a proxied backend response - `checkHealth()`'s `response.json()` call then threw a parse error, and the gate's own deliberate "if /health itself is unreachable, don't get stuck behind a gate that can never resolve" fallback treated that parse failure exactly like a genuinely-down backend and defaulted to unlocked. Caught immediately by actually loading the app in the browser with the passphrase set (it went straight to the character creator with a login-shaped error buried in an unrelated screen, instead of showing the gate) rather than trusting the offline `TestClient` tests alone - those tests exercise the FastAPI app directly and have no Vite proxy layer in between, so they couldn't have caught this. Fixed by adding `/health` and `/auth` to `vite.config.ts`'s existing proxy map.

Frontend: `web/src/api/accessKey.ts` (new) wraps `localStorage` read/write, matching the app's existing per-viewer-convenience storage pattern (e.g. the debug-mode toggle) rather than a cookie - this app has no server-side session concept to tie one to. `client.ts`'s single shared `request()` helper attaches the stored key as a header whenever one exists (harmless when the gate is disabled, since the backend simply never checks it); `sessionClient.ts` appends it as a WS query param the same way. New `AccessGate.tsx` screen, shown by `App.tsx` only when `/health` reports `passphrase_required` and no already-stored key still verifies (a stored-but-now-wrong key, e.g. after the operator rotates the passphrase, correctly falls through to re-showing the gate rather than looping forever).

Backend: 5 new tests in `tests/unit/test_access_gate.py` (REST passes through unchanged when unset; REST rejected without/with-wrong-header and accepted with the right one when set; `/health` stays reachable either way; WS rejected without `?key=`, accepted with the correct one) - 548 tests passing (up from 543), `ruff`/`mypy` clean. Live-verified end to end against two real backend processes (one with the env var unset, one manually started with `SHARED_ACCESS_PASSPHRASE=letmein`) through a real dev-server restart: no gate shown when unset; gate shown, wrong passphrase rejected with a clear message, correct passphrase unlocks and persists across a full page reload when set; a raw WebSocket without `?key=` never completes its handshake (browser reports `CLOSED:1006` - the expected shape for a pre-accept rejection, not a clean numbered close code, since the upgrade never finished) while the same connection with the correct key resolves normally. `tsc -b`/`oxlint` clean (same 3 pre-existing warnings, none new).

**Issue #43 (rate-limit/guard LLM- and GPU-triggering endpoints) done - closes out the plumbing/security sequence before the multiplayer feature work (#44-#46).** Reading the actual graph wiring first (`graph_builder.py`'s fixed `player_agent -> intent_parser -> rules_engine -> narrator -> scene_image` edge chain) rather than trusting the issue's own speculative "current state" section - it guesses at endpoints like an on-demand portrait pipeline or an exposed campaign-generation route that don't actually exist yet (portraits are pre-generated by a batch script, campaign generation is CLI-only) - showed the real expensive surface is narrower and more concentrated than the issue's framing suggested: `POST /sessions` itself is cheap (`campaign_runner`'s scene-chain walking is pure/deterministic, confirmed by reading it, no LLM involved), but *every single resolved turn* inside a live WS session - human, companion, or monster, since the graph's edges run unconditionally - triggers a real narrator LLM call and a real scene-image GPU generation, plus a real intent_parser LLM call for genuine free text. The thing actually worth bounding is per-session turn throughput and total concurrent sessions, not blanket-limiting cheap REST reads like `GET /characters/classes` the way the issue's generic "token-bucket per IP" framing implied.

Two guards, both in `src/api/ws/session.py` since that's where the real cost lives: (1) a new `TokenBucket` (`src/api/ws/rate_limit.py`, dependency-free, an injectable `now_fn` instead of real `time.sleep` for deterministic tests - the same "inject the clock/RNG, don't rely on real timing" discipline this project already applies elsewhere) gates the three WS message types that actually reach `session.graph.invoke` (`player_action`/`player_move`/`debug_action` - `rest`/`continue_campaign` never touch the graph, so they're deliberately exempt), one bucket per `Session` shared across every connection to it, since the resource being protected is the session's own shared turn pipeline, not any individual client. (2) a hard cap (`MAX_CONCURRENT_SESSIONS`) on how many *distinct* sessions may have a live connection at once, checked pre-`accept()` at WS connect time (same pre-accept-rejection shape as #42's passphrase check) - a second connection joining an *already-active* session (the multiplayer case) deliberately doesn't count against the cap, only opening a genuinely new one does.

Both defaults (`ACTION_RATE_LIMIT_CAPACITY=10`, `ACTION_RATE_LIMIT_PER_MINUTE=10`, `MAX_CONCURRENT_SESSIONS=3`) are chosen to sit generously above real single-player pacing (this file's own measured ~7s/round) rather than empirically load-tested against real concurrent GPU contention - documented honestly as a starting point, not a measured number, matching the issue's own "worth confirming empirically" framing for a future pass once this is actually deployed multi-session (the Day-25 Ollama-concurrency measurement is the precedent for how that measurement would be done, once there's a real reason to).

8 new tests: `test_rate_limit.py` (4, pure `TokenBucket` cases against a fake clock - capacity-then-reject, refill-after-window, refill-clamped-at-capacity, partial-refill-not-enough) and `test_ws_rate_limiting.py` (4, real WS round trips through `TestClient` - an (N+1)th action rejected within the window, the limit resetting after a real short sleep, a 3rd distinct session rejected when the cap is 2, a 2nd connection to an *already-active* session correctly exempt from the same cap). 556 tests passing (up from 548), `ruff`/`mypy` clean.

Live-verified against two real running backend processes, not just the offline suite: with `MAX_CONCURRENT_SESSIONS=2`, two distinct sessions connected normally and a 3rd was rejected before its handshake completed (`CLOSED:1006`, the same pre-accept-rejection shape as #42's WS check), while a second connection to one of the two already-active sessions succeeded despite the cap already being "full" - confirming the multiplayer-exemption logic works against the real server, not just the monkeypatched test. Separately, with default limits and `ALLOW_DEBUG_ACTIONS=1`, 6 real `debug_action` messages sent ~150ms apart (a faster clip than any real narrator/scene_image-gated turn could actually resolve at) produced zero rate-limit rejections - only the expected "not X's turn" errors from reusing a stale actor after the first one resolved - confirming the default headroom doesn't throttle real play by mistake, the issue's own explicit manual-verification ask.

**Issue #44 (backend support for multiple human players per session) done - the first genuinely new-feature step of the multiplayer initiative, after #40-#43's plumbing/security work.** Implements the lobby design from the issue's own 2026-09-18 clarification: pick a campaign -> an open lobby exists immediately -> other players join live via a shared code -> a personal token identifies each returning player -> the leader starts once ready, filling any unclaimed seats with chosen companions.

The session id doubles as the shareable lobby code - a deliberate scope cut (documented on `CampaignProgress.id`'s own docstring) rather than also building a short-code-generation scheme #45's frontend hasn't asked for yet. Three new endpoints alongside the untouched legacy `POST /sessions` (`src/api/routes/sessions.py`): `POST /sessions/lobby` (creates an "open" `CampaignProgress` row with an empty party); `POST /sessions/{id}/join` (claims a seat for an already-created character, issuing a personal token - or, given an existing token instead, resumes idempotently regardless of lobby status, the real "returning player" case; a repeated join for the same character without a token returns the *same* token rather than minting a duplicate, so a client retrying a lost response doesn't orphan a seat); `POST /sessions/{id}/start` (explicit, like the existing single-shot flow's own player-chosen companion list - not an auto-fill heuristic - fills remaining seats and flips `status` from "open" to "in_progress", after which new seats can no longer be claimed, though existing tokens can always still resume).

**The core backend generalization**: `Session.human_character_ids: dict[str, str]` (token -> character id) replaces the old singular `human_character_id: str | None`. `_build_real_session_setup` no longer assumes `party_character_ids[0]` is the one human and everything else is a companion - it derives the human/companion split from `CampaignProgress.player_tokens` (new JSON column, migration `a7237fd2c628`) when non-empty, falling back to the original index-0 assumption (with a throwaway in-memory token, never exposed) when empty - which is exactly what the legacy single-shot flow's rows look like, since it never calls `/join`. This one fallback rule is what keeps every pre-#44 session (and every existing test) working completely unchanged - confirmed by the full offline suite passing with zero modifications needed to any pre-existing test. `_autoplay_non_human_turns` generalizes the same way (`current_actor_id in human_character_ids.values()` instead of `== human_character_id`), and the WS connect's `controlled_character_ids` derivation adds a `?token=` query param (mirroring #42's `?key=` passphrase pattern) with a matching single-seat exemption: a session with exactly one human seat doesn't require a token at all, so the pre-#44 frontend (which never sends one) keeps working, while a 2+-seat lobby session requires a real, matching token or the connection is rejected (accepted, then immediately closed with a clear error - the token can only be checked once the real session exists, unlike #42's pre-accept passphrase check).

**A real, necessary enforcement gap found while implementing this, not called out explicitly in the issue's own text: nothing previously stopped one connection from submitting an action for whoever's turn it currently is, regardless of which character(s) that connection was actually issued a token for.** `intent_parser` and the `player_move` message builder both derive the acting character purely from "whoever's turn it is" in the shared game state - never from anything the client claims about itself - so narrowing `controlled_character_ids` at connect time was purely cosmetic (only affecting who gets `awaiting_input`) until something actually checked it before resolving an action. Added that check in `_handle_client_message`: `player_action`/`player_move` are rejected with a clear "It is not your turn" error if the sender's connection doesn't control the current actor - checked *before* the #43 rate-limit consumption, so an out-of-turn message costs nothing from the session's shared budget. `debug_action` is deliberately exempt (it's the explicit, already-gated-by-#41 full-override tool, not a real player action).

New tests: 16 in `test_api_sessions.py` (lobby creation, join new/resume/idempotent-retry/unknown-token/unknown-character/rejected-once-started, start fills-companions/rejects-if-started/rejects-empty-party/rejects-unknown-companion) and 3 in `test_ws_lobby_multiplayer.py` (real concurrent `websockets`-client connections, mirroring `test_ws_session.py`'s own live-server pattern combined with `test_ws_session_real_start.py`'s in-memory-DB/stubbed-graph setup: each connection only ever sees `awaiting_input` for its own character; a connection that tries to act for the character it doesn't control is rejected; reconnecting with an existing token resumes the same character after the in-memory session is fully evicted and rebuilt from the DB). 575 tests passing (up from 556), `ruff`/`mypy` clean.

**Two real bugs found and fixed while writing the concurrent-connection tests, not in the feature code itself - both about real-time-order assumptions in a genuinely async test, not the underlying mechanism.** (1) Racing two tasks with `asyncio.wait(..., return_when=FIRST_COMPLETED)` and cancelling the loser isn't enough on its own - `websockets`' own `recv()` raises a `ConcurrencyError` if a second `recv()` call starts on the same connection before an already-cancelled one has genuinely unwound; fixed by `await`ing the cancelled task (via `asyncio.gather(..., return_exceptions=True)`) before touching that socket again. (2) After determining which connection is "wrong" (doesn't control the current actor), immediately sending and reading its *next* message could pick up a stale queued broadcast (e.g. a leftover per-connection state_update) instead of the actual rejection - fixed by draining any already-queued messages on that connection first, so the message read right after sending is genuinely the response to that specific send.

Live-verified end to end against the real running server (not just the offline suite): created two real characters, walked the full `POST /sessions/lobby` -> two `/join` calls -> `POST /sessions/{id}/start` sequence, then opened two real WebSocket connections with their respective tokens - only the player whose turn it was received `awaiting_input`, the other's attempt to move was rejected with "It is not your turn," and restarting the backend process entirely (evicting every in-memory session) followed by reconnecting with the first player's original token correctly rebuilt the session from the DB and resumed them as the same character. Dev DB cleaned of both test characters and the lobby row afterward.

**Issue #45 (frontend party-join flow) done - the UI side of #44's backend, closing out the multiplayer feature pair.** Code-only discovery per the issue's own 2026-09-18 clarification (no in-app "browse open lobbies" screen - smaller surface, the host shares the code out-of-band) and character creation happening *after* joining, not baked into an upfront party-setup step by one leader.

**A real gap found while implementing, not called out in the issue's own text: nothing let a joined-but-not-yet-started player learn that the game had actually started.** #44 shipped only write endpoints (`lobby`/`join`/`start`) - no way to read a lobby's current status, and the WS endpoint itself doesn't gate on `status` at all (a connection at any point just builds the encounter from whatever's currently in `party_character_ids`, which is exactly why `/start` exists as a separate step - connecting too early would start the game with unfilled companion seats). Without a way to poll, a joined player's client would have no way to distinguish "still waiting" from "started, go connect" except a manual, out-of-band cue - not a fully-working feature. Added `GET /sessions/{id}` (`src/api/routes/sessions.py`, read-only, no side effects) returning `{session_id, campaign_id, status, party_character_ids}`; the new `Lobby.tsx` screen polls it every 2.5s and auto-transitions to live play the instant `status` flips to `in_progress` - whether that happened via this browser's own Start click or another player's.

**The other real design question, resolved by reading the backend rather than assuming: there's no "leader" role anywhere in #44's model** - `POST /sessions/{id}/start` has no caller-identity check, it's just "the party's ready, start now, whoever's asking." `Lobby.tsx` matches that honestly: every player who lands there sees the same "Start the Adventure" option (which reuses the existing `PartySetup.tsx` companion-picker component completely unmodified, embedded as a sub-view rather than a separate top-level screen), not just whoever happened to create the lobby first. A `409` from `/start` (lost the race to another player's own Start click, or this browser's own poll) is treated as success, not an error - the game is starting either way.

Frontend flow, replacing the old character-first ordering entirely (campaign selection now comes first, matching the real "let's play X" framing established in this session's own earlier design conversation): `Home.tsx` (host vs. join) -> `CampaignSelect.tsx` (unchanged component, but `onStart` now calls the new `createLobby` instead of the old single-shot `startSession`) -> `CharacterCreator.tsx` (unchanged except one button label) -> auto-join via the new character's id -> `Lobby.tsx` -> `LivePlay.tsx`. The join path is `Home.tsx` -> new `JoinByCode.tsx` -> either straight to `Lobby.tsx`/`LivePlay.tsx` (a stored token for that code already exists - resumed, not re-created) or `CharacterCreator.tsx` scoped to that lobby (no stored token - a genuinely new player). New `web/src/api/lobbyTokens.ts` mirrors `accessKey.ts`'s localStorage pattern but keyed per session id, since one browser can hold tokens for several different lobbies over time, unlike the single global access passphrase. `sessionClient.ts`'s WS URL builder widened from a single `?key=` param to a real `URLSearchParams`, since a multi-human session now needs both the access-gate key *and* the per-player token riding along together.

The legacy single-shot `POST /sessions` REST endpoint is untouched and still tested (issue #44 already established this) - the frontend simply no longer calls it, since the new lobby flow subsumes the solo-play case for free (a "lobby of one," started immediately by its own creator, needs no code-sharing at all in practice even though the mechanism is identical).

2 new backend tests (`GET /sessions/{id}` reflects join/start transitions; rejects an unknown session) - 577 tests passing (up from 575), `ruff`/`mypy` clean. `tsc -b`/`oxlint` clean (one new warning caught and fixed during review - `Lobby.tsx` was updating a ref directly during render to keep a callback current for its polling loop, which oxlint correctly flagged as unsafe; moved into a `useEffect`).

Live-verified end to end in the browser exactly as the issue's own verification section asks, using two real tabs plus a third to prove the reconnect case: hosted a new game (Home -> Create Game -> character creation -> Lobby showing the real shareable code), joined from a second tab with that code (cleared its localStorage first to genuinely simulate a separate device, since two tabs of one browser profile share one origin's storage - a testing-environment artifact, not a product concern, worth naming since it would otherwise produce a misleading result), created a second character, watched "2 players joined so far" appear on the host's own already-open lobby poll, clicked Start from the host's tab, and watched *both* tabs land in the same live combat encounter within seconds - the host's sidebar correctly reading "Issue45Host (you)" and the joiner's reading "Issue45Joiner (you)" in the same shared game state. Then closed the joiner's tab, opened a brand-new one, supplied its real personal token (looked up directly from the dev DB's `player_tokens` column, working around the same shared-localStorage artifact), re-entered the same code, and confirmed it landed directly back in the live game as the identical character - mid-combat HP and all - with no character-creation screen shown at all. Dev DB cleaned of both test characters and the lobby row afterward.

**Issue #46 (reconnection handling for a dropped player's WebSocket) done - the last piece of the multiplayer initiative tracked under #39.** #44's token-based connect already handled the *mechanical* half of reconnecting (present the same token, land back on the same `controlled_character_ids`) - confirmed by this issue's own live verification reusing that exact path with zero backend changes needed for the "same character comes back" case. What #46 actually added was everything #44 never touched: a dropped connection not silently poisoning the rest of the session, a stale connection not lingering forever, and the party actually being told when someone drops or comes back - "wait indefinitely, no timeout" (the issue's own recommended default for a friends game where nobody wants to get auto-skipped over a wifi blip) needed no code at all, since that's already the unconditional current behavior; it's called out here as a deliberate non-goal, not a silent gap.

**A real, concrete robustness bug read directly out of Starlette's own source, not assumed:** `WebSocket.send()` catches a dead socket's `OSError` internally and re-raises `WebSocketDisconnect`, or a plain `RuntimeError` if the socket's already fully closed - and neither `_broadcast` nor `_send_awaiting_input` had ever caught either, so a single connection dying without a clean disconnect (a phone sleeping, a wifi blip, anything short of the browser tab closing cleanly) would abort the whole broadcast loop the *next* time anything tried to message it - silently dropping that message for every other connection queued after the dead one too, not just failing to reach the one that's actually gone. Both now catch `(WebSocketDisconnect, RuntimeError)` per-connection and prune the dead entry from `session.connections` (and `connected_human_character_ids`) rather than letting the exception propagate.

Two new `Session` fields track what's needed to tell a genuine reconnect apart from a character's first-ever connect: `connected_human_character_ids` (who's live right now) and `ever_connected_human_character_ids` (who's *ever* connected, never cleared) - a `player_reconnected` broadcast only fires when a character was previously in the second set but not the first, so a brand-new player joining for the first time doesn't get a nonsensical "reconnected" notice. `player_disconnected` fires from the main receive loop's own `finally` block (the primary, prompt detection point - a clean tab close reliably raises `WebSocketDisconnect` there) once no *other* remaining connection still controls that character; the defensive pruning inside `_broadcast`/`_send_awaiting_input` deliberately doesn't also fire a notice, since a send failure there is a late-discovered symptom of something the receive-loop path should already have reported, not a second independent event.

Frontend: `ServerMessage` widened with the two new message types; `useSessionSocket` tracks a `disconnectedActors: string[]`, reset on every fresh connect (a stale banner from before a reconnect shouldn't survive it, since this client starts blind to whatever happened while it was gone); `LivePlay.tsx` renders a small banner ("X's player is reconnecting...") whenever the list is non-empty, resolving id -> display name off the already-available `gameState.characters` map, the same pattern `formatEvent.ts` and others already use elsewhere in this codebase.

New tests in `test_ws_lobby_multiplayer.py`: a mid-session reconnect (no server restart, no in-memory eviction - the ordinary case, distinct from #44's own eviction-focused test) confirms the reconnecting client gets `awaiting_input` correctly if it's genuinely that character's turn; a second test drives two real connections through disconnect-then-reconnect and confirms the *other* connection receives both `player_disconnected` and `player_reconnected` for the right actor. 579 tests passing (up from 577), `ruff`/`mypy` clean; `tsc -b`/`oxlint` clean (same 3 pre-existing warnings, none new).

Live-verified against the real running server with three real tabs: two players reached a live combat encounter (REST-driven lobby/join/start setup, then each tab's own real React `LivePlay` opened via the actual join-by-code UI flow with a pre-seeded token, to see the real rendered banner rather than just a raw WS message), closing one player's tab produced "Live46Elrond's player is reconnecting..." on the other's screen within seconds, and reconnecting with that player's real token (a fresh tab, same shared-localStorage workaround as issue #45's own live verification) both resumed the exact same character mid-combat *and* cleared the banner on the other player's screen. Dev DB cleaned of both test characters and the lobby row afterward.

**This closes out the entire internet-hosted-multiplayer initiative** (issues #40-#46, tracked under #39) - started from a user question about hosting this app for friends over the internet, scoped into plumbing (#40 configurable URLs), security (#41 debug backdoor, #42 passphrase gate, #43 rate limiting), and the multiplayer feature itself (#44 backend, #45 frontend, #46 reconnection) - six sub-issues, each live-verified against a real running server, not just its own offline test suite.

**Live-found, unrelated to the multiplayer initiative: a character literally named "asssssass" (a real player test-naming their character with a repeated-letter nonsense string) got every single action rejected with a confusing near-identical-looking error - "It is asssssass's turn, not assssssass's".** The two names in that message are NOT actually the same string (easy to miss by eye in a wall of s's, confirmed by extracting the raw JSON and diffing character codes directly rather than trusting a visual read) - `expected_actor` was the real 9-character id, `action.actor` was a 10-character near-miss the model had generated with one extra "s".

Root cause, found by reading `intent_parser_node` directly rather than assuming: `chat_structured` asks the model to produce the *entire* `ParsedAction` as structured JSON, including its own `actor` field - the prompt gives `actor_id` as context text, but nothing makes the model's JSON output literally copy that string byte-for-byte, and for a repeated-letter nonsense string (exactly the shape LLM tokenization reproduces worst - a real word/name almost never triggers this) it occasionally didn't. But the acting character was never actually the model's decision in the first place - it's always exactly whoever's turn it currently is, already known with certainty in Python before the call ever happens. Same "give the model pre-computed facts instead of asking it to reason/reproduce" fix shape already used twice this project for this exact node (cast_spell's target field, the closest-enemy/direction resolution) - new `_force_actor(action, expected_actor_id)` overrides the model's own `actor` field unconditionally after every successful parse, across all three backends (teacher/finetuned/finetuned_ollama). `_invalid_action`'s own fallback path already built its `ParsedAction` directly from Python state and needed no change.

Reproduced live first (a raw WS connection to the player's actual real session, using their real token, sending a genuine free-text action) before writing a single line of fix - confirmed the exact 9-vs-10-character mismatch, then fixed it, then reproduced the identical live call again to confirm the fix holds. 2 new offline unit tests (`test_intent_parser_force_actor.py`, the pure override behavior) plus a new live golden-set case in `tests/llm/test_intent_parser.py` using the actual repro string directly as a character id - `test_intent_parser_preserves_actor_id` (already existed) never caught this because an ordinary name is trivial for the model to echo exactly, so the new test is the first thing in this suite that actually exercises the vulnerable case. 581 offline tests passing (up from 579), all 14 live intent-parser tests passing (up from 13), `ruff`/`mypy` clean.

Also fixed while investigating the same live report: `data/campaigns/encounters/wolf_den.yaml` (and, found by auditing every other encounter file for the same gap, `bandit_hideout.yaml`/`kobold_ambush.yaml`/`goblin_ambush.yaml` too) only defined 2-3 party spawn points, but a party can have up to 5 members (1 PC + up to 4 companions) - all four bumped to 5 real spawn points each, verified by actually building each encounter's `GameState` with a synthetic 5-person party and confirming none raise. One hand-computed test fixture (`test_load_encounter_parses_battle_map_and_monsters`) updated to match the new real spawn-point count.

**Issue #47 (intent parser only resolves one action per turn-submission) done - a real player asked for this live, not a pre-planned feature.** A player typed "I fly into rage, come to wolf 1 and smash it with my warhammer" and only Rage resolved - structural, not a model-quality problem: `chat_structured` grammar-constrains `intent_parser_node`'s output to exactly one `ParsedAction`, and `rules_engine_node`/`_handle_client_message` only ever resolved one per WS message. The default backend is already the "big" model (`qwen2.5:7b-instruct`, `INTENT_PARSER_BACKEND=teacher`) - swapping models wouldn't have fixed this, since the schema itself has no way to express more than one action regardless of model capability.

Same "give the model structure, let Python own the legality logic" split already used repeatedly in this project: new `ParsedActionSequence` (`src/engine/actions.py`, `{"actions": [...]}`) and `parse_intent_sequence` (`graph/nodes/intent_parser.py`) ask the model to extract one entry per distinct action mentioned, in order - pure extraction, explicitly told not to judge legality/sequencing itself (`intent_parser.md`: "do not try to judge whether a later entry is still legal... that's decided after parsing, not your job here"). `intent_parser_node`'s own single-action contract is untouched - still what companion turns (`player_agent_node`), autoplay, and every existing test go through; `parse_intent_sequence` is a new, additional function called directly by `_handle_client_message` for real human free text only. Only the "teacher" backend understands the list format; `finetuned`/`finetuned_ollama` (off by default, never trained/prompted on it) fall back to the existing single-action call wrapped in a one-element list - not a regression, since batching was never something they could do anyway.

`_handle_client_message`'s single `graph.invoke()` call became a loop over `actions_to_resolve` (always a list now - `player_move`/`debug_action` just wrap their one action, unchanged behavior): each sub-action resolves through the graph exactly like a `debug_action` would (parsed_action pre-supplied, `intent_parser_node`'s LLM call skipped), broadcasting its own narration/state_update, and the loop stops the moment `resolve_action`'s own per-verb `ends_turn` signal has actually ended the actor's turn (checked by comparing `game_state`'s current actor against who it was when the batch started) or a sub-action raises `TurnEngineError`/`NotImplementedError` - whatever already resolved stands, the rest of that one utterance simply isn't attempted. The #43 rate-limit charge moved inside the loop too, charged per sub-action actually resolved rather than once per WS message - a multi-action utterance genuinely triggers that many narrator/scene_image calls, so one long sentence isn't a way to dodge the per-turn budget.

3 new offline tests (`test_ws_multi_action.py`, monkeypatching `parse_intent_sequence` with a canned list to test the *sequencing loop* deterministically, independent of the model's own extraction quality): all actions in a sequence resolve when none end the turn; the sequence stops the instant the turn actually ends, leaving a trailing action untouched; a failing middle sub-action stops the sequence but keeps the earlier progress. One of these hung on the first attempt for a real reason worth remembering: the hand-built fixture had two PCs and zero monsters, so `_check_victory_defeat`'s "no monsters alive" check (vacuously true with none present) flipped `status` to `"victory"` after the very first sub-action, silently stopping `_send_awaiting_input` from ever firing again - fixed by giving the fixture a real monster, not a second PC. New live golden-set case (`tests/llm/test_intent_parser.py`) proves the real model actually extracts `[rage, ..., attack]` in order for a rage+move+attack sentence against the real teacher model - 4/4 passes across repeated runs.

**Live-verified against the real running server, and this is where an honest, separate finding turned up.** Restarted the backend to pick up the fix (this project's mid-fight in-memory-state tradeoff, same as every other live-restart this session), reproduced the user's own live session directly with their real token, and confirmed Rage now resolves as the first of a real multi-action sequence rather than being the only thing that happens. But the *rest* of their exact sentence ("come to wolf 1 and smash it") still doesn't fully complete end-to-end - not a bug in this fix, a **separate, pre-existing limitation**: `intent_parser.md`'s "move"/"dash" instructions have always been restricted to "only handle single-step moves onto an immediately adjacent square... if the player describes a move further than one square away, use invalid instead" - a deliberate Day-something simplification (asking the model to compute a full obstacle-avoiding path was judged unreliable), confirmed live twice: one run returned `attack` with no target set (the model dropping context across sub-actions), another returned a malformed `move` (`params.path` effectively empty, "cannot afford this move (cost=None...)"). The sequencing mechanism itself handled both correctly - resolved what it could, reported the real error, stopped cleanly, never silently dropped or corrupted state - so issue #47's own scope is genuinely done; a real player wanting "move to X" over more than one square (not just "attack the closest/left one," which `_direction_label`/distance-sorting already solved) needs its own follow-up, most likely reusing `monster_ai`'s existing greedy `_best_step`/`_approach_path` logic so the model only has to name *which* visible character to approach, the same "give it the target, let Python compute the mechanical detail" split as everything else in this file.

584 offline tests passing (up from 581), 15 live intent-parser tests passing (up from 14), `ruff`/`mypy` clean.

**Issue #48 (free-text movement only supported a single adjacent-square move) done - closes the follow-up #47 itself surfaced.** `monster_ai.py`'s `_approach_path` (a greedy step-by-step path already proven against `turn_engine._resolve_move`'s own affordability/cost accounting for monster movement) promoted to public (`approach_path`, dropped the leading underscore) so `intent_parser.py` could reuse it. Prompt widened: "move"/"dash" can now set the top-level `target` field to a visible character's id ("I charge the wolf") instead of trying to compute exact squares; a new `_resolve_move_target` post-processing step (alongside `_force_actor`/`_normalize_cast_spell_target`, now unified under one shared `_postprocess_action` helper applied everywhere an action is finalized - single-action `intent_parser_node` too, not just #47's sequence, since a companion's own free-text move declaration goes through the identical prompt) computes a real path from the named target's position when set, stopping at 5ft/adjacent using the actor's real remaining speed budget (doubled for `dash`, matching `_resolve_move`'s own budget formula exactly).

**A real bug in this fix's own first draft, caught by actually resolving the result through the engine rather than just inspecting the parsed verbs (which is all the #47 golden-set test had checked, tellingly).** The model doesn't reliably treat `target` and `params.path` as mutually exclusive the way the prompt asks - confirmed live: for the exact repro utterance, one response set *both* `target="wolf_1"` **and** its own bogus single-entry `path` landing two squares away (not actually adjacent). The first draft only computed a fresh path when `params.path` was *absent*, so the already-present (wrong) path from the same response silently reproduced the original bug this issue exists to fix - deferring to "whatever the model already gave" is precisely the failure mode being closed. Fixed by making `target` unconditionally override any `params.path` the model also supplied, including explicitly clearing a stale one when the freshly-computed path comes back empty (already adjacent, blocked, or out of speed) - `target` is a plain, reliable lookup; exact square math is what the model keeps getting wrong, so it never wins the tie-break once a target is named. A move genuinely naming no character at all (a bare destination square) is completely untouched, exactly as before.

6 new offline tests (`test_intent_parser_move_target.py` - a real multi-square path matching `approach_path`'s own output exactly; `dash` doubling the budget; the target-overrides-a-wrong-model-path case above, using the actual live-observed bogus coordinates; a bare destination square with no target left alone; a non-move verb ignored; an unknown target id left for turn_engine's own error) plus a new live end-to-end case (`tests/llm/test_intent_parser.py`) that - unlike #47's own verb-only check - actually resolves every returned action through the real `resolve_action` and asserts Rage applied, the actor's position genuinely changed, and a real `attack_roll` event exists against the wolf: 5/5 passes across repeated runs against the live model. 590 offline tests passing (up from 584), 16 live intent-parser tests passing (up from 15), `ruff`/`mypy` clean.

Live-verified against the real running server with a fresh throwaway Barbarian (a solo character against 3 wolves lost before ever getting a turn on the first attempt - not a bug, just genuinely under-matched odds; retried with a full 4-companion party): sending the *exact* original repro sentence ("I fly in rage, come to wolf 1 and smash it with warhammer") produced real Rage narration, then real movement narration that triggered an actual opportunity attack from the wolf (proving the computed path genuinely traversed real squares, not a no-op), then the attack sub-action correctly reached `turn_engine` with a real target - which is where a **separate, unrelated** quirk turned up and got its own issue (#49): the model sometimes attaches a bogus Paladin Divine Smite param to an ordinary attack when the utterance names a handaxe specifically, confirmed live to reproduce identically through the *original, unmodified* single-action code path (nothing to do with #47/#48) - a follow-up attack with no weapon named resolved cleanly (a real hit-or-miss roll against the wolf), closing the loop on this issue's own actual scope. Dev DB cleaned of both throwaway characters/lobbies afterward.

**Issue #49 (intent parser hallucinating Divine Smite on an ordinary attack) done - the user's own follow-up live report ("smash" also triggers it) reframed the whole bug, not just added a new trigger word.** The original filing had narrowed this to a single weapon name ("handaxe"). Systematically sweeping verb x weapon combinations against the real model (`"I <verb> goblin_1 with my <weapon>"`, 3 trials each) showed the actual shape is broader: forceful verb synonyms ("smash"/"crush"/"pummel"/"smack"/"strike"/"hit" instead of "attack") paired with certain weapons (warhammer, longsword, handaxe, battleaxe - not dagger/mace/shortsword/rapier/quarterstaff) reliably hallucinate `params.smite_slot_level`, while plain "attack" stayed clean across every weapon in the same sweep - a "this reads like a mighty blow" association with the verb choice, not the single weapon name originally suspected.

Fixed with the same "give Python the fact, don't ask the model to reason" pattern used repeatedly in this project: whether the acting character is actually a Paladin is already known with certainty in `game_state.characters` before the call ever happens, so a new `_strip_invalid_smite` (alongside `_force_actor`/`_resolve_move_target` in `_postprocess_action`) unconditionally drops a hallucinated `smite_slot_level` for anyone but a real Paladin - `turn_engine`'s own validation was already correctly rejecting this outright (no bad state was ever at risk), but that meant a real, ordinary attack failed with a confusing rules error for something the player never asked for. A genuine Paladin's own smite declaration is completely untouched. Also tightened `intent_parser.md`'s attack bullet with explicit negative examples (naming the exact forceful verbs found live) - a probabilistic reduction in the model's own hallucination rate, not the actual fix (the deterministic strip is what guarantees correctness).

**Fixing this surfaced a second, real, pre-existing bug while re-running the live suite - not caused by this change, but blocking one of its own tests.** `test_the_full_rage_move_attack_repro_actually_resolves_end_to_end` (issue #48's own live test) failed with `"move/dash action requires params['path']"` on a clean rerun, ~1/5 of the time by direct measurement - traced to the exact same stray-`params.target`-nesting quirk `_normalize_cast_spell_target` already existed to patch for `cast_spell`, just never scoped beyond that one verb: in a multi-action sequence, once the model nests `target` under `params` for one action (here, "move"), it can keep doing so for the rest of the same response (the following "attack" nested it too, in the identical failing trial) - confirmed by a direct repro script unrelated to this session's own change. Generalized the fix instead of re-scoping it narrowly again: renamed to `_promote_stray_target` and dropped the `verb == "cast_spell"` restriction entirely - promoting a stray `params.target`/`params.targets` to the real top-level field is harmless even for a verb that never reads it back out of params, so there's no reason to enumerate which verbs need the safety net. 3/3 clean reruns of the previously-flaky test afterward.

9 new/changed offline tests (`test_intent_parser_smite.py`, new - strips for a non-Paladin, leaves a real Paladin's smite alone, leaves a smite-free attack alone, ignores a non-attack verb; `test_intent_parser_normalize.py` - renamed function, one test's "only cast_spell" assumption flipped to its new verb-agnostic correct behavior) plus a new live golden-set case (`tests/llm/test_intent_parser.py`, the exact "I smash wolf_1 with my warhammer" repro against a Barbarian, asserting no `smite_slot_level` survives). 594 offline tests passing (up from 590), 17 live intent-parser tests passing (up from 16, all green including the now-fixed flaky one, 3/3 reruns), `ruff`/`mypy` clean.

Live-verified against the real running server (restarted to pick up the change - this project's usual mid-fight-progress-reset tradeoff for a live code change): a direct script calling the real `intent_parser_node` for "I attack goblin_1 with my handaxe" / "I smash goblin_1 with my warhammer" / "I smash goblin_1 with my longsword" / "I crush goblin_1 with my battleaxe", 3 trials each, all 12 resolved with `params={}` - confirming the deterministic strip holds even on trials where the model still hallucinated the param upstream, closing #49.

**Character sheet now shows current AC and attack/damage as base + modifiers, not bare totals - a proactive UX ask, not a bug fix.** `character.ac`/an attack's own bonus were always correct under the hood, but a player had no way to see *why* without re-deriving the math by hand - the same gap issue #38's debug-mode roll-log breakdown closed for a resolved roll, never extended to a static "what would this attack roll right now" preview on the sheet itself.

Backend: `rules.armor_ac_breakdown` extracted from `armor_ac` (now just `sum(v for _, v in armor_ac_breakdown(...))`, so the two can never drift) returns the named components (worn-armor base or "base (unarmored)", a DEX mod line - capped and labeled as such when the armor's `max_bonus` actually clamps it, Unarmored Defense's WIS mod for a shieldless Monk, a shield's flat bonus, Defense fighting style's +1). `turn_engine.current_attack_summaries(actor, srd)` is a thin display-only wrapper reusing `_pc_attack_params` directly (same "extract once, reuse for both resolution and display" precedent as `rules.spell_damage_notation`, issue #30) - one entry for the main hand (or unarmed) and, only when genuinely dual-wielding, a second off-hand entry with its real reduced damage bonus already applied (`include_ability_damage_bonus=False`, matching `offhand_attack`'s own resolution). Empty for a monster (nothing to preview - their stat-block bonus is already one precomputed number).

Wired into `_state_update_message` (api/ws/session.py) as a new `combat_summaries` field, computed fresh per broadcast for every `is_pc` character (cheap - at most a handful of party members) rather than cached on `Character`, since equipped gear and Rage can change mid-encounter and a stale display would be worse than none. Empty when `session.srd` isn't set (the demo-encounter/offline-test fallback) - real sessions always have it.

Frontend: `sessionClient.ts`'s `NarrationEntry` gained a `combatSummariesSnapshot` alongside the existing `gameStateSnapshot`, applied at the exact same reveal moment (same lockstep-with-the-paced-log reasoning already established for `gameStateSnapshot` itself, since both fields always arrive in the identical `state_update` message). `CharacterDetailSheet.tsx` renders a new "AC breakdown" line and "Attack & damage" section using a small local `formatBreakdown`/`formatDamage` pair (same `"label value"`-join convention `formatEvent.ts`'s own debug-mode badges already use, just without a natural-roll prefix since this is a no-roll-yet preview) - both no-ops (nothing rendered) when `combatSummary` is undefined, so a session with no srd or before the first `state_update` arrives looks exactly like the sheet did before this feature.

7 new backend tests (`armor_ac_breakdown` - 4 cases confirming it sums to `armor_ac`'s own total across unarmored/capped-DEX/shield+Defense/Monk-Unarmored-Defense; `current_attack_summaries` - matches `_pc_attack_params` exactly for a single weapon, a correctly-reduced off-hand entry when dual-wielding, unarmed fallback, empty for a monster; `_combat_summaries`/`_state_update_message` - includes the right breakdown for a PC, excludes monsters, degrades to `{}` when `srd` is unset). 607 offline tests passing (up from 594), `ruff`/`mypy`/`tsc -b`/`oxlint` clean (the one `oxlint` warning touching a shifted line number in `sessionClient.ts` confirmed pre-existing, not new, by diffing against the pre-change file directly).

Live-verified end to end through the real running app, not just the tests: walked a real Human Fighter (Defense fighting style, Longsword/Chain Mail/Shield) through the actual character-creation wizard into a live Goblin Ambush encounter, and confirmed the rendered sheet read exactly "AC breakdown: Chain Mail base 16 + Shield 2 + Defense fighting style 1" (= 19, matching the character's own `ac`) and "Longsword: +5 to hit [STR mod 3 + proficiency 2], 1d8 + 3 slashing damage" - both hand-verifiable against the character's real STR 16/proficiency 2. Also independently confirmed the raw WS wire payload (a direct `state_update` message) carried the identical structured breakdown before ever touching the UI, isolating whether a mismatch would be a backend or a rendering bug.

**Live-found via the new AC-breakdown feature itself, immediately: Barbarian's own Unarmored Defense (10 + DEX + CON while unarmored) had never been implemented at all - only Monk's version existed.** The user asked directly ("what about unarmored defense for barbarian?") right after seeing the new breakdown display, and checking `armor_ac`/`armor_ac_breakdown` confirmed the gap: `class_index == "monk"` was the only branch, so every Barbarian's AC had been silently under-computed since this engine's AC formula was written - a real correctness bug, not a display gap, exactly the kind of thing this feature exists to make visible. A good demonstration of the feature paying for itself on its very first real use.

Unlike Monk's version, SRD's Barbarian Unarmored Defense has a different formula (CON, not WIS) *and* a different shield rule - "you can use a shield and still gain this benefit," the literal opposite of Monk's "not wielding a shield" gate - so it's checked independently of `equipped_shield`, not gated on it. `armor_ac`/`armor_ac_breakdown` gained a `con_mod: int = 0` parameter (same "defaults to 0, every pre-existing caller unaffected" shape `wis_mod` already established for Monk) with an `elif class_index == "barbarian" and con_mod` branch alongside the existing Monk one. All 3 real call sites updated to pass it: `character_creation.create_character` (already computes `con_mod` locally for HP), `turn_engine._resolve_equip`'s AC recompute, and `api/ws/session.py`'s `_combat_summaries` (the new display path itself). Also widened `level_up`'s existing Monk-only "recompute AC on an ASI that could move it" block (Phase 9I) to `class_index in ("monk", "barbarian")` - a Barbarian's level-4 ASI can raise CON exactly the same way a Monk's can raise WIS, and would have gone stale the identical way without this.

5 new tests (2 `armor_ac`/`armor_ac_breakdown` pure-function cases - CON mod applies unarmored, survives a shield unlike Monk's WIS mod, ignored for a non-Barbarian; a `create_character` case confirming a real Dwarf Barbarian's AC includes the racial CON bonus; a `level_up` ASI case mirroring the existing Monk/WIS test exactly, confirming AC moves when CON does). 612 tests passing (up from 607), `ruff`/`mypy` clean. Live-verified against the real running server: created a real Dwarf Barbarian (CON 13+2 racial = 15, mod +2) via the API, started a session, and confirmed the live WS `combat_summaries` payload read `[["base (unarmored)", 10], ["DEX mod", 2], ["CON mod (Unarmored Defense)", 2]]` summing to AC 14, not the previous (wrong) 12.

**Issue #55 spell audit, first implementation pass - data-gap fixes, Sleep, a new "condition" mechanic, an AC-buff mechanic, and True Strike's banked advantage.** Following up on the audit itself (#55) and a real user report ("Sleep is not supported"), implemented a representative slice of the 153-spell backlog rather than all of it in one pass - explicitly scoped down to what could be built, tested, and live-verified properly, deferring the rest (documented per-spell in the audit issue) rather than rushing untested breadth. Two companion issues were also filed and left unimplemented by design: #53 (Bardic Inspiration needing a genuine mid-resolution pause-and-resume mechanism this engine has never had) and #56 (a full summoning subsystem plan - Conjure Animals/Animate Dead/familiars - phased out as its own multi-part initiative, not attempted here).

**Data-gap fixes** (`rules.py`): `_ATTACK_TYPE_OVERRIDES` (Scorching Ray, Flame Blade) and `_DC_OVERRIDES` (Call Lightning) - both real attack-roll/save spells whose vendored SRD entry just omits the field `spell_mechanic` keys off, confirmed directly against each spell's own `desc` text rather than assumed. Checking each candidate individually mattered: two other spells sharing the exact same field shape - Flaming Sphere (a move-and-ram hazard, not a plain attack or save) and Branding Smite (a banked bonus-damage-on-next-hit buff, the same shape as Divine Smite) - were deliberately left unsupported rather than folded into the "attack" override, since neither is structurally an attack-roll spell despite looking like one at a glance. `spell_dc_info` mirrors the existing `spell_damage_notation` "extract once, reuse for resolution and classification" precedent so `_spell_save_params` never has two sources of truth for a spell's DC shape.

**Sleep** (`_resolve_sleep_spell`): genuinely unlike every other spell mechanic this engine resolves - no attack roll, no saving throw. Rolls 5d8 as a shared "HP pool," applies `unconscious` (10-round duration) to `action.targets` in ascending-current-HP order until the pool runs out, skipping undead (checked against the real monster `type` field, not `monster_is_undead_or_fiend` - that also exempts fiends, which Sleep's own SRD text doesn't) and charm-immune creatures. This engine has no point/AOE targeting concept, so named target ids stand in for "creatures within the spell's area" - a documented adaptation, not literal SRD. New `Character.conditions` source tag (`"sleep"`, distinct from the existing `"0 HP"`/`"hazard"` unconscious sources) lets `_apply_damage_and_handle_downing` wake a sleeper the instant they take any damage, per SRD, without touching the ordinary "downed at 0 HP" unconscious at all - the two never share a code path, only a condition name.

**New "condition" spell mechanic** (`_CONDITION_SPELLS` table, `rules.py`): spells whose entire effect is "apply an existing `ConditionName` to a willing target" - Invisibility/Greater Invisibility are the first, representative entries (both reuse the condition already fully wired since Phase 9A - `condition_attack_advantage`/`disadvantage` already give it real mechanical teeth). More buffs that map cleanly onto an existing condition are now a cheap table addition, not new plumbing. Needed a real, live-caught fix to `_resolve_cast_spell`'s target validation: the existing "only heal skips the friendly-fire check" logic would have let a condition spell be cast on an enemy with no rejection at all (silently beneficial-buffing a hostile) - fixed with an explicit same-side requirement for the "condition" mechanic, the opposite polarity from the attack-target guard.

**AC-buff mechanic** (`_resolve_ac_buff_spell`, new `Character.mage_armor_active`/`temporary_ac_bonus` fields): Mage Armor (13 + DEX while genuinely unarmored, checked before Monk/Barbarian's own Unarmored Defense variants so a caster keeps whichever is better) and Shield of Faith (flat +2, stacks with anything). `rules.armor_ac`/`armor_ac_breakdown` gained two more optional params for these - given the function's param list has grown with every AC-affecting feature this project has added (Monk, Barbarian, now these two), extracted a new `turn_engine._recompute_ac(actor, srd)` helper so every call site passes the *same* full set of live inputs instead of each one needing to remember which params matter, closing off a real drift risk before it could bite.

**And it did bite once, immediately, live**: `api/ws/session.py`'s `_combat_summaries` (the character-sheet AC-breakdown display built earlier this session) builds its OWN `armor_ac_breakdown` call, separate from `_recompute_ac` - adding the two new params to the function signature didn't mean this call site passed them. Live-verified end to end against the real running server: casting Mage Armor correctly moved a real character's actual `.ac` from 13 to 16 (confirmed via the narration text and the state itself), but the sheet's own AC-breakdown line kept showing the stale unarmored formula until this second call site was fixed too. A clean demonstration of why "extract once" only helps if every consumer actually gets touched - caught by testing the *display*, not just the underlying mechanic, exactly the kind of gap this AC-breakdown feature was built to surface in the first place. New regression test locks in both fields now flowing through to the display.

**True Strike** (`_resolve_true_strike`, new `Character.true_strike_advantage`): banked advantage on the caster's own next attack roll, cleared the moment it's used - narrowed from real SRD's "advantage against the specific target you pointed at" to "advantage on whoever the next attack roll is against," the same class of documented simplification Bardic Inspiration's own die already makes, consistent with `has_help_advantage`'s existing one-shot-banked-flag shape rather than inventing a second, narrower kind.

**Two more real gaps found live, both fixed**: (1) "I cast sleep on the goblins" (plural, no explicit target list) reliably set only the singular `target` field, not `targets` - Sleep's resolver now falls back to `[action.target]` the same way every other multi-target spell already does, rather than rejecting a perfectly ordinary phrasing. (2) "I cast mage armor on myself" left `target` unset entirely (the model treats a reflexive "myself" as needing no explicit name) - both AC-buff spells now default to the caster when no target is named, since they're overwhelmingly self-cast in practice.

Spare the Dying (`_resolve_spare_the_dying`) needed no new mechanic at all - mechanically identical to the existing `stabilize` verb's effect (`target.is_stable = True`), just reached via a spell instead of a skill check, with none of stabilize's own Medicine-check roll (real SRD Spare the Dying has no roll whatsoever).

13 new offline tests (`test_turn_engine_spell_audit.py`) covering every mechanic above end to end via real `resolve_action` calls (not just classification), plus 1 more locking in the `_combat_summaries` display fix (`test_ws_combat_summaries.py`). 2 pre-existing tests updated to reflect spells that are now genuinely supported (scorching-ray) rather than still-correctly-rejected regression cases (swapped to flaming-sphere, which remains unsupported on purpose). 626 tests passing (up from 612 before this pass), `ruff`/`mypy` clean. Live-verified against the real running server with a fresh Wizard: Mage Armor's AC math and (after the fix) its display breakdown both confirmed correct end to end through the real intent parser, and Sleep correctly put a real goblin unconscious mid-combat via ordinary free text ("I cast sleep on the goblins").

**Issues #50/#51 - the character sheet's spell lists now show real detail and descriptions, and class resources got hand-authored hints too.** #50: `CharacterPreviewSheet.tsx`'s "Cantrips available" line was a flat comma-joined string of bare names - swapped for a real `<ul>` using `nameWithSpellDetail` (the same helper "Known spells" already used one section down), so it now reads like every other spell list in this project (e.g. "Acid Splash (cantrip, 1 action, 60 ft: 1d6 acid, DEX save)").

#51 went further: even with real mechanical detail, a player who doesn't already know 5e spells by name can't tell what a spell actually *does* from stats alone. The data was already fully in place and reaching the frontend, just never read - `SpellSummary.desc` (the real SRD description text) has been on both the backend model and the TS interface since issue #30's own widening, `spellDetail.ts`'s formatter just never touched it. New `spellHint(spell)` extracts the first sentence (capped at 140 chars for a compact list context - the raw SRD text can run several sentences, fine for a spellbook page, not a one-line list entry) and renders as a smaller, muted second line under each entry's existing mechanical detail (new `.spell-hint` CSS class, same "label" styling `.companion-meta` already established elsewhere) - applied to both `CharacterPreviewSheet.tsx`'s cantrip/known-spell lists and `CharacterDetailSheet.tsx`'s live "Known spells" section. Deliberately left `CharacterDetailSheet.tsx`'s own compact "Available actions - cantrips: X, Y, Z" one-liner untouched - that's a quick action-verb scan, not a spell reference, and a hint per entry there would clutter a context meant to stay terse.

Broadened during a follow-up ask: **class resources** (Second Wind, Rage, Ki, Wild Shape, Arcane Recovery, Bardic Inspiration) needed the same treatment but couldn't reuse the spell-desc mechanism at all - these are this project's own invented bookkeeping keys (`character_creation.CLASS_RESOURCES_AT_LEVEL_1`, plus `ki`/`wild_shape`/`bardic_inspiration` granted via `level_up`), with no SRD `desc` field anywhere to surface. New `RESOURCE_HINTS: Record<string, string>` in `CharacterDetailSheet.tsx` - 6 short, hand-authored one-liners (e.g. "Bonus action: heal 1d10 + your level, once per short or long rest.") - rendered the same `.spell-hint` way under each "Class resources" entry.

Pure frontend, no backend changes for either issue - `SpellSummary.desc` was already on the wire. `tsc -b`/`oxlint` clean (same 3 pre-existing warnings, none new). Live-verified against the real running server: walked a fresh Wizard through the actual character-creation wizard, confirmed all 14 real cantrips rendered as a real list with both mechanical detail and a genuine plain-English description for every single entry (e.g. "Fire Bolt (cantrip, 1 action, 120 ft: 1d10 Fire) / You hurl a mote of fire at a creature or object within range.").

**Issue #52 - narration log reveal delay bumped 3s -> 8s.** Live feedback: 3 seconds wasn't enough time to actually read an entry before the next one revealed, especially during a chained multi-action turn or an auto-played companion/monster sequence. `sessionClient.ts`'s `ENTRY_REVEAL_DELAY_MS` is the one constant the whole pacing mechanism (`scheduleDrain`/`lastRevealTimeRef`) already reads off, so this was a one-line change - no other logic needed touching.

**Issue #53 - Bardic Inspiration's recipient now chooses whether to spend their banked die, after seeing the roll but before learning the outcome, via a genuine two-phase WS exchange.** The real architectural gap this closes was explicit and pre-flagged: this engine has never had a way to pause an action mid-resolution, ask the player something, and resume with their answer - every other mechanic resolves fully synchronously in one call. Divine Smite's own docstring already explains why it has to be declared *before* the attack roll for exactly this reason, and general reactions (Shield, Counterspell) were punted on in Phase 9H for the same one. Real SRD lets the Bardic Inspiration holder wait until after the d20 lands, so building this properly meant building the pause-and-resume mechanism for real, not another workaround.

**Design, scoped deliberately narrow (documented, not silently dropped):** only a PC's single, non-Extra-Attack `attack` roll offers the choice - Extra Attack, Multiattack, and spell attacks still auto-apply the die immediately the instant it would help, exactly like every attack roll behaved before this issue. `turn_engine._resolve_attack` only sets a new `defer_bardic_choice=True` flag when `num_attacks == 1`; every other caller of `_resolve_single_attack` keeps passing the old default (`False`), so nothing about their behavior changed.

**Mechanism**: `_resolve_single_attack`, when deferring, rolls a real probe *without* the die (a genuine RNG draw, not a discardable peek) to see the actual natural/total. A hit, or a natural 1 (no bonus could fix it), finalizes immediately with nothing to offer - the die stays untouched either way. A fixable miss raises a new `BardicChoicePending` exception carrying a `PendingBardicChoice` snapshot (natural, total, target AC, die size, and everything `_finalize_attack_result` needs to complete the roll later) instead of finalizing. The event/Sneak-Attack/Smite/damage/unconscious-death-save tail that used to sit at the end of `_resolve_single_attack` was extracted into `_finalize_attack_result` specifically so both the normal synchronous path and the later resume path call the exact same code - no second copy of that logic to keep in sync, the same "extract once" discipline this project always follows for a shared tail.

`api/ws/session.py` catches `BardicChoicePending` in the human-facing action loop, stores the choice on a new `Session.pending_bardic_choice`, broadcasts a `bardic_inspiration_offer` message (visible to the whole party, so no one's left guessing why play paused, but answerable only by the connection controlling the holder - the same authorization discipline issue #44 already established), and returns early - deliberately skipping the trailing autoplay/awaiting_input calls, so the session genuinely stays paused until a `bardic_inspiration_response` message resumes it. A new `resolve_pending_bardic_choice` (turn_engine.py) does the actual resume: declining finalizes the already-known miss at zero cost (the die stays banked - real SRD only spends it the moment it's actually rolled); accepting rolls a fresh die, rebuilds the result, and hands off to `_finalize_attack_result`. `_autoplay_non_human_turns` gets its own, simpler catch for the same exception: an AI-controlled companion holding a die has no human to ask, so it auto-decides yes immediately, reproducing this project's own pre-#53 behavior exactly for that case.

**A real bug caught by the test suite, not live**: the original attempt that raises `BardicChoicePending` never reaches `resolve_action`'s own tail (the victory-check/turn-advance calls at the very end of its dispatch) - it's aborted mid-flight by the exception. `resolve_pending_bardic_choice` has to replicate that tail itself (`_check_victory_defeat` then `_advance_turn_skipping_dead`, since `attack` always ends the turn), or the resumed game state would show the holder's turn as still active even though their attack - which always ends the turn - had genuinely finished. Caught by a straightforward assertion in the very test written to lock in the accept-the-offer flow, not found live.

9 new test functions: 4 pure-engine cases added to `test_turn_engine_class_features.py` (declining keeps the die banked at no cost; a natural 1 never offers; an already-hitting roll never offers; an Extra-Attack-eligible PC still auto-applies immediately, no offer at all) plus a turn-genuinely-advances assertion added to the existing accept/decline tests (the regression check for the bug below), and 5 real WS-protocol-level integration tests in a new `test_ws_bardic_inspiration.py` (the offer message's exact shape and pause behavior; accepting/declining through a real `bardic_inspiration_response` round trip; a stale response rejected when nothing's pending; a response rejected from a connection that doesn't control the holder - using a small `_FakeWebSocket` stand-in calling `_handle_client_message` directly for the last case, since two genuinely concurrent `TestClient` WS connections deadlock in Starlette's own transport, a known gotcha already documented in this file). Found and fixed two real setup bugs while writing these: `build_graph()` needs `rng=` passed explicitly or it silently uses real, undeterministic randomness instead of the test's own fixed sequence (the existing `test_ws_multi_action.py` precedent never needed this, since that file's own scenarios don't roll dice); and a fixture goblin's real 7 HP died outright from the test's own boosted hit, flipping `game_state.status` to "victory" and silently short-circuiting `_send_awaiting_input`, hanging the test on a message that would never arrive - fixed by giving the fixture generous HP, the same lesson this project's Phase 9I entry already recorded for an unrelated damage-bonus test.

635 tests passing (up from 626 before this pass), `ruff`/`mypy` clean. Frontend: `sessionClient.ts` gained the `bardic_inspiration_offer` message type and a `sendBardicInspirationResponse` function; `LivePlay.tsx` renders a prominent, blocking prompt (new `.bardic-inspiration-offer` styling) to the holder's own connection with "Use it"/"Save it" buttons, and a small informational line to everyone else in the party so the pause doesn't read as the game hanging. `tsc -b`/`oxlint` clean.

Live-verified against the real running server through a real two-human lobby session (a Bard PC + a deliberately low-STR/low-AC Fighter PC, driven via two real WS connections): confirmed live that casting Bardic Inspiration through the real intent parser correctly banks the die, and that a natural-1 attack correctly skips the offer entirely with the die staying banked - the full offer-and-accept round trip wasn't captured in this same live pass (the intentionally fragile test Fighter went down and the party was defeated by goblins before a second, non-natural-1 attack could land), so that half rests on the 18 automated tests above, which exercise the identical `_handle_client_message`/`resolve_pending_bardic_choice` code the live server runs, not a mock of it.

**Issue #30's deferred second phase, closed - Prepared casters (Cleric/Druid/Wizard/Paladin) now choose and are restricted to a real spell list, the same as "Spells Known" casters already were.** Live-prompted: the user pointed out a Cleric was "hardly playable without spells" - `cast_spell` had zero restriction for these four classes, since issue #30 explicitly scoped "Prepared" casters out as a deferred phase (they have standing access to their *entire* class list in real SRD, then choose a working subset - a genuinely different mechanic from Bard/Sorcerer's fixed per-level "Spells Known" table).

New `character_creation.PREPARED_CASTER_CLASSES = {"cleric", "druid", "wizard", "paladin"}` and `prepared_spell_count(class_index, level, ability_mod)` - the real SRD formula (spellcasting-ability modifier + caster level, minimum 1), not a flat table, since it genuinely depends on the player's own ability scores. Paladin is a half-caster (issue #21): caster level is character level // 2, and the whole formula floors at 0 (not 1) below character level 2, matching `SPELL_SLOTS_BY_LEVEL["paladin"][1] == {}` - a level-1 Paladin correctly needs zero prepared spells.

**The interesting design problem, and why this needed its own wizard-step placement rather than reusing "Spells Known"'s existing spot:** the required count depends on ability scores, which aren't assigned yet at the Class & Skills step where the Bard/Sorcerer picker already lives - `create_character`'s own prepared-caster validation block had to move to *after* `final_scores` (post-racial-bonus) is computed, and the frontend picker moved to the Ability Scores step instead, gated on `allAbilitiesAssigned` and computing the live count via a new `preparedSpellCount()` TS mirror + `ClassDetail.spellcasting_ability` (a new field - the class's real SRD spellcasting ability, exposed specifically so the frontend can derive the count itself once scores are in). `class_spell_indices`/`known_spells_pool` needed zero new logic - reused exactly as-is (already "every real level-1 spell this class can access," independent of which restriction mechanic applies).

DB persistence (`Character.prepared_spells`, migration `a9c7ddc3736e`) added in the same pass with a `server_default`, not found live after the fact - this project's now-established discipline since the `known_spells`/`equipped_weapons` gaps were each found live in earlier sessions. `turn_engine._resolve_cast_spell` gained a second restriction block mirroring the existing Known-caster one exactly, just checking `prepared_spells` instead.

**The real cost of this feature wasn't the mechanic itself - it was the test blast radius.** Making Wizard a restricted class broke ~30 test files across the whole suite (166 initial failures), because Elrond (a Wizard) and Mira (a Cleric) are this project's two canonical "any spellcaster, cast any spell for mechanic testing" fixtures, duplicated across a dozen-plus files (`_two_person_party()`'s own copy in each, plus `src/cli/play.py`'s `build_demo_party()` - the *production* demo/fallback party, not just a test fixture). Every one of them was casting spells that don't actually belong to Wizard/Cleric's real SRD list (Hold Person, Cure Wounds, Scorching Ray, Call Lightning, Shield of Faith, Guiding Bolt...) - which only ever worked because there was no restriction at all before this issue. Fixed each fixture with real level-1 spells from the actual class list where the test's own cast matched a real option, and - for the genuine mismatches (spells above level 1, or belonging to a different class than the fixture's own) - poked the spell directly onto `.prepared_spells` post-construction, the same "poke state directly rather than build a whole new fixture" pattern this file already used for `spell_slots[2] = 1` (Hold Person is 2nd level). A human ability-score racial-bonus trap caught two tests along the way: Gandalf (Human, +1 to every ability) needs 4 prepared spells where Elrond (Elf, no INT bonus) needs 3 - the same base ability scores producing a different required count purely from race.

Sister Mira (the one Cleric companion) authored with `chosen_prepared_spells: [cure-wounds, healing-word, bless, shield-of-faith]` - two heals (one bonus-action), a support buff, and an AC buff, matching her battlefield-medic persona; WIS15+human's own +1 -> mod+3 -> exactly 4 required. Live-verified end to end through the real wizard and a real WS session: a fresh Cleric's prepared-spell picker appeared correctly in the Ability Scores step (4/4 required after "Fill recommended"), the live character sheet showed a new "Prepared spells" section, casting Guiding Bolt (a real Cleric spell, just not one she'd prepared) was rejected with a clear "hasn't prepared Guiding Bolt" error, and casting Cure Wounds on herself succeeded and healed her for the real rolled amount.

**Environmental false alarm while verifying the above: `uv run pytest -q` (the full suite) appeared to hang indefinitely - 3+ minutes with zero output, twice in a row - immediately after live-verifying the Prepared-caster feature through the real dev servers.** Bisected methodically rather than assumed: every individual WS-real-server test file (`test_ws_session.py`, `test_ws_lobby_multiplayer.py` - the two that spin up an actual `uvicorn.Server` thread) passed cleanly alone and together in ~13s each; the *whole* suite only hung when run alongside the two live `preview_start`-managed dev servers (backend + frontend) plus Ollama, all sharing this machine's already-tight RAM (see Day-16's own "only 15.9GB total" note). Stopping both preview servers before re-running immediately fixed it - 636 passed in 19s, no code changes involved. Not a deadlock, just severe resource contention masquerading as one; worth remembering to stop the live preview servers before a full-suite run on this machine, not just after finishing live verification.

**Issue #57 (Bless) done.** Live-reported follow-up to the spell-audit work: `cast_spell` had no mechanic for Bless (real SRD: up to 3 targets each get to roll a bonus d4 on every attack roll and saving throw for up to 1 minute, concentration). Unlike the individually-implemented spells before it (Sleep/True Strike/Mage Armor/Shield of Faith/Spare the Dying), this one turned out to be a clean fit for the *existing* "condition" mechanic (`_CONDITION_SPELLS`, built for Invisibility) rather than needing a new dispatch shape - it already loops over every id in `action.targets` independently and applies a `ConditionSpellSpec`, so a `"blessed"` tag added to `ConditionName` plus one new table entry (`"bless": ConditionSpellSpec(condition="blessed", duration_rounds=10)`) got the *casting* side working for free, targeting-cap-and-all (real SRD's own "up to three creatures," not separately enforced - same as every other targets-count in this engine).

The actual new work was the *mechanical* side: a repeatable, ongoing d4 bonus is a different shape from anything this project had - Bardic Inspiration's die is single-use and banked, True Strike's advantage is a one-shot flag. New `rules.blessed_bonus(character, rng) -> int` rolls 1d4 if `has_condition(character, "blessed")`, else returns 0 with zero RNG consumed (so every pre-existing fixture's exact RNG sequence stays untouched) - computed once per roll by the caller and threaded into both the bonus total and its debug-mode breakdown so they can never disagree, the same discipline `attack_bonus_breakdown` already established elsewhere. Wired into all 4 real roll sites: `_resolve_single_attack` and `_cast_attack_spell_at_target` (the actor's own attack bonus, via `dataclasses.replace` since `AttackParams` is frozen) and `_target_saving_throw_bonus`'s two call sites, `_cast_save_spell_at_target` and `_check_concentration_break` (the target's own saving throw) - deliberately including concentration saves (a real saving throw per SRD) and deliberately excluding death saves (already flat/unmodified by this engine's own design, a precedent from Phase 9, not a new one).

6 new tests in `test_turn_engine_spell_audit.py`: casting applies "blessed" to up to 3 targets, the same-side rejection (mirrors Invisibility's), a blessed attacker's roll includes the d4 (and an explicit unblessed-attacker control proving zero RNG is consumed when not blessed - the regression guard for every other fixture in the suite), and a blessed concentration save clearing a check that would have failed without the bonus. 641 tests passing (up from 635 before this session's Prepared-caster + Bless work), `ruff`/`mypy` clean.

Live-verified against the real running server (not just the offline suite): cast Bless on a companion via real free text through the actual intent parser - correct narration ("...granting Fenwick Quickfingers a blessed aura..."), a "blessed" condition badge appeared on his live character sheet, the caster's sheet correctly showed "Concentrating on: Bless" with the spell slot consumed, and three separate concentration-save events fired correctly across the next few rounds of real damage (visible in the debug-mode breakdown). The one thing not captured live: watching the blessed companion's own attack roll show the +1d4 bonus - the party went down in a losing fight before his turn came up again, and by then Ollama had slowed to ~25s per call (confirmed directly, unrelated to this change - this machine under simultaneous load from two live dev servers, the SD-Turbo image pipeline, and a full pytest run). That mechanic rests on its own exact hand-computed test instead, which exercises the identical `_resolve_single_attack` code path the live server runs.

**Story-adaptive encounters initiative, phase 1 of 3 - situation-matched battle-map generation.** The user's own framing, kicking off a new direction: make the game more interesting from a story perspective - a model-led, adaptive campaign that considers player choices, encounters whose grid matches the actual narrative situation, and a shared-input mode where the whole party weighs in on a narrative beat before the story continues. Scoped into three phases in that order (grid generation first since it's cheap and self-contained, then the shared-input narrative mode, then full adaptive story generation last since it depends on both) - this entry is phase 1.

Checked the actual state before proposing anything: even `campaign_generator.py`'s existing "generated" campaigns never generated a battle map at all - `_generated_battle_map()` was a single fixed 5-row template reused for every fight, with only `monster_index`/`monster_count` actually varying. `campaign.py`'s own docstring had already anticipated needing this kind of adaptivity ("Phase 8's planned dynamic campaign adaptation needs to splice new scenes into this chain mid-session... nothing here should assume the chain is fixed") - that label turned out to be a stale/renumbered reference to the old multiplayer initiative, not an active plan, but the underlying intent (keep the scene chain as plain data, not resolved into anything rigid) was exactly the right foundation to build on.

**Design: never let the LLM touch a raw grid.** Every other time this project asked a 7B model to reason about exact coordinates (closest-enemy resolution, move-target pathing, cast_spell's target field), it needed a deterministic Python layer underneath to be reliable - direct coordinate output was never trustworthy enough to hand off wholesale. Applied the same split here: new `src/engine/battle_map_templates.py` (pure, LLM-free, same "engine" boundary as everywhere else) turns a small closed-set of choices - `layout` (open_room/narrow_corridor/two_rooms/cluttered), `size` (small/medium/large), and `difficult_terrain`/`hazard` amount (none/light/heavy) - into a real `BattleMap`, deterministically. The LLM's only job is picking from those enums (now extra fields on `campaign_generator._build_schema`'s existing `GeneratedCombatBeat`, reusing the exact call/retry/validation loop that already generates the rest of a campaign - no new LLM round-trip needed), conditioned on the same `narrative_intro` text it's already writing for that scene.

**Every layout is connected by construction, not validated after the fact.** `_generated_battle_map`'s own retired docstring had already taken this stance for its one fixed shape ("no walls - minimizes the risk of a procedurally-placed spawn point ending up unreachable") - extended it rather than adding a BFS reachability check as a safety net: narrow_corridor's walls are confined to the very top/bottom rows (every interior row fully open), two_rooms leaves exactly one explicit doorway row in its dividing wall, and open_room/cluttered never place a wall at all (cluttered's texture comes purely from a heavier difficult-terrain scatter). A real BFS connectivity test in `test_battle_map_templates.py` checks this holds for all four layouts anyway, at the tightest textured (`heavy`/`heavy`) setting, rather than just trusting the construction argument - the one property that actually matters for "can the party reach the monsters."

Old `GeneratedCombatBeat` fixtures across every existing `test_campaign_generator.py` test predate these four new fields - rather than update every one, gave each field a Pydantic default reproducing the old fixed template's own shape (`open_room`/`medium`/`none`/`none`) so an older or degenerate model response still resolves to a sane encounter instead of failing schema validation. All 5 pre-existing tests passed completely unmodified; 2 new ones cover the real generated-choice path and the default-fallback path explicitly.

12 new `test_battle_map_templates.py` tests (pure structural fixtures - dimensions, wall placement per layout, spawn points never overwritten by scattered terrain even at the tightest heavy/heavy setting across 10 seeds, the BFS connectivity check) plus 2 new `test_campaign_generator.py` cases. 655 tests passing (up from 641), `ruff`/`mypy` clean.

Live-verified against the real teacher model, not just the mocked tests (this is genuinely new LLM-facing behavior, matching this project's "LLM code gets live verification instead of mocking" stance) - generated 3 real campaigns end to end and rendered each resulting battle map as ASCII to eyeball layout sanity: a "step into the clearing" giant-rat fight got an open, unwalled small room; wolves "circling the party" in "the woods" got a medium open room with light difficult-terrain texture; cultists performing a ritual in an enclosed space got a walled, textured small layout. Real variety, no crashes, no malformed maps across all three - the narrative-to-layout mapping itself won't be perfect every time from a 7B model (the cultist-ritual pick read as "enclosed room" more than the prompt's literal `narrow_corridor` framing), but that's a prompt-tuning question, not a correctness one - every structural guarantee (reachability, spawn-point safety) is Python's job here, not the model's, and held across all three real runs.

**Story-adaptive encounters initiative, phase 2 of 3 - shared party input for narrative decision points.** Phase 1 gave the LLM a closed vocabulary for battle-map shape; this phase gives the whole party (not just whoever's turn it happens to be) a real voice at a narrative decision point, with the model synthesizing a continuation from what everyone said - still without touching the scene chain's own fixed structure (that's Phase 3's job).

**Design: a new scene type, not a flag.** `party_choice` joins `SceneType` alongside `short_rest`/`long_rest`/`skill_challenge` - same established precedent (`campaign.py`'s own docstring already argues for this: "the campaign author's intent... is baked into the scene chain itself, same as everywhere else in this schema") rather than a boolean flag on `narrative_beat`. `campaign_runner.advance_to_next_encounter` stops the chain-walk at one exactly the way it already stops at `combat` - it has no way to resolve a party_choice itself (no deterministic mechanic, needs a live session and an LLM call), so it just hands the scene back with its own `narrative_intro` already in the returned narration log, identical contract to a combat stop.

**Session-level pause, not a GameState field.** A party_choice scene has no turn order, no battle map, nothing for `GameState` to represent - so, mirroring issue #53's `PendingBardicChoice`/`BardicChoicePending` precedent (a session-level pause that isn't modeled inside the engine's own state), a new `PendingPartyChoice` (session.py, not turn_engine.py this time - nothing here is raised as an exception from deep engine code, campaign_runner just returns cleanly, so there's no need for the exception-based control flow bardic inspiration needed) sits on `Session.pending_party_choice`. `_start_party_choice` generates every living companion's own in-character reaction immediately and synchronously (a new, narrower prompt - `generate_companion_party_choice_response`, deliberately NOT `player_agent_node`'s combat-turn prompt, which demands exactly one mechanical verb, the wrong shape for a narrative opinion) and broadcasts a `party_choice_offer` naming which human seats are still pending. A new `party_choice_response` WS message (no `character_id` in the payload - derived from whichever human seat the sending connection controls that hasn't already answered, same "derive the actor from server state, never from client claims" discipline `bardic_inspiration_response` already established) fills those in one at a time; `_resolve_party_choice` fires the instant every living human has answered, synthesizes one continuation narration (`synthesize_party_choice_narration`, a second new prompt) from every party member's stated input, and resumes the chain from the scene's own `next_scene_id` - fixed either way, no branching, matching skill_challenge's own "no branching in MVP" stance.

**Refactored `_advance_campaign_after_victory`'s while-loop into a recursive `_advance_chain_from` shared by both callers that now need to "keep walking the data until something needs a live pause"** - the original loop only ever needed to handle "combat or end of chain"; adding a second kind of pause point (party_choice) without a shared helper would have meant either duplicating the walk in `_resolve_party_choice` or losing the loop's own "companions alone can finish a trivial fight and chain straight into the next scene" behavior. The recursion reproduces that exactly: after auto-playing a freshly-built encounter, if it resolved in victory on its own, it recurses into the next scene instead of stopping.

**Scope cut, documented not silent: a party_choice scene can't be the very first scene in a campaign, before any combat.** `_build_real_session_setup` builds a session's initial combat GameState synchronously, before any WebSocket connection exists - there'd be nowhere to broadcast an offer or collect a response from. If a campaign's own initial walk stops at a party_choice instead of combat, it falls back to the demo encounter the same way "no combat scene at all" already does, with a loud comment rather than a silent mishandling. No authored campaign needs this yet - the new content lives well after the first fight (see below).

**Real content, not just a synthetic fixture**: `kobold_warren_full.yaml`'s own `rising_action` scene - the beat where the party finds a bandit's signet ring and realizes the kobolds were hired muscle - changed from `narrative_beat` to `party_choice`. It already read as a genuine decision point before this phase; now it actually asks the table what they make of it before the story moves on to scouting the bandit hideout.

**A pre-existing test deadlocked the instant this content change landed - not a Phase 2 bug, but a real regression in test coverage caught immediately.** `test_continue_campaign_chains_to_the_next_encounter` drove `kobold_warren_full` straight through to `hideout_combat` in one `continue_campaign` call - now blocks forever on `ws.receive_json()` waiting for a `party_choice_response` nobody sends, since the chain genuinely pauses at `rising_action` now. Fixed by teaching that test (and the direct-call `_advance_campaign_after_victory` test, and the all-companion-party test) to answer the pause the same way the new dedicated party_choice tests do - a good reminder that changing existing campaign content can break a test that never touched the code being changed at all.

**A second, more interesting bug found live, in the browser, not by the test suite: nothing stopped a second `continue_campaign` (or a `rest` request) from firing while a party_choice was already pending.** `session.game_state.status` stays `"victory"` for a party_choice's *entire* duration - it's deliberately never touched, since there's no combat happening - which meant the existing `status == "victory"` guards on both `_advance_campaign_after_victory` and `_handle_rest_request` had nothing to say no. Live-verified in a real browser: after a party_choice fired, the "Victory! Short Rest / Long Rest / Continue" banner from the *previous* stop was still sitting on screen, fully clickable, right next to the new party-choice panel - clicking Continue again would have silently re-walked the chain from the same `current_scene_id`, re-triggering the identical short_rest and a *fresh* `_start_party_choice` call that overwrites `session.pending_party_choice` (and whatever responses were already collected) with a brand-new offer. Fixed with an explicit `session.pending_party_choice is not None` check added to both guards, plus hiding the rest-controls block client-side whenever a party_choice is active - the same two-layer fix shape (a real server-side rejection, not just a UI nicety) this project always reaches for. A new regression test (`test_continue_campaign_and_rest_no_op_while_a_party_choice_is_pending`) locks in both halves: a rest request during the pause gets the same clear rejection combat already gets, and a second continue_campaign leaves `session.pending_party_choice` as the exact same object, untouched.

18 new/changed tests (1 campaign_runner stop-at-party_choice case, 3 existing tests taught to answer the pause, 3 new: the full pause-and-resume round trip, the rejection-on-bad-sender case, the re-entrancy guard) - 659 tests passing (up from 655), `ruff`/`mypy`/`tsc -b`/`oxlint` clean (same 3 pre-existing oxlint warnings, none new).

Live-verified extensively against the real teacher model and a real browser session, not just the stubbed offline tests (this is genuinely new LLM-facing, multi-turn interactive behavior): called the two new prompt functions directly against real companions first (Grom's aggressive "storm that hideout" vs. Silvana's more measured "follow the map," synthesized into one coherent paragraph naming both without contradicting either) before touching the UI at all. Then played a real character through `kobold_warren_full` end to end in the actual React app - won the kobold fight, hit the Victory stop, clicked Continue, watched the real party-choice panel render with Grom's and Mira's real in-character reactions, confirmed the stale rest-controls banner was correctly gone (the live-found bug above), typed and submitted a real response through the actual form, watched the synthesis narration broadcast and the chain continue through the scout-the-hideout skill challenge into the bandit-hideout fight - the full loop, no shortcuts. Separately hit (and worked around, not fixed - a testing-environment artifact, not application code) React StrictMode's dev-mode double-connect opening two WebSockets to the same session, one of which silently swallowed a handful of `computer`-driven browser actions with no error - diagnosed by opening a raw WebSocket directly and confirming the server-side session state was correct the whole time, same "the real DOM/raw connection is ground truth, not a flaky tooling layer" lesson this file has recorded several times before (Day 20/21, Phase 9-era).

**Story-adaptive encounters initiative, phase 3 of 3 - full model-led story generation.** Phase 1 gave the LLM a closed vocabulary for battle-map shape; Phase 2 gave the whole party a real voice at a decision point, but the scene chain itself stayed fixed - only the narration reflected the party's choice, not where the story actually went. This phase closes that gap: an "open" party_choice scene (no authored `next_scene_id`) now hands control to the model, which generates what happens next live, genuinely conditioned on what the party said.

**Design: reuse generate_campaign's whole machinery, seeded from a live moment instead of a premise.** `campaign_generator.generate_continuation` (new) shares `_build_encounter` outright and mirrors `_build_schema`/`_validate_generated`'s shape closely (`_build_continuation_schema`/`_validate_continuation`) rather than inventing a second generation pipeline - same dynamically-built monster/skill `Literal` enums, same validate-and-retry loop, same battle-map generation from Phase 1. The one new schema addition is a `"party_choice"` scene type the whole-campaign generator doesn't have: a generated continuation may itself end in a fresh party_choice, deliberately left with `next_scene_id` unset - the exact same "open" marker that triggered this generation in the first place. This makes the mechanism **self-similar by construction**: `_resolve_party_choice` (session.py) needs zero special-casing to handle "the model's own generated choice gets answered later" - it's not a different code path, it's the identical one recursing.

**Bounded, not infinite.** `config.MAX_ADAPTIVE_GENERATIONS` (default 3) caps how many times one session can chain a live generation - a narrative pacing bound, not a resource-safety one (this project already has separate, harsher resource caps - `MAX_CONCURRENT_SESSIONS`, the action rate limiter - for the actual GPU/Ollama contention concern). The generation that hits the cap gets `force_ending=True`, told to actually conclude the story rather than the session just silently refusing to continue - and, matching this project's now-established "a hard constraint gets enforced structurally, not just asked for nicely" stance (same reasoning as `monster_index`/`skill` being closed enums, not free text), `force_ending` is a real validation rule (`_validate_continuation` rejects any party_choice scene when set), not only a prompt instruction.

**Two real generation-quality bugs found on the very first live test, both fixed before this ever reached the browser.** (1) Asking the model for "a genuine, satisfying conclusion" within the ordinary 1-3 scene budget reliably produced 4+ scenes, three validation failures in a row - "bring this to a close" reads as license to spend *more* scenes wrapping up, not fewer, even with the same numeric limit restated right next to the instruction. Fixed by asking for **exactly 1** scene when concluding instead of "still 1-3" - removes the ambiguity outright rather than fighting it with stronger wording, and it's simply the right shape for an ending anyway (a hand-authored campaign's own final scene is already just one narrative_beat). (2) A generated combat scene's `encounter_ref` came out double-prefixed (`live_verify__live_verify__adaptive1_2`) - `generate_continuation`'s own `scene_id` already bakes in `campaign_id` (needed for cross-generation uniqueness within one campaign's scene list, unlike `generate_campaign`'s bare `scene_N` ids), so reusing `generate_campaign`'s own `f"{campaign_id}__{scene_id}"` pattern verbatim duplicated it. Harmless functionally (write and read both used the same string consistently) but sloppy - fixed by using `scene_id` directly as the ref.

**A third, more interesting bug found only by playing a real session all the way through to a real ending - not caught by any offline test, because no offline test had ever driven a campaign past a *non-combat* conclusion before.** `session.current_scene_id` only ever advances inside `_advance_chain_from`'s combat branch - it has no reason to update when the walk ends on a pure narrative scene instead. Before this phase, that was harmless (an authored campaign's own trailing `narrative_beat` never needed re-visiting). Phase 3 made it a real problem: a story that concludes via model-generated narrative alone (no further combat) leaves `current_scene_id` frozen at the *last combat that actually happened*, with `game_state.status` still `"victory"` and the "Continue" button still showing client-side. Clicking it again would silently re-walk the whole chain from that stale point - re-resolving the same already-answered party_choice from scratch, generating an entirely fresh (and likely incoherent, contradicting what already happened) continuation, spending a real LLM call for nothing good. Live-reproduced in the browser: after a real 3-round adaptive story concluded, reloading the page showed "Victory! ... Continue" sitting right next to "the adventure has concluded" would have needed to say instead.

Fixed with an explicit `Session.campaign_complete: bool`, set the instant the chain genuinely runs out (ran off the end with nothing left, or hit the generation cap with no combat scene to fight through) and included on every `state_update` - not a one-shot broadcast, so a late-joining or reconnecting client learns it too, the same "don't assume whoever's currently connected is the only one who needs to know" discipline issue #46's reconnect-banner work already established. `_advance_campaign_after_victory` now also no-ops once set (alongside its existing party_choice-pending guard from Phase 2 - the same shape of problem, found the same way, one phase later). Frontend: the Continue button is hidden once `campaignComplete`, applied in lockstep with the paced narration log exactly like `gameStateSnapshot`/`combatSummariesSnapshot` already are (so the button doesn't vanish before the player has actually read the ending that explains why) - Rest stays available, since resting after the story's own final battle is harmless and still narratively reasonable.

12 new/changed tests (6 `generate_continuation` unit tests - chaining, the self-similar party_choice-ending case, the not-last-scene retry, the force_ending-rejects-any-party_choice case, exhausted retries, prompt-content verification; 6 `session.py` tests - the direct-call generation trigger, the generation cap, force_ending on the last allowed round, a full WS round trip, and the live-found campaign_complete regression reproducing the exact stale-Continue-click bug) - 670 tests passing (up from 659), `ruff`/`mypy`/`tsc -b`/`oxlint` clean throughout (same 3 pre-existing oxlint warnings, none new).

**Content: `kobold_warren_full.yaml`'s own ending changed shape.** The old `hideout_combat -> resolution` (a single fixed epilogue) became `hideout_combat -> hideout_aftermath` (a new, deliberately open `party_choice` scene - the party finds a bandit ledger naming an unaccounted-for backer, with `next_scene_id` left unset) - the campaign's own conclusion is now genuinely adaptive rather than a foregone one-line wrap-up, replacing the old static `resolution` scene entirely rather than leaving it as dead, unreachable content.

**Live-verified extensively against the real teacher model, not just the stubbed offline tests - this is the most structurally novel piece of the whole three-phase initiative, so it got the most scrutiny.** First, `generate_continuation` called directly with a hand-built situation and genuinely divergent party responses (Grom: "hunt down whoever's name is at the top of the ledger, no loose ends" vs. Mira: "bring it straight to the guard captain, let the law handle it") - the model produced a real branching continuation (narrative_beat -> a surprise combat scene, goblins and a wolf mid-fight with bandits -> another party_choice), with the combat scene's own encounter correctly written to disk and loadable.

Then a full real browser playthrough of `kobold_warren_full` end to end (fast-forwarded through both real combats via `debug_action` - bypassing only intent_parser's LLM call, not the real engine, narrator, or generation calls - after free-text move-to-target intermittently mis-targeted, a known, already-documented pre-existing limitation, not a Phase 3 regression). The human PC actually died in the second fight - which turned into an unplanned but genuinely valuable stress test: with `human_character_ids` now containing only a dead character, `_start_party_choice`'s "no living human seat" fallback (added defensively in Phase 2, never previously exercised live) fired correctly and repeatedly - the party's sole surviving companion, Grom, drove **three consecutive full rounds of choice-and-generation entirely on his own**, no human input possible or needed, each round's real, persona-consistent decision ("secure the camp and find who's behind this" -> "press on before they regroup" -> "take them by surprise") genuinely shaping what the model generated next. The third round correctly hit the generation cap and force_ending held - the model produced no further party_choice, the story concluded, and the "Continue" button correctly disappeared behind the new `campaign_complete` flag (verified separately with a clean, non-fatal playthrough of `goblin_ambush_oneshot` to see a real victory-to-conclusion transition without a character death muddying the picture) - all without a single validation failure or malformed encounter across the whole live run.

**This closes out the entire story-adaptive-encounters initiative** (Phases 1-3) - started from the user's own framing ("let model lead the campaign, being adaptive and consider player choices... whenever there is interaction or choice available, all party members should be able to give their input... model should consider those choices and continue story considering those inputs") and delivered exactly that: situation-matched encounters, real shared party input at decision points, and - for any campaign willing to leave a choice genuinely open - a story that the model actually continues writing in response to what the table decided, not just narrates around a fixed skeleton.

**Action/bonus-action availability UI (live-requested, proactive UX ask) - green/yellow dots on every character card showing whether this turn's main action and bonus action are still available, fading once spent.** `Character.bonus_action_used` already existed; the main action had no equivalent concept at all. New `Character.action_used_this_turn: bool`, set unconditionally right after `resolve_action`'s own verb dispatch - `ends_turn` already *is* "did this verb consume the main action" for every verb except "move" (which spends the movement budget, never the action, and already leaves `ends_turn=False` for a plain move): a bonus-action verb (cast_spell/second_wind/rage/equip/offhand_attack/cunning_action/flurry_of_blows/martial_arts_strike/revert_wild_shape) returning `ends_turn=False` on success is the identical signal this field needs, confirmed by reading every one of those resolvers' own return statements rather than assuming. Resets in `_advance_turn_skipping_dead` on the exact same "only when the turn actually advances TO this character" schedule `bonus_action_used` already uses, for the identical reason (a bonus-action verb leaves the turn open for this same actor's own follow-up main action, which must still see it as already-spent). Not persisted to `CharacterRecord` - combat-turn-scoped, like the other three fields on this same reset schedule, correctly reset to its Pydantic default on a fresh encounter build. `LiveCharacter.action_used_this_turn` added (data already reaches the browser via `Character.model_dump()`, same "data already on the wire" pattern this file has recorded several times before - just needed declaring). `CharacterSheet.tsx` (the party sidebar, shown for every character in turn order including monsters) renders two small dots next to the AC line - green for the action, yellow for the bonus action, each faded to 25% opacity once used. 4 new hand-computed tests in `test_turn_engine_action_economy.py` (plain move leaves it False; a bonus-action spell leaves it False until the follow-up main action sets it True; an ordinary action-spell sets it True directly; it resets on the actor's own next turn after a full round-trip through the other actors). 674 offline tests passing (up from 670), `ruff`/`mypy`/`tsc -b`/`oxlint` clean.

**Live-found, and fixed in the same pass: monster/companion pathing treated every occupied square - ally or hostile - as fully impassable, when only a hostile's should be.** User report: "we should be able to move through the tile occupied with another ally, but cannot end our turn on occupied tile" - the second half was already correctly enforced (`turn_engine._resolve_move`'s own destination-occupancy check, issue-#-era fix, only ever checked the *final* square), but `monster_ai._best_step`/`approach_path` (reused by both `choose_monster_action` for real monster turns and, since issue #48, `intent_parser._resolve_move_target` for a player's own "move toward X" free text) built its `occupied` set from *every* other living character regardless of side, treating an ally's square exactly like a wall - forcing a detour around a friendly character standing in the way, or leaving the mover stuck short of its target entirely in a narrow space with no detour available. Real SRD allows moving *through* an ally's space, just not ending a move standing on it.

Fixed by splitting occupancy into two buckets: `approach_path` gained a second `ally_occupied` param (alongside the existing `blocked`, renamed from `occupied` for clarity) - a hostile square stays genuinely impassable (`_best_step` still excludes it from candidates, unchanged), an ally square is now a legal intermediate step, and the finished path has any *trailing* ally-occupied squares trimmed off the end (a `while` loop, not just the last element - though in practice the greedy walk only ever stops on one) so the mover never illegally lands on an ally, leaving them one square short in that specific case rather than either stacking on the ally or refusing to move through them at all. New `monster_ai.occupied_squares_by_side(game_state, actor) -> (hostile, ally)` - a single shared helper (both call sites need the identical "which side is this character on relative to me" split: monster vs. `is_pc`, or `actor.is_pc` vs. everyone else) so `choose_monster_action` and `_resolve_move_target` can't drift against each other. New regression test (`test_move_path_passes_through_an_ally_occupied_square_but_never_ends_there`) forces the path through a 1-row corridor with an ally directly in the middle and no detour available - proves both that the ally's square appears in the path (genuinely passed through) and that it's never the final square (never landed on) - the existing `test_move_path_avoids_a_square_already_occupied_by_another_character` regression test needed no changes (traced through by hand: its own scenario's greedy tie-break never actually selected the blocked square as "best" even before this fix, confirmed by it still passing unmodified). 675 offline tests passing, `ruff`/`mypy` clean.

**Live-found, and fixed in the same pass: thrown weapons (javelin, dagger, handaxe, spear, trident, light hammer) couldn't be thrown at all - rejected as "out of range" the instant a target was more than 5ft away.** Root cause confirmed directly against the vendored SRD data: every melee weapon's structured `range.normal` field is a flat 5ft regardless of the "thrown" property - the real, much longer throw distance (javelin: 30ft normal/120ft long) only exists in a separate `throw_range` field that `rules.weapon_range_feet` never read at all. Two weapons that already carry "thrown" but are classified `weapon_range: "Ranged"` in the SRD data (dart, net) were already unaffected - their real range comes from the same `range` field the fix doesn't touch, confirmed by checking their raw data directly rather than assuming every "thrown" weapon needed the same treatment.

New `rules.weapon_throw_range_feet(weapon) -> tuple[int,int] | None`, returning the real `throw_range` only for a weapon that's both melee-classified *and* has a `throw_range` field (the 6 real SRD thrown-melee weapons) - `None` for a plain melee weapon (nothing to throw) or an already-Ranged one (dart/net, whose normal range already covers it). `AttackParams` gained `thrown_range_normal_feet`/`thrown_range_long_feet`, populated only by `_pc_attack_params`'s weapon branch. The actual decision of "was this swing or a throw" is made per-attack, from the target's real distance, inside `_resolve_single_attack` (not baked into a fixed classification at declare-time): within the weapon's plain melee reach, completely unaffected (still melee, `is_ranged=False`, no engaged-in-melee disadvantage even if another hostile happens to be adjacent); beyond it, the throw numbers take over for range/long-range-disadvantage purposes, and - matching real SRD precisely, not just approximating it - the "Ranged Attacks in Close Combat" engaged-disadvantage check (Phase 9F) now correctly applies to a thrown attack too, since it's genuinely a ranged attack at that point, while a same-weapon melee swing moments later in the same fight still correctly doesn't trigger it. The ability-modifier/proficiency math itself needed no change - thrown weapons use identical STR-based (or finesse) rules whether swung or thrown, already correct via the weapon's fixed `weapon_range=="Melee"` classification, confirmed by re-reading that logic rather than assuming it needed touching too. 4 new tests (`weapon_throw_range_feet` pure-function cases in `test_rules.py`; three end-to-end `resolve_action` cases in `test_turn_engine.py` - a 30ft javelin throw succeeds, a 130ft throw beyond even the 120ft long range is still rejected, an adjacent javelin swing stays plain melee). 679 offline tests passing (up from 675), `ruff`/`mypy` clean.
